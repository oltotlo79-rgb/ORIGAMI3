# ORIGAMI3 作業規約

## このファイルの目的

- Claude Codeを統括役、Codex等を実作業役として、ORIGAMI3での役割分担・委譲・検査・記録の共通規約を定める。
- Claudeとすべてのコーディングエージェントは、作業開始前に本書を最後まで読む。利用者の最新の明示指示が本書より優先する。
- 理由: 役割の混同、粗い一括指示、検査不足によって、未完了・起動クラッシュ・利用者画面への警告表示が実際に起きたため。

## 参照すべき他の文書

- `docs/requirements-definition.md`: 要件の正本。特に§2「設計原則」を最優先する。
- `docs/implementation-roadmap.md`: タスク分割、対象ファイル、実装順、受け入れ基準の詳細を確認する。
- `docs/progress.md`: 現在の到達点、実測値、過去の失敗と対処を確認する。
- 本書には作業規律だけを集約し、機能要件や進捗の詳細は上記文書を重複記載しない。

## 1. 役割分担

- **実作業はすべてCodexが行う。** 実装、レビュー、デザイン、アプリのGUI操作、スクリーンショット撮影、資料の読み取り、PDF作成、環境の調査と修復を含み、作業の種類を理由に例外を作らない。
- **Claudeは「指示・確認・検査・git操作・アプリの起動と終了・利用者への報告と報告記録」だけを行い、自分で成果物を作らない。** 「自分でやったほうが速い」は委譲しない理由にならない。
- Claudeが行ってよい作業は次の6つに限る。
  1. Codexへ渡す指示書の作成
  2. Codexの成果物を読んで確認
  3. `scripts/check.ps1` の実行
  4. git操作（Codexは `.git` に書けないため）
  5. アプリの起動と終了
  6. **利用者への報告と、`docs/報告記録.md` への記録**（報告しただけでは完了ではなく、記録を書いて初めて完了）
- **アプリの起動・終了と実機確認の役割を、実名のコマンドで次のとおり固定する。**

  | 統括が行う | 担当が行う |
  |---|---|
  | `Start-Process desktop.exe` | 組み立て（`npm run tauri -- build --no-bundle`） |
  | `CloseMainWindow()` での終了 | CDPでの操作と撮影 |
  | git操作（commit / push / tag） | 実装・調査・検査 |

- **担当は `desktop.exe` を起動しない。** 配信サーバーを含むアプリの起動・終了も、担当へ指示・委譲しない。
- **実機確認は `npm run tauri -- build --no-bundle` で作った同梱版だけを使う。** 配信サーバーが不要になり、
  「誰かがサーバーを立てる」場面そのものが無くなる。debug版（`cargo build`）を実機確認に使わず、
  指示書に `cargo build -p desktop --release` と書かない。
- **担当への指示書に「アプリの起動方法」節を書かない。** 代わりに必ず次を書く。

  ```text
  アプリの起動と終了は統括が行います。あなたは起動しないでください。
  昇格承認を求める必要もありません。
  組み立てが終わったら、実行ファイルの実パスと組み立てコマンドを報告してください。
  ```
- 理由: 統括と実作業を分離しないと、確認者が不在になり、委譲すべき作業の抜けや自己判断による仕様変更が起きるため。

## 2. Codexの起動と使い方

- モデルは必ず **Sol Ultra** とし、リポジトリのルートで次の形で起動する。

```text
codex exec --model gpt-5.6-sol -c model_reasoning_effort=ultra --sandbox workspace-write --skip-git-repo-check - < 指示ファイル
```

- 作業フォルダを一時フォルダへ移さない。リポジトリ外から起動すると、対象ファイルへの書き込み権限を失うことがある。
- 可能な限り複数エージェントを並列で起動する。ただし、工程や検査を飛ばしたり、品質を落として速度を稼いだりしない。
- 並列実行する各エージェントの指示には、**触ってよいファイル**と**絶対に触らないファイル**を実パスで明記する。作業ツリーを共有するため、担当の重複も避ける。
- Codexにcommit、push、checkout、merge等のgit操作をさせない。変更内容と対象ファイルだけをClaudeへ報告させる。
- 理由: 誤った作業場所による書き込み失敗、共有ツリー上の変更衝突、Codexの `.git` 書き込み失敗が起きるため。

## 3. Codexへ渡す指示の書き方

- 粗い依頼は禁止する。指示書には必ず次の7項目を含める。
  1. 対象のファイルパスと関数名を、略称ではなく実名で記す。非コード作業でも関数名欄を省かず、「該当なし」と理由を記す。
  2. 合格条件を数値で記す（例: 「平らに畳めない点の警告が0」「裂けの量が `1e-6` 未満」「16層で200ms以内」）。
  3. 変更・起動・緩和してはいけないものを列挙する。
  4. 作業を段階に分け、各段階で中間報告を義務づける。
  5. 関係する過去の失敗例と、その原因を記す。
  6. 使用可能な道具について、コマンド、対象、確認方法まで具体的に記す。
  7. 成果物の保存先パスを明記する。
- 達成単位は小さく区切る。「144手順すべて」のような大目標を1回で依頼しない。
- 理由: 大きく曖昧な依頼は何度試しても完了せず、合格の自己判断や過去の失敗の再発を招いたため。

## 4. 委譲後の停滞監視

- Codexへ委譲すると同時に `scratchpad/watch-codex.ps1` を並行して走らせ、完了まで監視する。
- 「プロセスが生きている」「CPUを使っている」ではなく、成果物の更新時刻・サイズ・内容が変化しているかで進行を判定する。空回りでもプロセスとCPU使用は継続する。
- 確認間隔は **10分** とする。短すぎる監視でトークンを浪費しない。
- 停滞や異常は利用者が気づく前に検知し、原因確認、指示の縮小・修正、再委譲を行う。
- 理由: プロセスだけを見た監視では空回りを見逃し、利用者の指摘まで不具合が放置されたため。

## 5. 禁止事項

- **Codexにブラウザの窓を開かせない。** Playwright等のブラウザ自動化も使わせない。利用者の画面に警告付きの窓が実際に出たため。
- **`desktop.exe` を二重起動しない。** GUI確認時は既存プロセスの有無を確認し、1つだけを使う。後から起動したプロセスがデバッグポートを取得できずクラッシュし、エラーポップアップが出たため。
- **`Cargo.toml`、`Cargo.lock`、`vendor/` を無断で変更しない。** 依存関係の変更が必要なら作業を止め、理由と差分案をClaudeへ提出する。Claudeが差分を読んで採否を決める。`wry` のパッチ変更で起動クラッシュが発生し、取り消した経緯があるため。
- **ビルド途中のCodexを強制終了しない。** ビルドの終了を待ってから停止する。途中終了で実行ファイルが壊れ、起動クラッシュが起きたため。
- **テストの期待値を安易に書き換えて赤を消さない。** 性能テストの上限値を緩めることも禁止する。不具合を直さず合格に見せかけることになるため。
- **`#[allow]` で警告を黙らせない。** 警告の原因を修正する。問題を隠すと後の変更で欠陥が顕在化するため。

## 6. コミットとプッシュ

- git操作はClaudeだけが行う。Codexの成果をClaudeが検査した後、**タスクごとに都度**コミットし、`https://github.com/oltotlo79-rgb/ORIGAMI3.git` の `main` へプッシュする。
- コミットメッセージは日本語で、何を変え、何ができるようになったかが具体的に分かるように書く。利用者以外にも通じる言葉を使い、専門用語は極力避ける。
- `feat:`、`fix:` 等のプレフィックスは使わない。
- Codexの成果をコミットする場合は、本文末尾に次の行を付ける。

```text
Co-Authored-By: Codex <noreply@openai.com>
```

- 理由: タスク単位の追跡と復旧を容易にし、実作者を明示するため。Codexは `.git` に書けないため、操作はClaudeが担当する。

## 7. コミット前の品質ゲート

- コミット前にClaudeが自分の環境で `scripts/check.ps1` を実行し、cargo test、clippy（警告をエラー扱い）、画面のbuild、lint、testの**全5検査**を通す。
- Codexの「合格しました」という報告を鵜呑みにせず、Claudeが再実行結果を確認する。
- Claudeは `git diff` の実際の差分を読み、指示した各項目との対応、実装漏れ、対象外変更、勝手な仕様変更がないことを照合する。
- CI（`.github/workflows/ci.yml`）とpush前フック（`scripts/hooks/pre-push`）でも同じ品質条件を自動検査する。自動検査があることを、手元の確認を省く理由にしない。
- 理由: 自己申告だけでは検査環境の差や指示漏れを発見できず、不完全な変更が取り込まれるため。

## 8. 設計の規律

- **f64 + 明示的な許容誤差を原則とする。** 唯一の例外は展開図の頂点座標で、必要性を実測した場合に `Q(√2)` で厳密に扱ってよい。3D計算、たわみ、衝突判定、描画は常にf64とする。全面的な厳密演算がORIGAMI2を巨大化させたため。
- **止めずに警告する。** 不収束、めり込み、平坦条件違反があっても操作をブロックするゲートを作らない。ORIGAMI2では判定保留で操作が頻繁に止まったため。
- **常設UIは4区画に固定する。** ツールレール / 2D展開図 / 3Dビュー+タイムライン / コンテキストパネル（1枚）から固定区画を増やさない。多数の要素を平置きしたORIGAMI2の画面が複雑で使いにくかったため。
- **フロントの状態はZustandストア1本に一元化する。** コンポーネントの `useState` はホバー等、表示専用の一時状態だけに使う。状態の分散が保守を困難にしたため。
- **3D状態は保存しない。** 「展開図 + 折り手順」から再生して導出する。保存状態の不整合を避け、同じ手順を再現できるようにするため。
- **表現の完全性を最優先する。** 実際の紙で折れる操作はすべてアプリで表現できなければならない。折れない操作が見つかった場合は利用者側の制約にせず、アプリの欠陥として実装で解消する。個別技法だけの継ぎ足しでは未知の折り方を表現できないため。
- **説明を読まなくても直感的に触れるUIにする。** 紙を直接操作でき、結果と現在の操作状態が画面から分かることを優先する。操作方法を知らないと使えない画面は設計アプリとして実用にならないため。
- 詳細と優先順位は `docs/requirements-definition.md` §2を正とする。

## 9. 数値上限の撤廃（2026-08-08）

- 行数、Tauriコマンド数、ツールボタン数、ダイアログ数に数値上限を設けない。過去の上限値を合否条件として復活させない。
- 既存文書に古い数値上限が残っていても、本節と `docs/requirements-definition.md` の2026-08-08改訂を優先する。
- 上限撤廃後も、同種の操作を操作enumへ集約すること、状態を一元化すること、UIを分かりやすく保つことは必須とする。
- ORIGAMI2の失敗要因である**厳密演算の全面化**と**UI要素の平置き**を再現しない。
- 理由: 個数だけを制限しても設計品質は上がらず、必要な機能を妨げる一方、前作の本質的な失敗は計算方式と情報配置にあったため。

## 10. 実際に起きた失敗と、その再発防止（2026-08-11）

本節は「起きた失敗」「原因」「二度と起こさないための仕組み」を対にして記す。**気をつける、注意する、といった心構えは対策として認めない。仕組みで防げない対策は書かない。**

### 10.1 手元では通るのに自動検査（CI）が落ちる

- **起きたこと**: 2回連続でCIが失敗した。1回目はテストが読む作品ファイルをコミットに含め忘れ、2回目はアプリ本体のソースが、リポジトリに含めない `verification/` の中のファイルを参照していた。
- **原因**: `scripts/check.ps1` は作業フォルダの全ファイル（未コミット・無視対象を含む）を使って実行される。CIは**コミット済みの内容だけ**で実行する。この条件差を検査していなかった。
- **仕組みでの対策**:
  - `scripts/check-ci.ps1` を実行し、**コミット済みの内容だけを取り出した複製**で、CIと同一のコマンド一式を通す。
  - push前フック（`scripts/hooks/pre-push`）が同じ検査を自動実行する。
  - **`scripts/check.ps1` の合格だけを根拠にpushしない。** 「手元で全5検査合格」はCIの合格を意味しない。
- **あわせて守ること**: テストやアプリ本体から、`.gitignore` 対象（`verification/`、`scratchpad/`、`vendor/` など）のファイルを**読み込む形で参照しない**。テスト用データは必ず `crates/*/tests/fixtures/` に置き、コミットする。調査用の一時コードを製品側のソースに残さない。

### 10.2 委譲の切れ目にエージェントが止まる

- **起きたこと**: Codexの完了報告を受けたあと、Claudeが検査・コミット・リリース作業をしている間、**次の指示を出しておらず、稼働エージェントが0になった時間帯が生じた**。利用者の「進んでるか確認して」で発覚した。
- **原因**: 「報告を受ける → 検査する → コミットする → 次を指示する」という**直列の手順**にしていた。検査とコミットの所要時間がそのまま空白時間になった。
- **仕組みでの対策**:
  - **完了報告を受けたら、検査・コミットより先に次の指示を投入する。** 順序を逆にしない。
  - **稼働エージェントが0になってよいのは、残作業が本当に無いときだけ。** 状況を確認したら必ず稼働数を数え、0なら即座に次を投入する。
  - 停滞監視（`scratchpad/watch-codex.ps1`）は、成果物の停滞に加えて**「稼働エージェント数が0なのに未完了作業が残っている」状態も異常として検知**する。

### 10.3 Codexが本体リポジトリのgitを操作しかけた

- **起きたこと**: CIの再現のためにリポジトリを複製させた際、`git checkout` が**本体側**で実行されかけた。書き込み拒否で未遂に終わったが、成功していれば他エージェントの作業中の変更が消えていた。
- **原因**: 指示に「複製先で操作する」とは書いたが、「**本体では一切のgit操作を禁止する**」と明示していなかった。
- **仕組みでの対策**: 複製や検証を伴う指示には、禁止事項として「本体リポジトリ（`C:\Users\oltot\Documents\git-projects\ORIGAMI3\.git`）へのcheckout、commit、stash、resetを一切行わない」を**実パスで明記する**。

### 10.4 折り図の読み解き資料どうしが矛盾していた

- **起きたこと**: 悪魔の手順25で、`anchors.md` は動きを `C→A`、`part-025-064.md` は `C→B` と記していた。矛盾に気づかないまま実装を6回試み、すべて失敗した。
- **原因**: 資料を作った時点で相互の突き合わせをしていなかった。
- **仕組みでの対策**: 折り図の読み解き資料に基づく実装を指示するときは、**該当箇所を原資料の画像で確認し、資料間に矛盾がないかを先に検証する段階**を指示に含める。矛盾が見つかった場合は実装を始める前に報告させる。

## 10.5 「合格」と報告する前に、成果物を自分の目で見る（2026-08-12）

- **起きたこと**: 1分ローズを「完成」「裂け0.0」と繰り返し報告したが、利用者が手順書PDFを開いたところ、**面が交差した塊でバラの形になっていなかった**。Claudeは一度も3D画像を見ていなかった。
- **原因**:
  1. **エージェントの報告を、確認の代わりにしていた。** 報告書の数値を読んで合格と判断した。報告の確認は成果の確認ではない。
  2. **合格条件そのものの妥当性を疑わなかった。** `max_seam_gap`（共有する辺が離れていないか）だけを条件にしていたが、**面どうしが突き抜けていても、形が全く違っても、この値は0になり得る**。「この条件で本当に折れていると言えるか」を自問していなかった。
  3. 悪魔の担当は自分の判断で自己交差の検証を9箇所入れていたが、ローズには0箇所だった。**その差にClaudeが気づかなかった。**
- **仕組みでの対策**:
  - **作品の受け入れ条件に `self_intersection_pairs` が空であることを必ず含める。** 全fixtureを走査して自己交差0を検査する共通テストを置き、新しい作品を足したときも自動で対象になるようにする。
  - **完成形は「形の条件」でも検査する**（外形、高さ、対称性、層の重なり順）。裂けの量だけを合格条件にしない。
  - **Claudeは「完成」「合格」と報告する前に、必ず成果物の画像を自分で開いて見る。** 数値とエージェントの報告だけで完成を報告しない。画像を見ていない場合は「未確認」と明記する。
  - 資料（PDF等）を成果として報告する前に、**中身を開いて確認する**。ファイルサイズや生成の成功だけを根拠にしない。

## 10.6 push前に必ず通す検査の一覧（2026-08-12）

- **起きたこと**: 複製での検査を「Tests, lint, and desktop checks」ジョブ分しか行わずにpushし、**もう一方の「Release performance tests」ジョブを一度も実行していなかった**ため、接触診断の性能超過でCIが落ちた。あわせて繰り返し実行による決定性の確認もしておらず、層順が実行環境で変わる非決定性もCIに見つけてもらった。
- **原因**: `ci.yml` に**ジョブが2つある**ことを検査手順に反映していなかった。「複製で検査した」という言葉で、片方だけの実行を全体だと錯覚していた。
- **仕組みでの対策**: pushの前に、**検証済みの複製**で次を全て実行し、結果を1つずつ確認する。1つでも未実行なら push しない。

| # | 確認 | コマンド |
|---|---|---|
| 0 | 複製が正しいこと | `.git` の存在 / `git rev-parse HEAD` がコミットと一致 / `git status --porcelain` が空 / 作業中ファイルが無い |
| 1 | Rustのテスト | `cargo test --workspace` |
| 2 | 書き方の点検 | `cargo clippy --workspace --all-targets -- -D warnings` |
| 3 | 画面の組み立て | `npm ci` → `npm run build` |
| 4 | 画面の書き方の点検 | `npm run lint` |
| 5 | 画面のテスト | `npm run test` |
| 6 | **性能(最適化あり)** | `cargo test --release -p ori3-soft --test perf_soft -- --nocapture` |
| 7 | **性能(最適化あり)** | `cargo test --release -p ori3-rigid --test perf_miura -- --nocapture` |
| 8 | **決定性** | 折りに関わるテストを**連続10回以上**実行し、結果が毎回同じであること |
| 9 | **性能(最適化あり)** | `cargo test --release -p ori3-rigid --test perf_yakko --test perf_contact -- --nocapture` |
| 10 | **性能(最適化あり)** | `cargo test --release -p ori3-cp --test curve -- --nocapture` |
| 11 | **性能(最適化あり)** | `cargo test --release -p ori3-layers --test replay -- --nocapture` |
| 12 | **性能(最適化あり)** | `cargo test --release -p ori3-propose --test perf_packing -- --ignored --nocapture` |
| 13 | **180度の重なり順の全数掃引(最適化あり)** | `cargo test --release -p desktop --lib surface_order_179_999_to_180_all_110_creases -- --nocapture` |
| 14 | **完全に折った端点の重なり順(最適化あり)** | `cargo test --release -p desktop --lib surface_order_exact_endpoint_is_rank_stable_for_previous_19 -- --nocapture` |
| 15 | **性能(最適化あり)** | `cargo test --release -p ori3-soft --test soft_crane -- --nocapture` |
| 16 | **性能(最適化あり)** | `cargo test --release -p ori3-propose --test perf_packing -- --nocapture` |
| 17 | **性能(最適化あり・画面・checks)** | `npm run test -- --maxWorkers=1 --mode=production src/lib/symmetry.test.ts` |
| 18 | **提案の探索の決定性10回(最適化あり)** | `cargo test --release -p ori3-propose --test acceptance -- completion_search_uses_safe_subsets_and_is_deterministic_ten_out_of_ten --exact --nocapture` |
| 19 | **提案を通しで折る決定性10回(最適化あり)** | `cargo test --release -p ori3-propose --test end_to_end -- named_sample_completes_end_to_end_and_is_deterministic_ten_out_of_ten --exact --nocapture` |
| 20 | **重なりの部分集合の候補(最適化あり)** | `cargo test --release -p ori3-propose --test acceptance -- a_safe_coincident_partial_network_appears_after_the_first_fold --exact --nocapture` |
| 21 | **提案の探索が時間の打ち切りに当たらないこと(最適化あり)** | `cargo test --release -p desktop --lib the_heaviest_proposal_never_hits_the_time_limit -- --nocapture` |
| 22 | **提案matrixのrelease境界(最適化あり)** | `powershell -NoProfile -ExecutionPolicy Bypass -File crates/ori3-propose/tests/run-proposal-matrix.ps1 -Mode Performance` |

次の検査はpushごとの通常検査には含めず、cleanなcommit済みtreeを対象にnightlyまたはリリース前の手動CIで実行する。

| 実行時期 | 確認 | コマンド |
|---|---|---|
| 毎日03:17 JST / リリース前の`workflow_dispatch` | **実装から生成した6指標と文書marker・登録mirrorの一致（full 2-pass）** | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-current-status.ps1 -Check` |

- **CIの`checks`実コマンド**: `cargo test --workspace -- --skip surface_order_179_999_to_180_all_110_creases --skip surface_order_exact_endpoint_is_rank_stable_for_previous_19 --skip completion_search_uses_safe_subsets_and_is_deterministic_ten_out_of_ten --skip named_sample_completes_end_to_end_and_is_deterministic_ten_out_of_ten --skip a_safe_coincident_partial_network_appears_after_the_first_fold --skip the_heaviest_proposal_never_hits_the_time_limit`。この既存のworkspace testに、`proposal_matrix_contract`のdebug・1候補・1要求・直列・空き・1回smokeが含まれる。重複するstepは足さず、`checks`の所要時間を延ばさない。
- **#17について**: production専用のsymmetry性能検査は、Node.jsと依存部品を既に用意する`checks`の末尾で、表と同じコマンドの別processとして走らせる。長い`performance`でNode setupと2回目の`npm ci`を重ねないためであり、検査数・production mode・`--maxWorkers=1`は変更していない。
- **#13・#14 について**: この2件は最適化なしでは手元で 476秒 / 175秒 かかる。CIの `checks` ジョブでは `--skip` で外し、`performance` ジョブが最適化ありで走らせる(どちらの検査も消していない)。**この2件は**手元の `cargo test --workspace`(#1)と `scripts/check.ps1` では、いままでどおり最適化なしでも走る。
- **#18〜#20 について**: この3件は最適化なしでは手元で **約7.5時間 / 374秒 / 375秒** かかる(変更前は 587秒 / 30秒 / 6秒)。花弁折り・つぶし折りの候補を既定で作るようにした(`crates/ori3-propose/src/enumerate.rs` の `WITH_EXTRA_CANDIDATES`)ためで、最適化ありなら **1,332秒 / 12秒 / 11秒**(CI換算 合わせて約81分)で済む。**#13・#14 と違い、この3件は手元の `cargo test --workspace`(#1)・`scripts/check.ps1`・`scripts/hooks/pre-commit` からも `--skip` で外してある。最適化なしでは現実的な時間で終わらないためである。** 手元で確かめたいときは上表のコマンド(最適化あり)を使う。**探索の検査でも `search_to_finish` を使うもの(作業24の終点測定など)は影響を受けないので外していない**(`crates/ori3-propose/src/search.rs` の `if completion.is_some()` が、追加の候補を作る場所を囲っている)。
- **#21について**: この検査は「先端12本(`MAX_LEAVES`の上限)の4候補すべてが、30,000msのwatchdogに当たらないこと」を主張する。**この主張は最適化ありでしか成り立たない。** watchdogは壁時計(`Instant::now()`)なので、最適化なしは最適化ありより16.8〜20.5倍遅く、必ず`SearchAbort::WatchdogExpired`になる。実測(2026-08-24、この作業機): 最適化なしでは先端12本の4候補すべてがwatchdogに当たり、最適化ありでは0件/10回・最大13.851秒だった。**#18〜#20と同じ扱いとし、手元の`cargo test --workspace`(#1)・`scripts/check.ps1`・`scripts/hooks/pre-commit`からも`--skip`で外してある。** 検査は消していない。決定的な`PLAN_BUDGET`の値(`max_states=2`・`branch=2`)は変更していない。
  別型のwatchdog値(`max_millis=30_000`)も変更していない。
- **#22について**: `performance`では、release・4候補・2要求・並列・混雑を1回と、製品の候補JSON／通常停止理由hashを確認する。手元の実測はrunner全体5.146秒(matrix cargo 1.555秒＋製品hash cargo 3.042秒)、CI換算は約18.5秒で、GitHub Actionsの1ジョブ6時間上限内である。これはprofile境界の短時間検査であり、次の全数受入の代用ではない。
- **リリース前の必須関門(CI外)**: 解決済み3検査×100回だけで手元の見積もりは約37時間39分かかり、さらに32 cell×100回(3,200反復)が加わるため、GitHub Actionsの1ジョブ6時間上限を超える。このためpushごとの表には番号を付けず、ほかの作業と測定をすべて止めたリリース直前に、回数を減らさず1回だけ手元で実行する。開始は `powershell -NoProfile -ExecutionPolicy Bypass -File crates/ori3-propose/tests/run-proposal-matrix.ps1 -Mode Full`、中断後の再開は `powershell -NoProfile -ExecutionPolicy Bypass -File crates/ori3-propose/tests/run-proposal-matrix.ps1 -Mode Full -Resume`。完了するまでリリースしてはならない。
- **性能について**: CIの計算機は手元より**約3.6倍遅い**実測がある。手元の測定値が上限の1/3以下でなければ、CIで落ちる可能性が高いとみなす。
- **`ci.yml` が変わったら、この表も更新する。** 表と `ci.yml` が食い違っていないかを、`scripts/check-ci.ps1` が自動で確認する。

### 10.6.1 手元とCIでCPUの種類が違う（2026-08-23）

- **事実**: この作業機のRustツールチェインは `aarch64-pc-windows-msvc`（ARM64）である。`.github/workflows/ci.yml` は `runs-on: windows-latest` を使っている。
- **確かめ方**:
  - 手元のCPUの種類は `rustc -vV` を実行し、`host:` の行を読めば分かる（実行して確認済み: `host: aarch64-pc-windows-msvc`）。
  - CI側のCPUの種類は、GitHub Actionsの実際のログを見るまでは分からない。「`windows-latest` は通常x86_64ランナーである」は**推測**であり、断定しない。
  - `scripts/check-ci.ps1` は**手元の計算機**で走る。手元がARM64である以上、この検査はCI側のCPUの種類（推測: x86_64）とは異なる命令セットで実行されており、**この差を検出できない**。
- **推測（まだ確かめていない）**: 浮動小数の丸め方はCPUの種類（命令セット）によって変わり得る。したがって、§10.7.7（完全に折った状態の近くで解が計算機ごとに変わり得ると記した失敗）と§10.7.9（測れる折り目の本数が手元50・CI49で1本ずれた失敗）は、CPUの種類の違いが一因である**可能性がある**。これは確かめていない推測であり、原因だと断定するものではない。
- **したがって**（§10.7.7に既にある定めに、理由が1つ増えたものとして記す）: `check-ci.ps1` の合格を「CIに通る」と報告しない。push後にGitHubの実際の結果を見るまで、CIに通ったとは書かない。

## 11. リリース時に必ず最新化するもの（2026-08-11 利用者指示）

- **リリースのたびに、ヘルプと取扱説明書を画像も含めて最新の状態にする。** 実装した機能が解説に載っていない状態でリリースしない。
- **起きたこと**: v0.3.0の後に多数の機能追加・不具合修正が入ったが、説明書PDFはv0.3.0時点のままで、ヘルプにも新機能の解説が無かった。利用者の指摘で発覚した。
- **画像も対象**: 画面の見た目が変わる変更（区画の広さ、色分け、通知の追加など）が入ったら、**既存のスクリーンショットは全て古くなる**。撮り直す。
- **仕組みでの対策**: `scripts/check-release-ready.ps1` を実行し、次を自動で検査してから タグを打つ。
  1. バージョン番号が4ファイル・5か所（`Cargo.toml`／`apps/desktop/package.json`／`apps/desktop/package-lock.json`のルート直下と`packages[""]`内の2か所／`apps/desktop/src-tauri/tauri.conf.json`）で一致している（`scripts/check-release-ready.ps1:330-389`）
  2. 説明書PDFの更新時刻が、`apps/desktop/src/`・`apps/desktop/package.json`・`docs/manual/assets/`の中で最も新しいファイルより新しい（`scripts/check-release-ready.ps1:391-435`）
  3. 説明書の生成元である`apps/desktop/package.json`のバージョン表記がタグと一致している。PDF本体の文字は図形化されており安定して抽出できないため読んでいない（`scripts/check-release-ready.ps1:8-13, 437-470`）
  4. ヘルプ（`apps/desktop/src/help/`配下）の章が説明書より新しくない（`scripts/check-release-ready.ps1:472-503`）
- **リリース手順の順序**: 機能凍結 → ヘルプ・説明書の更新 → 画像の撮り直し → 説明書の再生成 → `check.ps1` → `check-ci.ps1` → `check-release-ready.ps1` → コミット → タグ

### 11.1 解説に載せる範囲

- **利用者の操作と画面表示に関することだけを載せる。** 内部の計算方式、データ構造、最適化、アルゴリズム名は載せない。
- 判断基準: 「利用者が画面で見る」「利用者が操作する」もののみ。利用者が知らなくても使えることは書かない。
- 内部用語（ソルバー、hard/soft、warm start、ヤコビアン等）を利用者向けの文章に出さない。
- 理由: 利用者の指示「ユーザーの操作や表示に関するもの以外は載せなくていい」。説明書は操作の手引きであり、設計文書ではないため。

## 10.7 コミットと証拠の扱いで実際に起きた失敗(2026-08-12)

### 10.7.1 差分を読まずにコミットし、コミット済みの内容で組み立てられなくなった

- **起きたこと**: `git diff --stat` の行数だけを見てコミットし、まだ取り込んでいない作業でしか
  存在しない項目(`motion_path`)を参照する1行を混入させた。手元では通るのに、
  コミット済みの内容だけでは組み立てられず、複製での検査で初めて失敗した。
- **原因**: §7に「`git diff` の実際の差分を読む」と定めているのに、`--stat` の確認で代用した。
- **仕組みでの対策**:
  - コミット前に **`git diff --cached` の中身を全て読む**。`--stat` の行数確認で代用しない。
  - 未コミットの作業が他にあるときは、コミット後に
    **`git worktree add <一時パス> HEAD` でコミット済みの内容だけの作業ツリーを作り、そこで組み立てる**。
    複製での全検査(5〜6分)を待たずに、混入を即座に見つけられる。

### 10.7.2 `git checkout --` で他の未コミット作業を巻き添えにした

- **起きたこと**: 自分が書いた誤ったテストを取り消すため `git checkout -- <ファイル>` を実行し、
  同じファイルにあった別の未コミット変更も一緒に消した。
- **仕組みでの対策**:
  - `git status --porcelain` の変更件数が0でないとき、**`git checkout -- <ファイル>` を使わない**。
    自分が足した部分だけを編集で取り除く。
  - 大きな未コミット作業を抱えたまま作業しない。棚上げするなら
    **パッチと未追跡ファイルをリポジトリ外へ保全してから**作業ツリーを綺麗にする。

### 10.7.3 出所の分からない成果物を根拠に判断した

- **起きたこと**: 折り鶴を実機で折って「鶴に見えない」と報告したが、使った作品ファイルは
  棚上げした未完成の作業のもので、展開図が鶴のものですらなかった。
- **仕組みでの対策**: 画面確認や報告の根拠にするファイルは、**`git ls-files` で追跡対象か確認する**。
  未追跡・棚上げ中のファイルを判断材料にしない。

### 10.7.4 測る前に原因を断定した

- **起きたこと**: やっこさんで「1本を4°動かすと他が179.9°動く」ことを欠陥と報告したが、
  分割数を1/4/64と変えても同じ場所へ収束し、幾何的に正しい挙動だった。
  さらに根拠のない「30°以内」という合格条件のテストを足し、撤回した。
- **仕組みでの対策**:
  - 原因を報告する前に、**その原因なら変わるはずの量を1つ変えて測る**。変わらなければ原因ではない。
  - 合格条件の数値は、**実測値を根拠として同じコメントに残す**。実測が無い数値を条件にしない。

### 10.7.5 変えたcrateのテストだけで合格と判断した(2026-08-13)

- **起きたこと**: `ori3-rigid` のソルバーで、折り角の目標との差を「+180°と−180°は
  同じ折り方だから」と丸めた。`cargo test -p ori3-rigid` が通ったのでコミットしたが、
  壊れたのは `ori3-layers` の `preliminary_base_folds_the_square_into_four_layers` で、
  コミット済みの内容だけを取り出した複製で検査するまで気づかなかった。
- **原因**:
  1. §7が定める `scripts/check.ps1` を実行せず、変えたcrateのテストだけで代用した。
  2. 折り角の丸めは**山折りと谷折りの区別を消す**。折り終わった瞬間は同じでも、
     途中の形が裏返り、記録した紙の重なり順と上下が食い違う。
     「終点が同じなら同じ」は折り紙では成り立たない。
- **仕組みでの対策**:
  - `scripts/hooks/pre-commit` を追加した。`.rs` または `Cargo.toml`/`Cargo.lock` を
    含むコミットでは `cargo test --workspace`(§10.6 の #18〜#21 の4件を `--skip`)を
    自動実行し、落ちたらコミットを中止する。
    `scripts/install-hooks.ps1` が pre-commit と pre-push の両方を有効化する。
  - **角度の等価性を扱う変更は、平坦な終点だけでなく途中の姿勢で検査する。**
    `assert_display_order` は99%まで折った時点の上下を見ており、この誤りを実際に捕まえた。

### 10.7.6 テストが追跡対象のファイルを書き換えていた(2026-08-13)

- **起きたこと**: 上の調査中に `crates/ori3-layers/tests/fixtures/devil-025.ori3` が
  変更済みになっていた。悪魔の受け入れテストが実行のたびにこのファイルを上書きしており、
  数値の最終桁(1e-14)だけが毎回変わっていた。同じファイルを `folded_query.rs` と
  `step_oracle.rs` が読むため、テストの実行順で読む内容が変わる状態でもあった。
  折り鶴も同様に `apps/desktop/src/lib/__fixtures__/crane.json` を毎回上書きしていた。
- **原因**: 「fixtureを作る」と「fixtureで検査する」を同じテストが兼ねていた。
  §10.1が禁じる「テストが製品側ソースへ書き込む」と同じ形が、fixtureで起きていた。
- **仕組みでの対策**:
  - 普通のテストは**読んで照合するだけ**にする。作り直しは `#[ignore]` の
    再生成専用テストに分け、明示的に実行したときだけ書き込む
    (`regenerate_crane_front_fixture` / `ORI3_DEVIL_FIXTURE_OUT`)。
  - `scripts/check.ps1` が `cargo test --workspace` の前後で
    `git status --porcelain --untracked-files=no` を比べ、
    **追跡対象のファイルが1つでも変わったら失敗させる**。

### 10.7.7 手元の複製検査に通ったのにCIが落ちた(2026-08-13)

- **起きたこと**: `apps/desktop/src-tauri/src/store.rs` の
  `flat_fold_notice_does_not_stop_the_requested_motion` が、手元では通るのにCIで落ちた。
  8本を180°まで送った**計算結果**(自己交差の有無)に期待値を結び付けており、
  CIの計算機では自己交差が0組になって通知が空になった。
- **原因**: 完全に折った状態の近くは、浮動小数の丸めの違いで解が計算機ごとに変わり得る。
  `scripts/check-ci.ps1` は**手元の計算機**で走るため、この差を検出できない。
  「コミット済みの内容だけで検査した」ことと「別の計算機で通る」ことは別物だった。
- **仕組みでの対策**:
  - **solveの結果に期待値を結び付けたテストを書かない。**
    通知や判定の規則を試すときは、角度や交差の有無を**引数で明示して与える**。
    計算を通すのは「操作が止まらない」「有限の値が返る」など、
    計算機が変わっても成り立つ性質だけにする。
  - **`check-ci.ps1` の合格を「CIに通る」と報告しない。**
    push後にGitHubの結果が緑になったことを見るまで、通ったとは書かない。

### 10.7.8 関係する量を並べて比べずに、同じ場所を3回直した(2026-08-13)

- **起きたこと**: 「3Dで見えないはずの折り目が見える」という利用者の指摘に対し、
  深度判定(`depthTest`)の設定ばかりを3回直し、いずれも解決しなかった。
  1回目は5つある強調表示の材質のうち1つだけを直して「直した」と報告した。
  2回目に残り3つを直したが、まだ見えた。
- **原因**: 関係する2つの量を並べて比べていなかった。
  - 強調表示の太さ **0.006**(`HIGHLIGHT_RADIUS`)
  - 紙の重なり全体の厚み **0.001**(`MAX_STACK_RATIO`)、層の間隔 **0.0002**
  強調線は紙の重なり全体の**6倍太い円柱**で、裏側の折り目を強調すると
  手前の紙を幾何的に貫通して突き出す。突き出た部分は正しく手前と判定されるので、
  深度の設定をどう変えても消えない。
  さらにカメラの奥行き範囲(手前0.01・奥100)で区別できる細かさと 0.0002 の比較も
  していなかった。
- **仕組みでの対策**:
  - **見え方の不具合を直す前に、関係する量を表にして並べる。**
    大きさ・間隔・許容差・分解能を同じ単位でそろえ、桁が合っているかを最初に見る。
  - **同じ役割のものが他に無いか数える。** 材質・分岐・呼び出し箇所を数え、
    「全部でいくつあり、そのうちいくつを直したか」を報告に書く。
  - **画面を拡大して自分の目で見るまで「直った」と報告しない。**
    今回は拡大して初めて、別の不具合(面の縞模様)が出ていることに気づいた。

### 10.7.9 検査の数値をCIの計算機で試さずに決め、2回続けてCIを落とした(2026-08-17)

- **起きたこと**: v0.4.5のリリース作業中、**同じ入口で2回続けてCIが落ちた**。
  どちらも主張そのものは通っており、落ちたのは**数値の決め方**だった。
  1. `surface_rank_agrees_with_the_measured_gap_without_using_the_crease_rule` が
     `checked >= 50` で落ちた。**測れる折り目の本数が手元50・CI49**で1本ずれた。
     下限に手元の実測値をそのまま使っており、**余裕が0**だった。
  2. `crease_patterns_stay_identical_to_the_recorded_baseline` が落ちた。
     展開図を**書き出した文字列の完全一致**で記録と比べており、
     座標の最下位1桁(実測 1.11e-16)の違いで落ちた。
- **原因**: どちらも §10.7.7 が禁じる「解の結果に期待値を結び付ける」形だが、
  **禁止の書き方が「期待値」に限られていたため、
  「下限の件数」と「文字列の一致」は該当しないと読めてしまった。**
- **仕組みでの対策**:
  - **実測値をそのまま検査の境目(下限・上限)にしない。**
    実測値と、**余裕を取った境目の両方**をコメントに残す。
    余裕の取り方は既存に合わせる(このリポジトリでは**実測の約8割**。
    `robust_stacks >= 4_000`(実測4888)、`>= 1_000`(実測1298)、
    `checked >= 40`(実測50/49)がその例)。
  - **計算で出た小数を、厳密な一致で比べない。**
    個数・つながり・種類・IDは完全一致でよいが、**座標や角度は許容差つき**で比べる。
    許容差は実測から決め、**根拠(実測のずれ、別物と区別できる最小の距離)を
    同じコメントに残す**。
  - **同じ形が他に無いかを数える。** 直すときは1件だけ直さず、
    `assert_eq!`/`assert_ne!` を走査して**総数と直した数を報告に書く**。
    2の修正では、まだ落ちていない同型の1件も同時に見つかった。
  - **CIが落ちたら、利用者からログを受け取るまで修正に着手しない**(利用者指示)。
    Claudeの環境からGitHubの結果は見られない。落ちた事実だけで推測して直すと外れる。
    **CIが緑なら、指示を待たず次へ進む。**

### 10.7.10 「未確認」の報告を受け入れて、壊れた表示を利用者に見せた(2026-08-17)

- **起きたこと**: 利用者から
  「**紙の裏表と重なりの正しさと表示の整合性は確認しながら進めること**」と
  指示されていたのに、**利用者の画面で裏表がおかしい状態が再び発生した**。
- **原因**: 指示書には確認を合格条件として書いていた。しかし
  **担当が「実機確認は未確認」と報告したとき、Claudeがそれを了承して先へ進めた。**
  理由は「作業ツリーに他の担当の未コミット変更が混ざっており、
  画像がその担当の変更の証拠にならないから」だった。
  **理由は正しいが、その場合に取るべきは『了承』ではなく『整理して確認させる』だった。**
  複数の担当を並行させたことで、**どの時点でも作業ツリーが混ざっており、
  誰も実機確認できない状態**が続いた。
- **仕組みでの対策**:
  - **3Dの表示・重なり順・展開図の適用・手順の再生に触る変更は、
    「画像で確認済み」でなければコミットしない。**
    報告に「未確認」があれば、**コミットせず、確認できる状態を作ってから確認させる。**
  - **確認できる状態の作り方**: 変更を**1件だけ**含む作業ツリーを作る
    (`git worktree add <リポジトリ外の一時パス> HEAD` に対象の変更だけを載せる)。
    混ざった作業ツリーで撮った画像は**証拠にしない**(§10.7.3)。
  - **並行させる担当の数を、3Dの表示に触るものは1件までにする。**
    同時に2件以上が触ると、どちらも実機確認できなくなる。
  - **確認の内容は毎回同じにする**(下記)。担当ごとに書き分けない。
    1. 紙の面だけの裏の割合(数え方は `scratchpad/live-capture-report.md` の条件)
    2. `surface_rank` が**面の番号順(`[0,1,2,…]`)でない**こと
    3. 重なっている面の組で、**すき間の実測から求めた上下と `surface_rank` が一致**すること
    4. **重なり順で一番上の面が、実際にその画素へ描かれている**こと
    5. 上を**画像で目視**すること

### 10.7.11 組み立て先を担当ごとに分けた結果、ディスクが尽きた(2026-08-20)

- **起きたこと**: 担当を並行させるたびに専用の組み立て先
  (`CARGO_TARGET_DIR`)を新しく作らせ、**39個・約230GBが溜まって空きが0.2GBになった。**
  担当が「容量不足で組み立てられない」と報告して発覚した。
- **原因**: 取り合いを避けるために分けること自体は正しいが、
  **使い終わった組み立て先を片付ける手順が無かった。**
- **仕組みでの対策**:
  - **担当が終わったら、その組み立て先をClaudeが消す。**
    次の担当を投入する前に、**終わった担当の分を消してから**投入する。
  - **投入前に空き容量を確認する。**
    1つの組み立て先はおよそ **5〜20GB** を使う。並行数×20GBの空きが無ければ投入しない。
  - **使用中かどうかは、担当が動いているかで判断する。**
    担当自身に消させない(共有しているかを担当は判断できない)。
  - 置き場所は `%TEMP%\ori3-target-<担当名>` にそろえ、**名前で見分けられるようにする。**

### 10.7.12 組み立て先と複製を数え切れず、翌日に同じ容量不足を起こした(2026-08-21)

- **起きたこと**: Cドライブの空きが0バイトになり、担当の作業が止まった。
  担当は「作業12・13」の検査を実行できず、**`clippy` と実機確認が未実施のまま報告**になった。
  別の担当も「容量不足」を報告した。2026-08-20にも同じことが起き、
  §10.7.11に39個・約230GBの記録と組み立て先を片付ける仕組みを書いたばかりだった。
  **同じ失敗が翌日に再発した。**
- **原因**:
  1. **組み立て先が `%TEMP%` の外にも溜まっていた。** §10.7.11では
     `%TEMP%\ori3-target-<担当名>` に置くと決めたが、`verification/` の中にそれ以前の
     組み立て先が53個残っていた。内訳は `verification/target-*` が52個、
     `verification/ci-repro` が1個で、合計は約179GBだった。Claudeは `%TEMP%` だけを数え、
     `verification/` の中を数えていなかった。「片付けた」と報告した時点でも、実際には約179GBが残っていた。
  2. **複製を作る指示に、除外する場所を書いていなかった。** カエルの遅さを調べる担当へ
     「リポジトリを複製して比べてください」と指示したが、除外する場所を書かなかったため、
     担当は `verification/` を丸ごとコピーした。複製は179.8GBになり、
     そのうち `verification/` は178.91GBだった。**担当の落ち度ではない。除外を書く指示を出さなかったClaudeの落ち度である。**
- **仕組みでの対策**:
  1. **組み立て先を数えるときは、次の実パスをすべて数える。** `%TEMP%` だけを数えない。
     - `C:\Users\oltot\AppData\Local\Temp\ori3-target-*`
     - `C:\Users\oltot\Documents\git-projects\ORIGAMI3\verification\target-*`
     - `C:\Users\oltot\Documents\git-projects\ORIGAMI3\verification\ci-*`
  2. **複製を作らせる指示には、除外する場所を実名で必ず書く。** 最低限、
     `verification/`、`target/`、`node_modules/`、`.git/`（必要な場合を除く）を除外する。
     「軽い複製を作って」だけでは条件を満たさない。
  3. **複製の中で `cargo` を動かすときも、`$env:CARGO_TARGET_DIR` を必ず設定させる。**
     設定しないと、指定した組み立て先ではなく複製の中に出力され、数え上げから漏れる。
  4. **`verification/` に組み立て先を作らせない。** 組み立て先は
     `%TEMP%\ori3-target-<担当名>` だけとする。担当ごとの組み立て先を片付ける仕組みは§10.7.11に従う。
  5. **空き容量は、担当を投入する前と「容量不足」の報告を受けた時点の両方で、上の実パス3か所を数え直す。**
- **あわせて記録すること**:
  - `verification/` は `.gitignore:23` の `/verification/` で無視対象になっており、
    中身は1件も追跡されていない（`git ls-files verification/` は0件）。したがって、
    この中の組み立て先を消しても、コミット済みの内容には影響しない。
  - ただし `verification/push-tree` は git worktree なので、消す前に `git worktree list` で確認する。
    worktreeを消した場合は `git worktree prune` を実行する。
  - 証拠のファイルである `live3-frame.json`、画像、`.tsv` の測定値は消さない。

### 10.7.13 「サブエージェントで作業して」と3回言われたのに、Claudeが自分で作業を続けた(2026-08-22〜23)

- **起きたこと**: 利用者から「サブエージェントで作業して。SonnetとOpusを使い分けて」と
  3回指示されたのに、Claudeが自分で次を実行し続けた。
  - `cargo check --workspace`（作業ツリーが組み立てできるかの確認）
  - `cargo test -p desktop --lib`（落ちていた検査2件の確認）
  - push が失敗した原因の切り分け（複製の状態調査）
  - 変更されたファイルの一覧作成・集計

  これらはすべて、記憶（`model-usage-efficiency.md`）にある取り決めでは
  Sonnetのサブエージェントの担当だった。
- **原因**: §1が「Claudeが行ってよい作業」を5つ挙げているが、
  「やってはいけない作業」を実行できるコマンドの形で書いていなかった。
  そのため、Claudeが「これは検査だから自分の役割だ」と拡大解釈できてしまった。
  §7が定める「Claudeが `scripts/check.ps1` を実行する」を根拠に、
  `cargo` を直接叩くことまで自分の役割だと考えてしまった。
- **仕組みでの対策**:
  1. **Claudeが自分で実行してよいコマンドを、実名で次の表に限定する。**

     | 実行してよい | 内容 |
     |---|---|
     | `git` | コミット、push、状態の確認、差分の閲覧（§1のとおりClaudeだけが行う） |
     | `scripts/check.ps1` / `scripts/check-ci.ps1` / `scripts/check-release-ready.ps1` | §7が定める品質ゲート |
     | ファイルの閲覧 | 報告と検査のため（§10.5が求める成果物の目視を含む） |
     | プロセスと空き容量の確認 | 停滞監視と§10.7.11・§10.7.12の数え上げ |
     | `desktop.exe` の起動・終了 | 2026-08-22の利用者指示による |

  2. **上の表に無いコマンドをClaudeが直接実行しない。**
     **とくに `cargo` と `npm` を直接叩かない。**
     組み立ての確認、検査の実行、原因の切り分けが必要になったら、
     **Sonnetのサブエージェントへ出す。**
  3. **サブエージェントを起動するたびに、記憶の判定表でモデル（Sonnet/Opus）を選び、
     選んだ理由を利用者への報告に1行で書く。書けないならSonnetを選ぶ。**
  4. **利用者へ報告する前に、稼働しているサブエージェントの数を数える。**
     **0件で、かつ残作業があるなら、報告より先にサブエージェントを投入する**（§10.2の拡張）。

### 10.7.14 画面を同梱していない実行ファイルを起動し、利用者に接続エラーを見せた(2026-08-24)

- **起きたこと**: 利用者の画面に、白い接続エラーの画面が表示された。

  `このページに到達できません
  localhost 接続が拒否されました
  ERR_CONNECTION_REFUSED`

- **原因**:
  1. 統括が `cargo build -p desktop --release` で作った実行ファイルを、配信サーバーが動いていない状態で起動した。
     `cargo build` はdebug版・release版を問わず画面を同梱しないため、実行ファイルは
     `apps/desktop/src-tauri/tauri.conf.json` の `devUrl`（`localhost:1420`）を見に行く。
     同梱版を用いる組み立てと実機確認の役割は§1に定める。
  2. この事実は既に分かっていたのに指示書へ反映しなかった。同じ日の少し前には、
     1420番ポートが応答しないため起動しないと正しく止めていたが、次の起動では
     「release版なら同梱されているはず」と確かめずに思い込んだ。
- **仕組みでの対策**:
  1. `desktop.exe` の起動前に、次のAまたはBの少なくとも一方を機械的に確認する。
     両方を満たさない実行ファイルは起動しない。

     | 条件 | 確認方法 |
     |---|---|
     | **A: 配信サーバーが応答する** | `(Test-NetConnection 127.0.0.1 -Port 1420).TcpTestSucceeded` が `True` であること |
     | **B: 画面を同梱した実行ファイルである** | 担当に組み立てコマンドを明言させる。`npm run tauri -- build` 以外ならBではない。 |

  2. Aが `False` で、Bも確認できないなら起動しない。担当へ差し戻す。
  3. 起動直後、担当への最初の指示を「画面が出たか」の確認にする。接続エラーなら即座に閉じ、
     利用者の目に触れる時間を最短にする。
  4. 指示書に書く組み立てコマンドと「アプリの起動方法」節を置かない定型は§1に従う。
  5. 配信サーバーの起動（`npm run dev`）も担当にさせない。アプリの起動・終了と実機確認の役割は§1に従う。

### 10.7.15 利用者への報告が会話の中だけで流れた(2026-08-25)

- **起きたこと**: 会話が流れると利用者への報告内容が見えなくなり、判断・原因・実測値・未確認事項が失われた。同じ調査を繰り返すことになった。
- **原因**: 報告を会話の中だけで行っていた。残す仕組みが無かった。
- **仕組みでの対策**:
  1. `scripts/check-report-log.ps1` が `docs/報告記録.md` の存在、1件以上の記録、機械可読な見出し、日付降順、本文、`apps/` または `crates/` の最新コミット日との関係を検査する。既定では同じ日の記録を要求し、許容日数は引数で変えられる。
  2. `scripts/hooks/pre-commit` が、`apps/` または `crates/` を含むコミットで、同じコミットに `docs/報告記録.md` の変更が無ければ、重いRust検査を始める前に中止する。
  3. `scripts/check-release-ready.ps1` が、リリース日に `docs/報告記録.md` の最新記録があることを第5検査として確認する。

### 10.7.16 待機中の組み立て先を、フォルダーの更新時刻だけで消した(2026-08-26)

- **起きたこと**: 統括が空き容量を整理したとき、担当が実機確認を待っていた実行ファイル3つ
  （scene / store / fold）の組み立て先を消した。
- **原因**: 「フォルダーの更新時刻が2時間以上古い」を削除条件にした。
  フォルダー自身の更新時刻と、中の実行ファイルや最新ファイルの更新時刻は別物だった。
- **仕組みでの対策**:
  1. 組み立て先は手作業で消さず、必ず `scripts/clean-build-dirs.ps1` を使う。
     引数なしでは候補・大きさ・保護理由を表示するだけで、削除は0件とする。
  2. 実際の削除には、一覧にある候補と完全一致する絶対パスを `-DeletePath` へ指定し、
     PowerShell標準の `-Confirm` を通す。削除直前にも同じ候補と保護条件を再検査する。
  3. `release\desktop.exe` または中の最新ファイルが6時間以内に更新されていれば消さない。
     フォルダー自身の更新時刻は、表示にも削除判断にも使わない。
  4. 中の実行ファイルを動いているプロセスが使っている場合、または中のファイルを
     排他的に開けず書込み・利用中と判定した場合は消さない。
  5. `.git`、登録済みworktree、再解析ポイント、検査不能な場所、`verification/` の証拠を含む候補は消さない。
     `verification/push-tree` は一覧と `git worktree list` の確認だけを行い、このスクリプトでは消さない。
  6. `%TEMP%\ori3-target-*`、`verification/target-*`、`verification/ci-*` と
     `verification/push-tree` を毎回すべて数え、各候補について実パス・大きさ・
     `release\desktop.exe`・中の最新ファイル・使用中プロセス・判定・理由を削除前に表示する。
  7. `scripts/clean-build-dirs.test.ps1` が、古いフォルダー内の新しい実行ファイル／最新ファイル、
     既定のプレビュー、`.git`、排他ロック、実行中プロセス、証拠ファイル、対象範囲を隔離一時領域で検査する。
