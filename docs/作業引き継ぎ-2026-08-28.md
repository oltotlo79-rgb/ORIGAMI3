# 作業引き継ぎ（中断時点の完全な状態）— 2026-08-28 00:40

Codex の利用上限が近づいたため、利用者の指示で全担当を切りのよいところで中断した。この文書は、統括（Claude Fable）が
**同じ状態から再開する**ための正本である。読む順: `CLAUDE.md` → 本書 → `docs/報告記録.md` の 2026-08-27〜28 の節 → 各担当の報告書（§4 の表）。

## 1. git の状態

- 本体: `C:\Users\oltot\Documents\git-projects\ORIGAMI3`、branch `main`。利用者の最終 push は ea0a8ae（2026-08-27 23:51 JST）。それ以降のコミットは未 push（利用者が `! git push --no-verify origin main` で push する運用）。
- **未コミットの Rust 分（約 12,100 行の変更＋新規 18 ファイル）が本体の作業ツリーにある**（§3）。
- 退避参照 `refs/wip/*`（hook を通さない plumbing で作った完全スナップショット。push されない。復元は `git worktree add <path> refs/wip/<name>` または `git checkout refs/wip/<name> -- <path>`）:

| ref | 内容 |
|---|---|
| `refs/wip/main-rust-slice` | 本体の未コミット差分（Rust 分＋fix-a の修正＋供給網修正後）＋ `scratchpad/*.md`。`docs/competitive-review-2026-08-20.md`・root の `traditional_crane_*` は除外 |
| `refs/wip/fixb` | `%TEMP%\ori3-wt-fixb`（HEAD 885888f＋差分42件＋fix-b の #13/#14=0 修正） |
| `refs/wip/layer1` | `%TEMP%\ori3-wt-layer1`（HEAD 47970e3＋差分42件＋層順 単位1 完了＋単位2・花弁折り前倒しの途中） |
| `refs/wip/collapse` | `%TEMP%\ori3-wt-collapse`（HEAD 47970e3＋差分42件＋途中姿勢 実装 I 合格・II 打ち切り・A 段階1 途中） |
| `refs/wip/viewer` | `%TEMP%\ori3-wt-viewer`（HEAD 3efebe2＋6-E Viewer 遅延読み込み 完成・実機確認待ち） |
| `refs/wip/cifix` | `%TEMP%\ori3-wt-cifix`（HEAD ff8812d＋CI で落ちる desktop 検査2件の修正、進行中） |

作り直し・更新は `scripts/snapshot-worktrees.ps1`（引数なしで6本すべて、`-Name <名前>` で1本だけ）。作業ツリーは変更せず、`scratchpad` は文書と差分（`*.md`/`*.patch`/`*.txt`）だけを取り込む。

## 2. 利用者の決定（2026-08-27〜28）

1. **決定 B / B'**: 重なり順の端点検査 #13（`surface_order_179_999_to_180_all_110_creases`）と #14（`surface_order_exact_endpoint_is_rank_stable_for_previous_19`）を、実測値を理由文に書いて一時的に `#[ignore]` し、Rust 分を先にコミットする。期待値・下限・許容差は変えない。**fix-b は隔離ツリーで両方 0 に到達済み**（§4）。コミット後に本体へ移植して属性を外す。
2. **「基準を緩めたり、規約に違反することのないように手を抜かず精度高く進めて」**（21:5x）。
3. **動画は「両方」**（00:1x）: A = 一気に畳む作品の途中姿勢で紙の曲がりを許す方式を実装、C = 層順修正の完了後に手順を分けた鶴の作品を作って撮る。
4. `docs/competitive-review-2026-08-20.md` には触らない（読まない・参照しない・コミットしない）。悪魔・1分ローズは使わない。

## 3. 本体の未コミット Rust 分と品質ゲートの状態

- 対象パス一覧（50 件）は §9。コミット文の草案は §10。**除外**: `docs/competitive-review-2026-08-20.md`、`traditional_crane_complete_cp.png`（root）、`traditional_crane_math_bundle/`（root）、`scratchpad/`。
- 統括は差分を全て読了（§10.7.1）。`#[allow]`・`dbg!`・`todo!` の追加 0。期待値の変更は根拠つきの 3 箇所（`corpus.rs` の再生成前提を checksum へ、`movestep_contract.rs` の集計分離、`precrease_collapse.rs` の未決定警告）。Cargo の wasm32 限定依存（`web-time`、`getrandom wasm_js`）は統括が承認済み。
- `scripts/check.ps1` の結果:
  - 1回目（19:57〜20:10）: desktop lib 3件失敗（鶴の頭の層順の golden、提案探索の見張り2件）。
  - 2回目（21:40〜22:17）: desktop 通過。`crates/ori3-propose/tests/acceptance.rs:247` `task_24_measures_actual_completion_gaps_before_setting_tolerances` が失敗（折り鶴の position gap 記録 0.4326478860317622 に対し実測 0.46687177208734676）。
- 上の失敗の原因（fix-a の切り分け、`scratchpad/desktop-red-a-report.md:557〜`）: 決定的。原因ファイルは `crates/ori3-layers/src/precrease_collapse.rs` の `solved_layer_order`（一般制約で解いた順を渡す変更）。探索経路が `[16]` → `[3,16]` へ変化。Reseed 整列は無関係。記録値は 2026-08-20 の測定（再生成テスト無し）で、task 25 の許容（80%）の元になっているため**手で書き換えない**。
- **次の一手（統括が指示済み・結果未着）**: fix-a 段階5 = HEAD 版と現行版の `precrease_collapse.rs` で候補 3・16 の `state.order`・未決定対・discarded・警告を採取し、`validate_precrease_layer_order` で違反数/総数を表にし、「HEAD 版の順が一般制約に反していた（旧記録が物理的に無効な重なりの上で測られていた）」か「両方有効なのに previous_order の優先が効かず別の順を選んだ（`solved_layer_order` の不具合）」かを判定 → 前者なら記録の正規再測定（利用者に諮る）、後者なら修正 → `check.ps1` 再実行（並行 cargo を増やさない）→ コミット（§10 の文）→ 利用者に push 依頼。
- **fix-a 段階5 の結果（00:37、`scratchpad/desktop-red-a-report.md:849` の引き継ぎ節）**: 候補 16 の collapse 順は HEAD 版と現行版で同じだが、入力 CP に対して一般制約 **2/37 違反・discarded 5 組**。HEAD 版はその順を保存していた（Face ID tie-break の自己認証）のに対し、現行版は拒否する（`layer_order=None`＋警告）ため探索が `[3,16]` を選ぶ。→ **旧記録 0.4326 は物理的に無効な重なりの上で測られていた**。対処は「記録の正規再測定」（`scratchpad/propose-24-26-report.md:23-65` の手順）だが、task 25 の許容が 0.346→0.373 へ動くため利用者に諮る（§6-1）。恒久の回帰検査（記録用の再生成テスト）も無いので併せて設計する。一時切替 5 ファイルは開始時 SHA-256 と一致（復元済み）、実行中の cargo 無し、最終 HEAD db6d98d。
- **CI `Release performance tests`（run 33084609139）も failure**（00:3x 完了、ログ未受領。§13/#14 を含む release 検査群のどれが落ちたかは利用者のログ待ち）。
- 同時に直した本体の変更（fix-a、コミット待ち）: `apps/desktop/src-tauri/src/store.rs` の `crane_head_fixture_pose_has_ten_moving_faces_and_non_id_layer_order` を独立 oracle（`CRANE_HEAD_CAPTURED_LAYER_ORDER`）との正面積重なり 60 組の照合＋一般制約検証へ置き換え（10/10 合格）、`crates/ori3-rigid/src/motion.rs::Reseed::distance_from_previous` の EdgeId 整列加算、terra による `#[ignore]` 2 行（`surface_order_acceptance.rs:593`、`surface_order_sa_endpoint_heavy.rs:4`、理由文は実測値つき）。

## 4. 担当スレッドと状態（Codex MCP）

| 担当 | thread | 場所 / 組み立て先 | 状態 | 報告書 | 次に送る指示 |
|---|---|---|---|---|---|
| fix-a（desktop 検査・提案 gap 調査） | `01a040f2-c1a4-7e63-bdfd-9689fffbba26` | 本体 / `%TEMP%\ori3-target-fix-a` | 段階5 実測中 → 中断指示済み（引き継ぎ節を書く） | `scratchpad/desktop-red-a-report.md` | 段階5の判定に応じて §3 の次の一手 |
| fix-b（#13/#14） | `01a040f4-9ac7-75f0-962c-6d2f200a16d5` | `%TEMP%\ori3-wt-fixb` / `ori3-target-fix-b` | **完了・待機**。隔離ツリーで #14 `changed_directions=0`、#13（`--ignored`）`changed_directions=0`、rigid 146/0、clippy 0 | `%TEMP%\ori3-wt-fixb\scratchpad\desktop-red-b-report.md` | Rust 分のコミット後: 4 ファイル（`motion.rs`・`surface_order.rs`・`lib.rs`（rigid）・`surface_order_acceptance.rs`）を本体へ移植（fix-a の `distance_from_previous` 整列と統合）→ #13/#14 を release で再測 → 0 なら `#[ignore]` を外す → ゲート → コミット。未対応: `commands.rs` の Follow 経路が旧 API（実 Frame 持ち回りを UI へ反映する別作業） |
| 層順修正（単位1 完了・単位2＋花弁折り前倒し） | `01a042d0-8127-7d43-b35d-2e1ab1c3dd15` | `%TEMP%\ori3-wt-layer1` / `ori3-target-layer1` | 単位1 合格。単位2 は WIP を patch へ退避しツリーは単位1 相当へ復元済み（`ori3-layers` 276/0/18）。**Petal 5/51 違反、全探索で有効な順が 0 件**（§4.2） | `…\layer-order-unit1-report.md`、`…\layer-order-unit2-report.md` | 前倒し修正の完了（違反 0、既存受入無変更合格）→ 単位2 全測定 → 単位3（squash・カエル）→ 単位4（petal・鳥）→ 単位5・6。完了後に C（手順を分けた鶴の作品）を作る |
| 途中姿勢（調査 → I → II → A） | `01a042d2-5f52-7452-850b-411ba51cc33b` | `%TEMP%\ori3-wt-collapse` / `ori3-target-collapse` | 実装 I 全 gate 合格。II（剛体射影）は gate 不合格で打ち切り。**A 段階1 の実測も既存 soft のままでは不合格**（下の注記）。段階2 未着手 → 中断済み | `…\collapse-motion-design.md`、`…\collapse-motion-stage1-report.md`、`…\stage2-report.md`、`…\stage3-report.md` | 段階1の表 → 段階2 実装（gate: 1001点で連続移動 ≤0.02・裂け ≤1e-6・自己交差 0・辺/対角 ≤1.5%・面平面離れ ≤段階1の値・t=0/1 bit 一致・決定性 10/10・cold ≤200ms・cache ≤16ms）→ 段階3 表示接続（統括の返答後） |
| 6-E Viewer 遅延読み込み | `01a04285-90cc-7c02-9daa-24d7bab777af` | `%TEMP%\ori3-wt-viewer` | **完了・未コミット**。initial gzip 166,751 / max chunk 349,725 / Three 静的経路 0。`npm run test` は変更前から境界上の 2 件（keyboard 5 s timeout、FoldAllPreview 33 ms）以外合格 | `…\viewer-6e-report.md` | Rust 分のコミット後に本体へ適用 → 同梱版を組み立て → 実機で「初回3D表示・fallback 寸法・読込失敗表示」を画像確認（§10.7.10）→ コミット |
| CI desktop 2件の修正 | `01a043d1-d3dc-7181-9b21-87efe99ed6f1` | `%TEMP%\ori3-wt-cifix`（HEAD ff8812d）/ `ori3-target-cifix` | **完了。差分は本体の `commands.rs` へ適用済み**（§4.3） | `…\ci-desktop-fix-report.md` | 本体でゲートを通してコミット（Rust 分と一緒でよい） |
| 供給網 10-A | `01a043ba-c5c0-7b50-b950-a76d2dada101` | 本体 | **完了・コミット済み（db6d98d）** | `scratchpad/supply-chain-10a-report.md` | 判断待ち 2 件（§6） |
| 撮影担当（実機確認の待ち行列） | `01a040e1-965a-73d2-b7bf-293d5177212a` | 本体 / CDP 9222 | 待機（動画は製品修正待ち） | `scratchpad/live-check-runner-report.md` | C の作品ができたら 3D 図のみ・10〜30 秒・連続差分 ≤15% で撮影。実機確認待ち行列（一斉折り 20 画像・自動保存・角度・鶴・食い込み）は新しい同梱版で |
| 折り鶴（正本再構成） | `01a03dd7-8f90-7002-8e71-4a3f23b2529f` | 本体 | 完了（clippy・新規テスト5本合格） | `scratchpad/store-4b-report.md` | 無し |
| CI ワークフロー修正（terra） | `01a043aa-9325-7b82-b7de-8fc8d80cffce` | 本体 | 完了（ea0a8ae） | `scratchpad/ci-fix-report.md` | 無し |
| `#[ignore]` 属性（terra） | `01a042d5-85d6-7920-a50e-53e614c00bbe` | 本体 | 完了 | `scratchpad/red-b-ignore-note.md` | 無し |
| 語彙 3-B 準備 | `01a03de9-a86a-7aa2-92b4-7c3ebbfa507b` | 本体 | 完了（候補 30 件は `scratchpad/corpus-3b-candidates/`、施策1の契約確定待ち） | `scratchpad/corpus-3a-report.md` §「3-B 準備」 | 契約確定後 |

### 4.1 A（曲がりを許す hybrid）段階1 の実測 — 既存 soft をそのまま使うだけでは gate を満たさない

`ori3-soft` の既存設定で正本鶴の畳みを緩和した実測（`…\collapse-motion-stage3-report.md`）:

| 項目 | 実測 | gate 案 | 判定 |
|---|---:|---:|---|
| 共有 mesh の辺の変化 | **12.956%** | 1.5% | 不合格 |
| corner pair（面内対角）の変化 | **1.641%** | 1.5% | 不合格 |
| 面平面からの離れ | 3.337% | → **5.0% に確定** | 合格 |
| 紙どうしの突き抜け（proper piercing） | **6,905 組** | 0 | 不合格 |
| `relax` 1 回の時間 | 最大 8.312ms | — | 余裕あり |

**再開時の含意**: 「既存 soft を呼ぶだけ」では A は成立しない。段階2 は、exact 端点への引き寄せ・辺長拘束の強化・接触（突き抜け）処理を含む設計が要る。gate（辺 1.5%・面平面離れ 5.0%・突き抜け 0）は緩めない。満たせない場合は、A も不採用として利用者に諮る（その場合、動画は C だけで進める）。
測定用テストは固定 gate を保ったまま明示実行専用の診断に変えてあり、通常実行は `0 failed / 1 ignored`。3 package のテストは layers unit 40/40・水風船 2/2 まで合格を確認したが、長時間の鶴テストが約 9 分後に assertion 無しの `0xffffffff` で終了したため、**全テスト合格とは記録していない**（再開時に再実行して確認する）。

### 4.2 層順修正 単位2 の中断時点 — 花弁折りの形そのものに有効な重なり順が存在しない可能性

- 中断時に単位2 の WIP を `…\layer-order-unit2-wip.patch` へ退避し、ツリーは単位1 相当の挙動へ復元した。`cargo test -p ori3-layers` **276 passed / 0 failed / 18 ignored**（鶴・やっこさん・水風船・`bird_base_lifts_two_slender_points` を含む）。中断後の clippy は未実行（直近は単位1 で診断 0）。
- **実測**: 鶴の二回目 Petal は一般制約 **5/51 違反**。物理的に可能な候補を全探索した結果は `172,032 → 裂け 0: 490 → 既存の形と一致: 1 → 一般制約 0: **0**`。すなわち **14 面を保つ条件では、一般制約をすべて満たす重なり順が 1 つも存在しない**。
- **含意（再開時にまず判断する）**: (i) Petal が作る「形（placement）」自体が物理的に無理な配置になっている（面の分割・packet の運び方が誤り）→ 単位4 の本体修正が必要、(ii) 一般制約の実装が厳しすぎる（taco-tortilla / 0°連続の条件が、実際には成立する配置を弾いている）→ 制約側の検証が必要。**どちらかを先に切り分ける**こと。期待値・制約の緩和で赤を消さない。
- 参考: 鳥の基本形・カエルの受入テストは既知の欠陥として `#[ignore]` のまま（単位3・4 で扱う）。

### 4.3 CI で落ちる desktop 検査2件の修正 — 完了し、本体へ適用済み（未コンパイル）

- **A `proposal_candidates_are_the_same_computed_together_or_one_by_one`**: 個数・ID・種類・順序・object 構造は完全一致のまま、**小数だけ `1e-9`（`ori3_model::EPS` と同じ既存慣例）で比較**。固定契約は「小数を `1e-9` 単位へ丸めた正規形」の hash `5f0a59d3235f3956` に置き換えた。主張（同時計算と逐次計算が一致）は弱めていない。
- **B `proposal_candidates_carry_a_fold_plan_that_is_ready_to_use`**: この検査だけ壁時計を使わない決定的な探索（`SearchControl` ＋ `SearchBudget`）に変更。**製品の 30,000ms watchdog は不変**。
- 隔離ツリーでの実測: 単独3回 A 54.3/47.6/47.1 秒・B 12.3/12.5/12.5 秒すべて合格。全件 **180 passed / 0 failed / 18 ignored**（531.87 秒）。clippy 0、`#[allow]` 追加 0、`git diff --check` 0。
- **本体への適用**: 担当は sandbox の制限で本体へ書けなかったため、統括が `git diff HEAD -- …/commands.rs` を取り出し（`scratchpad/ci-desktop-fix.patch`、350 行）、本体へ `git apply`（`--check` 成功後に適用、`{}` の増減 0、適用前の複製は `scratchpad/commands.rs.before-cifix`）。**この統合後にコンパイル・テストは実行していない。再開時は最初にゲートで確認する。**
- **同型走査（今回は修正せず一覧のみ。再開後の課題）**:
  - 量子化していない固定 hash が **6 箇所**: `crates/ori3-propose/tests/proposal_matrix.rs` 1、`crates/ori3-propose/tests/run-proposal-matrix.ps1` 5。CPU の種類が変わると同じ形で落ちる（§10.6.1）。
  - 壁時計で落ちうる検査が **10 件**（1 時間 watchdog の 2 件を除く現実的な候補 8 件）。
  - 固定 bit パターン比較は 0 件。

Codex の呼び出し型: MCP `codex/codex`（新規）または `codex/codex-reply`（`threadId` 指定）。model は判断を伴う作業に `gpt-5.6-sol`＋`config: {"model_reasoning_effort":"ultra"}`、機械作業に `gpt-5.6-terra`。`sandbox: workspace-write`、`approval-policy: never`、`cwd` は作業ツリー。network が要る作業だけ `config: {"sandbox_workspace_write": {"network_access": true}}`。MCP の待ちは 2 時間で切れるが Codex 側は動き続けるので、結果は報告書で受け取り `codex-reply` で再接続する。指示書は `CLAUDE.md` §3 の 7 項目を必ず含める。

## 5. CI の状態

- 8/25〜8/27 の「job 0 件の即時失敗」は job レベル `env` の `${{ runner.temp }}` が原因（ea0a8ae で修正、CI が job を作るようになった）。
- run 33084609139（ea0a8ae）`Tests, lint, and desktop checks`: **失敗**（desktop lib 178/2/18、802.91 秒）。失敗2件: `proposal_candidates_are_the_same_computed_together_or_one_by_one`（候補 JSON の hash 固定値が x86_64 で不一致 = §10.6.1 の CPU 差の実測）、`proposal_candidates_carry_a_fold_plan_that_is_ready_to_use`（30,000ms の壁時計見張り。CI は手元の約 3.6 倍遅い）。→ `ori3-wt-cifix` で修正中（主張を弱めず移植可能に: 構造は完全一致・小数は 1e-9、見張りは決定的な `SearchControl` へ）。`Release performance tests` は結果未確認。
- `security.yml`（run 33084812717、Dependabot ブランチ 0abdb8d）: npm 監査 success。Cargo 監査（10-A readiness）・checkout 欠落・Node pin は db6d98d で修正済み（push 後に確認）。**判断待ち**: `Dependency update proposal evidence`（repository context 欠落 → 最小修正 `GH_REPO: ${{ github.repository }}`）、`SBOM and release hash readiness`（全イベントで意図的に失敗し status も二重赤 → 既知 blocker と回帰を分ける設計が要る）。
- Dependabot ブランチ 5 件（cargo minor/patch、pdf-writer 0.15.0、resvg 0.48.1、actions/checkout 7、actions/setup-node 7）: 未審査。

## 6. 利用者に諮る・依頼する事項（未回答）

1. 提案の完成度の記録値（0.4326）: fix-a 段階5 の判定後に「修正」か「正規手順で再測定（task 25 の許容が 0.346→0.373 へ動く）」かを諮る。
2. security.yml の 2 job（§5）の扱い。
3. A の変形 gate の値（段階1 の実測後）。
4. 各コミット後の push。CI が赤ならログ（`failures:` と `panicked at`）の貼付。

## 7. 運用の状態

- アプリ: 組み立て 58148D4B（`%TEMP%\ori3-target-store\release\desktop.exe`、PID 23892）を撮影用に起動していたが、中断時に統括が `CloseMainWindow()` で閉じた（desktop プロセス 0）。次回は新しい同梱版（Rust 分＋6-E＋修正を含む `npm run tauri -- build --no-bundle`）を担当に組ませ、§10.7.14/§10.7.17 の手順で起動する。
- 停滞監視: 統括の scratchpad の `watch-agents.sh`（10 分間隔・40 分無変化で終了、REPORTS 配列が監視対象）。再開時に対象を書き直して背景実行。
- 組み立て先: `%TEMP%\ori3-target-*`（fix-a / fix-b / layer1 / collapse / cifix / store / check など約 30 個）。空き 372 GB（8/27 22:00）。片付けは `scripts/clean-build-dirs.ps1`。
- 古い worktree（片付け候補、消す前に `git worktree list` で確認）: `ori3-push-check`、`ori3-wt-angle`、`ori3-wt-angle-before`、`ori3-wt-ci`、`ori3-wt-v050`。
- 利用者環境: `wasm-bindgen-cli 0.2.126` 導入済み（LLVM 22.1.8 を入れ PATH 追加）。Web 版の Rust 橋（`ori3-app-core`/`ori3-web`）は Rust 分コミット後に着手。

## 8. 再開の手順（統括）

1. `git status`、`git worktree list`、`git for-each-ref refs/wip` で §1 と一致することを確認。worktree が消えていれば `refs/wip/*` から作り直す（`git worktree add <path> refs/wip/<name>`。担当には「ツリーの内容は退避参照から復元した」と伝える）。
2. 各担当の報告書末尾「中断時点の引き継ぎ」節を読む。
3. §3 の次の一手から再開（fix-a 段階5 の判定 → ゲート → Rust 分コミット）。並行して cifix・A 段階1・層順 前倒しを再接続（`codex-reply` で「引き継ぎ節を読んで再開」と送る）。
4. スナップショットの作り方（中断直前に更新済み）: 一時 index に `git read-tree HEAD` → `git add -A .` → 除外パスを `git rm --cached` → `git add -f scratchpad/*.md` → `git write-tree` → `git commit-tree -p HEAD` → `git update-ref refs/wip/<name>`。

## 9. 未コミット Rust 分の対象パス（50 件）

- apps/desktop/src-tauri/src/autosave.rs
- apps/desktop/src-tauri/src/commands.rs
- apps/desktop/src-tauri/src/store.rs
- apps/desktop/src-tauri/src/store/tests/movestep_contract.rs
- apps/desktop/src-tauri/src/surface_order_acceptance.rs
- apps/desktop/src-tauri/src/surface_order_sa_endpoint_heavy.rs
- apps/desktop/src-tauri/tests/fold_all_frog.rs
- Cargo.lock
- crates/ori3-export/tests/fold_document_roundtrip.rs
- crates/ori3-layers/src/fold_target.rs
- crates/ori3-layers/src/lib.rs
- crates/ori3-layers/src/plane_pullback.rs
- crates/ori3-layers/src/pose_motion.rs
- crates/ori3-layers/src/precrease_collapse.rs
- crates/ori3-layers/src/replay.rs
- crates/ori3-layers/src/spatial_crease_only.rs
- crates/ori3-layers/tests/acceptance_balloon.rs
- crates/ori3-layers/tests/acceptance_crane.rs
- crates/ori3-layers/tests/acceptance_frog.rs
- crates/ori3-layers/tests/crease_only_top_nonflat.rs
- crates/ori3-layers/tests/fixtures/penetration-warning.ori3
- crates/ori3-layers/tests/fixtures/traditional-crane/README.md
- crates/ori3-layers/tests/fixtures/traditional-crane/README_JA.md
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional_crane_complete.fold
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional_crane_complete_cp.png
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional_crane_coordinates_and_equations.json
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional_crane_edges_equations.csv
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional_crane_vertices.csv
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional-crane-collapse-oracle.csv
- crates/ori3-layers/tests/fixtures/traditional-crane/traditional-crane-cp.ori3
- crates/ori3-layers/tests/flat_endpoint.rs
- crates/ori3-layers/tests/fold_target_bridge.rs
- crates/ori3-layers/tests/fold_target_pleats.rs
- crates/ori3-layers/tests/penetration_fixture.rs
- crates/ori3-layers/tests/precrease_pose.rs
- crates/ori3-layers/tests/precrease_stack.rs
- crates/ori3-layers/tests/sim011_completeness.rs
- crates/ori3-model/Cargo.toml
- crates/ori3-model/src/clock.rs
- crates/ori3-model/src/lib.rs
- crates/ori3-model/tests/serde_roundtrip.rs
- crates/ori3-propose/Cargo.toml
- crates/ori3-propose/src/plan.rs
- crates/ori3-propose/src/search.rs
- crates/ori3-propose/tests/corpus.rs
- crates/ori3-propose/tests/fixtures/corpus/stage-3c-release-metrics.json
- crates/ori3-rigid/src/lib.rs
- crates/ori3-rigid/src/motion.rs
- crates/ori3-rigid/src/surface_order.rs
- crates/ori3-rigid/tests/acceptance_yakko.rs

## 10. Rust 分のコミット文の草案

```text
折り鶴の正本を層の順番まで正しく畳み、ひだの枚数指定・折り目だけ付ける操作・自動保存の複数候補を入れた

利用者決定B'（2026-08-27）に基づく Rust 分の一括コミット（32ファイル変更・18ファイル追加）。

折り鶴（正本の展開図）
- 展開図と山谷だけでは決まらない面の上下を、保存した層の順番（`FoldStep.layer_order`）で明示し、一般の制約（隣接の山谷・taco-tortilla・taco-taco・0°の連続）を独立に検証してから採用する。首・尾が後翼と前翼の間に入る（翼間の128比較で違反0、自己交差0）
- 正本 fixture 9件（`crates/ori3-layers/tests/fixtures/traditional-crane/`）と受け入れ検査を追加
- 3Dの重なり順は、返した形そのものの深さを正本にし、別の解の順番を転記しない（`ori3-rigid` の `surface_order.rs` / `motion.rs`）

折る操作
- 折り線の直下にある「ひだ」を数え、上からK枚だけを折る指定（`target_pleat_count`）
- 平らでない形のいちばん上の紙に、動かさず折り目だけを付ける操作（`CreaseOnlyTop`）と、材料座標での照会

自動保存
- 異常終了した作業を複数の候補として持ち越し、利用者が選ぶまで消さない。復元した候補は明示保存まで残す

そのほか
- FOLD の往復で7作品の座標・角度のずれが 1e-9 以内であることを検査
- 提案の語彙の release 上限（1件 50秒・全体 594秒）を有効化
- ブラウザ版に向けて、時計と乱数の wasm32 限定依存（`web-time` / `getrandom`）を追加（デスクトップ版には影響なし）
- 食い込み警告の追跡 fixture `penetration-warning.ori3` と照合検査を追加

一時的に外した検査（期待値・下限・許容差は変えていない）
- 重なり順の端点検査 #13・#14（`surface_order_acceptance.rs`）は 179.999° と 180° の食い違いが残るため、実測値を理由に書いて `#[ignore]`。fix-b が隔離ツリーで究明中で、0 になったら外す
- 鳥の基本形・カエルの保存順が一般制約に反する既知の欠陥は、層順修正（6単位）の完了まで `#[ignore]`

Co-Authored-By: Codex <noreply@openai.com>
```

## 11. 中断完了の確認（2026-08-28 01:11）

- 稼働していた4担当すべてが報告書末尾へ「中断時点の引き継ぎ」節を書いて停止した。
  `scratchpad/desktop-red-a-report.md`(1071行)、`…\layer-order-unit2-report.md`(602行)、
  `…\collapse-motion-stage3-report.md`(349行)、`…\ci-desktop-fix-report.md`(219行)。
  既に完了していた fix-b・6-E は本書 §4 の表と各報告書がそのまま引き継ぎになる。
- アプリ（`desktop.exe`）は 0 プロセス。中断後に新しいコンパイル・検査は実行していない。
- 最終スナップショット（この時点の全内容）:

| ref | commit |
|---|---|
| `wip/cifix` | `f892591` |
| `wip/collapse` | `4a52d33` |
| `wip/fixb` | `fafa4b3` |
| `wip/layer1` | `8997d77` |
| `wip/main-rust-slice` | `a2d437d` |
| `wip/viewer` | `f760076` |

- 未 push のコミット: ea0a8ae 以降（`git log --oneline origin/main..HEAD` で確認）。利用者へ push を依頼済み。
- **再開時の最初の一手**: §8 の確認 → §3 の「次の一手」（提案の記録値の対処を利用者に諮る）→ `scripts/check.ps1` →
  Rust 分のコミット（§9・§10）。統合後の `commands.rs`（§4.3）は未コンパイルなので、ゲートで必ず確認する。

---

# 追記: 2026-08-28 の再開後の到達点（統括が随時更新）

本節は §1〜§11 の後に足した。**再開時は §12 → §13 → `docs/報告記録.md` の 2026-08-28 の節の順に読む。**

## 12. 担当スレッド（2026-08-28 再開後。前のスレッドは MCP 再起動で全て失効した）

| 担当 | thread | 場所 / 組み立て先 | 状態 |
|---|---|---|---|
| fix-a（提案の記録値・CI検査） | `01a04537-e2d6-7351-9fb2-0a3fbd3b7d7a` | 本体 / `%TEMP%\ori3-target-fix-a` | task 25 の主張の置き換えを実施中 |
| 層順（折り鶴の展開図） | `01a0453a-40a7-7b42-8f46-fbd90cb06c65` | `%TEMP%\ori3-wt-layer1` / `ori3-target-layer1` | 参照との差分 68本の工程別の仕分けを実施中 |
| 途中姿勢（方式A） | `01a0453c-9717-78c2-aa0d-c7f2b4410d60` | `%TEMP%\ori3-wt-collapse` / `ori3-target-collapse` | ±180° の枝の選び違いの修正を実施中 |

MCP の返信は 7,200 秒の無応答で打ち切られる。**打ち切られても担当は生きている**（実際に2回起きた）。成果は報告書ファイルに書かれるので、そちらを直接読む。稼働の判定は報告書と触ってよいソースの更新時刻、およびプロセスの CPU 時間で行う。

## 13. 2026-08-28 に確定した事実（正本は `docs/報告記録.md`）

### 13.1 正しい折り鶴の展開図が確定した
`crates/ori3-layers/tests/fixtures/traditional-crane-cp.ori3` が正解である。実測: 内部頂点 44、前川定理の違反 0/44、川崎定理の違反 0/44（最大誤差 2.61e-11 rad）、山折り 61本すべて +180°、谷折り 41本すべて −180°、**0°（折らない線）0/102**。
利用者から受け取った `traditional_crane_math_bundle/`（10ファイル）と、座標差 0・頂点 0/56・辺と山谷 0/114・面 0/59 で**完全一致**した。§10.7.3 の懸念なく正解として使える。

### 13.2 生成側との差分（段階9 手順1 の実測）
| 分類 | 実測 |
|---|---:|
| 参照にあって生成側に無い折り目 | 68 / 102本 |
| 生成側の余分な折り目 | 21本（0° 6本、±180° 15本） |
| 山谷が逆 | 12本（support 10） |
| 頂点の一致 | 13（生成 33、参照 56） |
| 面の一致 | 0 / 31 |

- 余分21本: E18,E21,E25,E27,E28,E30,E41,E44,E47,E52,E57,E58,E63,E66,E69,E74,E79,E80,E85,E90,E91
- 山谷逆12本: E17,E22,E26,E31,E38,E40,E50,E53,E59,E61,E72,E73

### 13.3 今日直った2つの欠陥（層順担当・未コミット、`%TEMP%\ori3-wt-layer1`）
1. 中割り折りの入力が異なる先端の面を混ぜていた → `[1,4,15]` へ修正（採用根拠は完成外形・左右対称性・材料上の頭尾・重なり順の比較）。
2. 花弁折きの中央の折り目が隣の翼面を分割していなかった → 分割を追加。

実測: Edge 37・44 の裂け 0.5658 → **8.671119018262734e-16**（警告 2 → 0）、完成時の最大の裂け 2.288783399261119e-15、自己交差 **0組**、鳥の基本形の保存順 7/51 → **0/48 違反・破棄0**、伝承折り鶴 0/1,285 維持、決定性 10/10、clippy 警告0。

統括が更新を許可した数え上げの契約は全7件（内訳で照合する形にすること・実測と理由と日付をコメントに残すことが条件）:
辺 `+7 → +11 = 5+2+4` / driver `7 → 9 = 0°×4 + |180°|×5` / 鳥の基本形 `14 → 16面` / 完成鶴 `29 → 31面` / 単回の花弁折り `9 → 11面` / 一般制約の母数 `51 → 48 = 14+16+7+11`。

### 13.4 前川定理の違反は前から在った
raw 4/15（修正前）→ 3/15（現行）。**今日の修正が作ったものではなく1件減っている。** 原因の一部は「実角 0° の線を山または谷として数えていること」（0°除外で 3/15 → 2/15。ただし V9 が新たに違反し V12 は残る）。個別の辺を直すと違反が反対端へ移り 0/15 にならない。→ 生成の規則を直す必要がある。

### 13.5 動画の飛びの原因
記録された折り鶴の終点は健全（裂け 7.63e-12、自己交差 0、平面離れ 0、山谷の記述の矛盾 0/102、辺長の相対差 7.2e-15）。**欠陥は解く側。** 差 `1.28105521614296025` は面 52・53・54・55・57 の**5面すべてで同一値**（witness は VertexId 3 / slot 2）で、数値のずれではなく**±180° の枝の選び違い**。平らな終点では山谷の符号が 102本すべて判別できないため。規約 §10.7.5 と同じ形。ヒンジの信頼差 48.390541453831119° は EdgeId 0（VertexId 8–11、山折り）。

### 13.6 方式A（曲がりを許す途中姿勢）の到達点
| # | 項目 | 条件 | 最新の実測 |
|---:|---|---:|---:|
| 1 | 連続移動 | ≤0.02 | 1.24966197799120882 |
| 2 | 裂け | ≤1e-6 | 7.63380505762875371e-12 |
| 3 | 貫通 | 0 | 最大4・累積59 |
| 4 | 辺・対角の変化 | ≤1.5% | 23.9681643964% |
| 5 | 平面離れ | ≤0.0333656245301764479 | 8.82149485940% |
| 6 | 端点 | bit一致＋直前区間≤0.02 | bit一致、最終区間 positions 0.000619229806638072 |
| 7 | 決定性 | 10/10 | 10/10 |
| 8 | 製品の経路の初回 | ≤200 ms | 2,567.8642〜2,841.6377 ms |
| 9 | 2回目以降 | ≤16 ms | 0.1779 ms |

作り直し（前の姿勢から温めて解く）で 4 は 56.88%→23.97%、8 は 152,547ms→2,567ms へ改善した。残りは 13.5 の枝の選び違いが主因と見ている。実装の入口は `bendable_pose.rs`、恒久 gate は `hybrid_crane_gate.rs`。

### 13.7 統括が対応する事項
- `crates/ori3-layers/tests/fixtures/` の作品ファイル**9件が未追跡**（`git status` で `??`）。規約 §10.1 違反。未コミットの Rust 分と一緒にコミットして解消する。
- **#18（`completion_search_uses_safe_subsets_and_is_deterministic_ten_out_of_ten`、最適化あり）が、規約の実測 1,332 秒に対し CPU 6,759 秒でも終わらず、統括が停止した。** 作業機の他の負荷と切り分けていないため断定はできないが、未コミットの変更で探索が重くなっている可能性がある。**push 前に、他の作業を止めた静かな状態で測り直すこと。**
- 提案の記録値は利用者決定により「記録は再測定値へ更新、許容 4項目は現行値を据え置き」。新記録に対し position は 74.13562556544%、width は 60.000000000000085%。標本は当面3作品（折り鶴・やっこさん・鳥の基本形）。

### 13.8 利用者に諮る予定の事項
- 生成側と参照の差分 68本をどう扱うか（生成器を参照に一致させる／今日の改善だけ先に取り込んで別途計画する／動画は検証済みの参照から作る）。段階9-1 の仕分け結果が出てから諮る。
