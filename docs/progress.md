# 開発進捗

各タスク完了時に新しい記録を上に追記する。1件3〜10行以内(要件定義書NFR-006: 長文の経緯記録は禁止)。

## 2026-08-06 - 引く操作と手順記録の細かな制限を解消した

- **根の面をつかんでも引けるようにした**(`lib/grabDrive.ts`)。ソルバーは全域木の根の面をその場に固定するため、根をつかむと経路が空で動かせなかった。実際の紙はどこをつかんでも動かせるので、根をつかんだときは**根に接する折り線を「相手側が逆に動く」向き(速度の符号を反転)で駆動**する。つかんだ所が止まって見え、残りの紙が逆に動く=手で持った紙とまったく同じ見え方になる。折り線が1本も無い1枚きりの紙のときだけ従来どおり動かない
- **引いた形を手順に残しても収束の警告が出なくなった**(`lib/poseStep.ts`)。角度を**小数3桁に丸めていた**のをやめ、f64のまま書き出す。頂点まわりのループ閉包は角度どうしを厳密に結んでいて、丸めると毎回の再生で「追従計算が収束していません」が出ていた(形のずれは見えないので気づきにくい)。**小数9桁でも足りない**(収束判定は残差RMS 1e-13)ことを実測で確かめたので、丸め幅を縮めるのではなく丸めをやめた。`.ori3` はJSONなので大きさへの実害はない
- **手順の途中への挿入は「制限ではない」と判断**した。挟んだ折りは展開図へ**辺を足すだけ**で、既存の折り線が分割されても `resolve_driver_edges` が断片を全部拾うため、**後続の手順が飛ばされて赤くなることは起きない**(Rust側テストで確認)。後続と矛盾する折りを挟んだ場合は最も近い形と警告を返す。**自動修復はしない**(利用者の意図と違う形になるため。設計原則: 止めずに警告)。代わりに**警告に直し方を書き添える**ようにした(`lib/techniques.ts::withFixHint`。設計原則3b)
- テスト: フロント11件(根の面をつかむ・丸めない記録・警告の言い換え)、Rust 2件(丸めると警告が出て丸めなければ出ない/挟んでも後続は飛ばされない)を追加。**全5検査合格**(既存テストは根の面の1件の期待値のみ更新)

## 2026-08-06 - 左右対称描画を削除と線種変更にも効くようにした

- **「線を引くときだけ効く」制限を解消**。`applyEdit` の入口で `RemoveEdges` / `SetEdgeKind` の辺IDに**鏡映の相手を足す**ようにしたので、展開図の右クリック消し・Deleteキー・コンテキストパネルのどこから消しても左右そろって変わる(呼ぶ側の変更ゼロ)
- 対称軸は紙の中心線決め打ちではなく、**展開図から自動で見つけた軸**(`buildMirrorIndex`。作品ごとに1回判定してキャッシュ)を効かせたい順に当てる。折り目がまだ少ない展開図のために紙の縦の中心線も最後の候補に残した。相手を探す索引は輪郭・補助線も含む全ての辺で作る(`lib/mirrorEdit.ts`)
- 軸の上に乗る線・相手のいない線はその線だけを処理する(警告は出さない)。画面の説明文も実態に合わせた
- テスト9件追加(相手探しの純関数5件・ストア4件)。既存テストは文言1件の更新のみで全件合格(全5検査合格)

## 2026-08-06 - ねじり折りの中央多角形を3D画面で指定できるようにした

- **「エンジンは任意の多角形に対応済みだが画面から指せない」制限を解消**。ねじり折りツール選択中に**3D表示で角を順にクリック**すると、その形のまま折れる(辺の数も長さも自由)。3つそろうと**多角形とそこから出るひだの折り線**を黄色で下見でき、下のパネルの「適用」で折る
- Ctrl+クリックで**中心も指定**できる(既定は多角形の重心を明示的に送る)。Backspaceで直前の角を取り消し、Escでやめる。今すべきことは立体表示の1行案内に常に出す(いま何個・あと何個・使えるキー)
- ねじる角は下のパネルの数値(既定30度)と「ねじる向き」で決め、`reference_point` は1辺目の中点を中心のまわりに回して作る。多角形で指したときは層を選ばなくてよい(選ばなければ全ての層)。従来の「1辺をドラッグして正多角形」も残した
- 純関数は `lib/twistPolygon.ts`(点の追加・取り消し・3点未満の判定・重心・下見の線分)。**常設4区画・ツールレール10個・Tauriコマンド13個は不変**、useStateなし
- テスト20件追加(純関数11件・ストア2件・画面7件)。既存テストは下ごしらえの項目追加に伴う1件の更新のみで全件合格(全5検査合格)

## 2026-08-06 - 左右同時に引く機能の対称軸を展開図から自動で見つけるようにした

- **実データで検証**: 鶴・カエルの完成形の展開図を受け入れテストからJSONへ書き出し(`apps/desktop/src/lib/__fixtures__/`)、フロント側テストで対称軸の判定を確かめた。**鶴の羽6本・カエルの前足21本の折り線がすべて左右で正しく対になった**
- 判明した事実: 実物の展開図は**どの軸でも完全には対称でない**(鶴は頭の中割り折り、カエルは体の段折りが片側だけ)。そこで完全一致ではなく**一致率**で軸を判定するようにした
- 軸は紙の形から決め打ちせず展開図から探す。候補は紙の縦・横・対角に加え「折り線の向きの組の平均」(鏡映は向きθを2α−θへ移すので軸はこの形に限られる)。並べ方は**根の面(ソルバーが固定する基準面)を保つ軸が先、次に一致率順**。鶴ではこれが羽どうしを入れ替える対角線を正しく選ぶ決め手になった
- 軸の上に乗る折り線(体の中心線)は相手なしで打ち切る。別の軸を当てると体の中心線を足の折り目と対にしてしまうため
- 判定は引き始めに1回だけ行い展開図ごとに使い回す。辺2万本で約19ms。テスト12件追加、既存テストは無修正で全件合格(全5検査合格)

## 2026-08-06 - つぶし折りで奥の層が本体につながっていても折れることを確かめた

- 「奥側の層が本体につながっていると裂ける」制限は**既に解消されていた**(奥側も二等分線Mで折り返し、折り返した紙を袋の中へ入れる分岐)。今回は**専用の回帰テストで固定**した(`crates/ori3-layers/tests/techniques.rs`)
- 予備基本形の袋(2層ともほかの袋と背でつながっている=向こう端が固定)を**10°・45°・80°の3通り**につぶし、いずれも**警告なし**・6面・折り返した2枚が袋の中(もとの2層の間)・記録した手順からの再生一致・山谷と重なり順の一致まで検証
- 探索で確かめた範囲では、裂ける警告が残るのは実際の紙でも折れない指定だけ: 袋の広がりの外へ倒す指定(相手の袋につながったまま裏返す)と、袋の片方の層だけを選んだ指定
- つぶし折りは既にある折り目を開く動きを含むため t=0.99 の高さ読みが使えない(`acceptance_frog.rs` と同じ事情)ので、重なりは `assert_fold_senses` で確かめた。テスト1件追加、既存テストは無修正で全件合格(全5検査合格)

## 2026-08-06 - ねじり折りで任意の形の中央多角形を指定できるようにした

- **ねじり折りの中央多角形が「1辺を中心のまわりに回してできる形」に限られる制限を解消**した。`TechniqueInput` に `polygon`(頂点を順に並べる)と `center` を `Option` で足し、`SeqOp::Technique` にも `#[serde(default)]` で通した(**Tauriコマンドは13個のまま**。古い作品ファイルはそのまま読める)
- 折り線の向きの決め方を一般化: ひだの境の折り線 q_j は「p_j を頂点jの**外角**だけ回した向き」になる(正多角形では外角が 2π/n で一定だったのを頂点ごとに測るようにした)。これで辺の数も長さも仮定しなくなり、**不等辺三角形・不等辺四角形でも警告なしで折れる**(中央1面+ひだn面+腕n面、折り線3n本、再生一致)
- 中心も明示指定できる(省略時は従来どおり選んだ層の重心)。Errにするのは頂点3つ未満・続く頂点が重なる・中心が頂点と重なる指定だけで、中心が多角形の外・凹んだ多角形は警告して続ける
- ねじり折りは腕と中央が折り目2つ分離れているため t=0.99 の高さ読みが使えない(中割り折りと同じ事情)ので、重なりの検証は `assert_fold_senses` で行った。テスト3件追加、既存テストは期待値・判定とも無修正で全件合格
- 画面(3D上での多角形指定)は次のタスク。エンジンとしては任意の多角形をねじれる

## 2026-08-06 - UI-007 3Dで引くとき左右の羽が同時に動くようにした

- 駆動する折り線を選んだあと、**対称軸で折り返した位置にある折り線**を展開図から探し(`lib/mirror.ts` の一致判定とEPSを流用)、見つかれば**同じ角度**で一緒に動かす。軸の上の線・軸に対称な線・相手のいない線は従来どおり1本だけ。純関数は `lib/grabDrive.ts::mirrorHingeOf`
- 対称軸は**紙の縦の中心線を第一候補**にしつつ、正方形の紙では**対角線も候補**に入れた。このアプリの折り操作で折った鶴は正方形を半分に2回折って組み立てるため、首と尾が向かい合う2隅・羽が残りの2隅から出る=**展開図の上での左右の軸は対角線**になり、縦の中心線だけでは羽が対にならないため(`acceptance_crane.rs` の折り順から導出)
- 切替は**引くツールを選んだときのコンテキストパネル**に置いた(**ツールレール10個のまま・常設4区画不変・Tauriコマンド13個のまま**)。**既定はオン**(折り紙の作品はほとんど左右対称で、相手がいなければ自動で1本だけに戻るため既定オンの損がない)。設定は作品の中身ではないので localStorage(`lib/displayPrefs.ts`)
- 表示: ドラッグ中は相手の折り線にも同じ色を付け、案内の1行を「左右対称に動かしています」に切り替える
- テスト: 純関数8件(対を見つける/中心線上・対称でない展開図・輪郭は1本だけ/対角線が軸の場合)・ストア3件・画面2件・好み2件を追加。**全5検査合格**(Rust 46件、画面315件)。実物の鶴の展開図はRust側でしか組み立てられない(`crates/` は並行編集中で触れない)ため、鶴の肩と同じつながり方の紙で代用した

## 2026-08-06 - 残る制限2件を解消(紙の色・方眼の保存/手順の途中への挿入)

- **紙の色と方眼が作品ファイルに保存されるようにした**: `EditOp::SetDisplay { display }` を追加し、`store.rs::apply_edit` で `Document.display` へ書く。**Tauriコマンドは13個のまま**(既存の `edit_apply` を使う)。スナップショット方式なので元に戻す/やり直しにそのまま乗る。方眼の数が2〜64の外なら**丸めて警告**(止めない。色は `[u8;3]` なので0〜255は型が保証)
- localStorage の見た目の記憶(`lib/displayPrefs.ts` の `Prefs.display`)は**削除**した。残すと、人からもらった作品を開いたときにその作品の色を黙って上書きしてしまうため。新規作成時の初期値は `Document::new`(赤い表・白い裏・8分割)が持つ。localStorage に残すのは分割比と左右対称に描く指定だけ
- **手順の途中へ折り操作を挿入できるようにした**: 手順の永続化が面ID・辺IDではなく幾何(折り線の線分・層順序の代表点)で行われているため、挟んだ折りで既存の折り線が分割されても `resolve_driver_edges` が断片を全部拾い、代表点も現在の面へ解決し直される。`up_to` に途中の値を許し `sequence.insert(up_to, step)` へ変更。後続が成り立たなくなった場合は既存の「飛ばして警告」に任せる(「止めずに警告」原則)
- 画面: タイムラインの各手順の前に**「＋」(この手順の前に折りを挟む)**を置いた(**常設4区画不変・ツールレール10個のまま**)。押すとその1つ前の形が出て、そこで折ると挟まる。途中の手順を見ている間も折れるように `canFoldNow` を緩め、どこへ挟まるかを立体表示の1行案内に添える。引きかけの折り線は見る位置を移したら捨てる(別の形の上の線になるため)
- テスト: serde往復1件・store側3件・画面側5件(タイムラインの画面テスト新設)を追加。**全5検査合格**(Rust 46件、画面298件)

## 2026-08-06 - CPE-010 左右対称に線を引けるモードを追加

- 何も選んでいないときのコンテキストパネル(紙の色・方眼の隣)に**「左右対称に描く」**の入り切りを置いた。**ツールレールは10個のまま・常設4区画も不変・Tauriコマンドは13個のまま**。設定は端末に覚える(`lib/displayPrefs.ts`)
- 対称軸は**紙の縦の中心線**に固定(v1。斜め・横の軸は無し)。線を1本引くと `AddSegment` を引いた順に2回送る(直列化キュー経由なので適用順は変わらない)。中心線に重なる線・もともと左右対称な線は二重登録を避けて1本だけにする。計算は純関数 `apps/desktop/src/lib/mirror.ts`
- 2D展開図に**対称軸を薄い破線**で出し、線を引いている最中のプレビューも反対側に見せる。画面の上に「左右対称に描いています(線を引くときだけ効きます)」と出して今の描き方が分かるようにした
- v1では**線を引くときだけ**効く(線を消す・種類を変えるときは片側ずつ)。その旨をパネルの説明文にも書いた。テストは純関数5件・ストア3件・画面2件・操作1件を追加(画面のテスト290件、全5検査合格)

## 2026-08-06 - UI-007 3Dの紙をつかんで引くと全体が連動して動く操作を追加

- ツールレールに**10個目**の「引く」を追加(選択ツールに相乗りさせなかった理由: 3Dの左ドラッグは視点回転に使われており、奪うと立体を見て回れなくなる。引くツールでは代わりに**右ドラッグで視点を回せる**ようにした)。**Tauriコマンドは13個のまま・常設4区画も不変**
- 駆動する折り線の選び方: つかんだ面から根の面までの全域木の経路(Rust側 `ori3-rigid::tree::build_forest` と同じ組み立てをTSで再現)のうち、**モーメントアーム v = 軸 × (つかんだ点 − 軸上の点) がいちばん大きい1本**。ドラッグ量は「つかんだ点が指にいちばん近づく回転量」(最小二乗)で角度に直し、既存の**60ms間引き+最新1件キュー**で `pose_solve` を呼ぶ。純関数は `apps/desktop/src/lib/grabDrive.ts`
- **折り上がった作品からも引ける**ようにするのが要。引き始めに、今見えている立体から全ての折り角を読み取って1回だけ送り、ソルバーの出発点(warm start)を今の形に合わせる。平らに畳まれた所は幾何だけでは±180°の符号が決まらないので展開図の線種で決める(山=+180/谷=−180)
- 折り鶴で実測(engine直接): 羽の付け根を−180°→−216°まで引くと**毎回収束し、他の31本の折り線が連動**して厚みが 0→0.26(紙の長辺=1)まで膨らむ。引いた形は「この形で仕上げる」で手順として残せ、再生でずれ 3e-6 に戻る
- 既知の制約: 左右対称の相手を一緒に動かす機能は入れていない(片羽ずつ引く)/根の面そのものをつかんだときは動かせない/仕上げ手順の角度は小数3桁に丸めるため、ループのある作品では再生時に収束警告のバッジが点くことがある(形のずれは 3e-6 で見た目には出ない)

## 2026-08-06 - 折り技法の層の指定と重なり順の細かな不具合を修正

- **花弁折りで無関係な層が動きうる穴**: 隣の層の羽で、領域に紙が残らないときに層の指定が空のまま動きを作っていた。`flat_motion` は空の指定を「全ての面」と読むので、隣の層が領域から全部落ちると重なり全部が動く。空なら部分を作らないようにし、単体テストで固定(`crates/ori3-layers/src/techniques.rs`)
- **`Beside` の置き場所が無いときに向きが無視された**: 基準面の紙が全部動いて残りが無いと、向こう側(Down)を指定しても手前の最上へ入っていた。向きどおり下/上へ入れるようにし、回帰テストを追加(`crates/ori3-layers/src/flat_motion.rs`)
- **ちょうつがい位置の規則を単体テストで固定**: 角が90°でないたこ形のフラップで、斜めの折り目が紙の外へ出る点(1e-9)を突き合わせるテストを追加。旧規則(縁の長さからの見積り)では紙が裂ける警告が出るので、規則の違いを確実に捕まえられる
- あわせて: 斜めの折り目が既存の折り目に完全に乗っても止まらないようにし、見積りに落ちたときは警告に出す/どの部分にも入らない指定層を警告に残す/袋の並びを面IDで決定的にする/つぶし折りで外とつながっているかの判定に平らな折り目(Aux)も数え、動きが変わったことを手順の注記で伝える
- 既知の問題: なし(全5検査合格。テスト3件追加、既存テストは無修正で全件合格)

## 2026-08-06 - SIM-009 立体的な仕上げの形を手順として残せるようにした

- 折り鶴の中央の膨らみのような**平らに畳まない形**を、コンテキストパネルの**「この形で仕上げる」**ボタンで手順一覧へ残せるようにした。押すと今の全ての折り線の角度(利用者の指定+ソルバーが求めた従属角度)を `DriverLine` の列にし、`kind: "Pose"` / `layer_order: null` の `FoldStep` を既存の `PushStep` で送る(**Tauriコマンドは13個のまま・常設4区画も不変**)
- 角度が全て0のときはボタンを消さず、「まだ角度が付いていません」と理由を出して押せなくする(再生中・折り線なしも同様)。組み立ては `apps/desktop/src/lib/poseStep.ts`、判定は `poseRecordReason`(`appStore.ts`)
- **展開図を編集しても立体形状が戻る**ことをRust側で検証(`crates/ori3-layers/tests/replay.rs` に2件追加): 90°に立てた形を記録したあと別の折り線を足して辺IDが変わっても、再生の z幅は 0.5 のまま一致する。平らに畳む手順の後ろに積んだ場合も「後のステップが勝つ」で立体のまま仕上がる
- 記録後は一時的な角度指定を捨て、立体表示は手順の再生結果に切り替わる(平らへ戻す計算は送らない)。画面のテスト252件→258件。既知の制約: 同じ形で2回押すと同じ手順が2つ増える(不要な方はタイムラインから削除する)

## 2026-08-06 - 折り図の矢印と書き出しの細かな不具合を修正

- **矢印が1本しか出なかった問題**: 段折り・ひだ寄せ・ねじり折りのように1手順で何本も折る場合、折り線は全部描かれるのに矢印が1本だけだった。折り線ごとに矢印を出すようにした。1つの指示が複数の折り目に分かれるときは一番長い1本を代表にし、多すぎるときは等間隔に間引いて最大6本にする(`crates/ori3-export/src/diagram.rs`)
- **矢印の向きが揺れた問題**: 折り線が紙の重心をちょうど通る左右対称な半分折りでは、向きの判定がほぼ0になり丸め誤差だけで向きが決まっていた。差がごく小さいときは折り線の向きから決める(画面の上側、それも決まらなければ右側)ようにして、同じ形からは必ず同じ絵が出るようにした
- **画像の大きさに負の数を入れると英語のエラーが出た問題**: 受け取る型を広げ、0以下・上限超えをすべて日本語で理由を返すようにした(`apps/desktop/src-tauri/src/commands.rs`。コマンド数は13個のまま)
- あわせて、角度0(折らない)の指示は線も矢印も出さないよう揃え、折り図の画像は途中で書き込みに失敗したら書けた分を片付けるようにし、日本語書体が1つも無い機械では注意を出すようにした
- 既知の問題: なし(全5検査合格。テスト7件追加)

## 2026-08-06 - 大きな展開図でも編集と検査が速くなるよう改良

- 一様格子(空間ハッシュ)の部品 `crates/ori3-cp/src/spatial.rs` を新設。線分をEPS分太らせた帯が触れるセルにだけ登録し、候補は必ず添字の昇順で返す(BTreeMap保持なので**同じ入力→同じ結果・同じ警告順**)
- **検査(`validate`)**: 全辺ペアの総当たりをやめ、同じセルに載った辺どうしだけを調べるようにした。**辺2万で1,329ms→20ms(約66倍)**。警告の文面も順序も変えていない
- **線の挿入(`insert_segment`)**: 頂点はID表+格子、既存辺は格子で近傍だけを調べるようにした。**辺2万で1本228ms→17ms(約13倍)**。線を引いて辺2万まで作る過程は8.2秒→1.1秒。吸着(EPS)の挙動は変えていない
- どちらも辺数にほぼ比例(O(E))になった。実測表は `crates/ori3-cp/tests/perf_large.rs` の冒頭コメント。既存テストは無修正で全件合格
- 残る律速は `solve`(辺2万・冷間で約2.6秒)。面抽出・平坦折り判定・書き出しは元から線形で問題なし

## 2026-08-06 - 紙の大きさ・色・方眼と、点の移動・手順の並べ替え・区画の分割比を画面から扱えるようにした

- **PAP-001**: 「新規」を押すと**新規作成ダイアログ**が出て、正方形/長方形と大きさ(mm)を選べる(既定150×150、よく使う大きさのボタン付き、形は見本の四角に映す)。作成は既存の `document_new` のまま
- **PAP-003 / CPE-003**: 紙の表・裏の色と方眼の分割数(2〜64)を、**何も選んでいないときのコンテキストパネル**で変えられる(常設パネルは増やさない)。展開図は表の色で塗り、立体表示・吸着にもその場で反映する
- **表示設定の持ち方**: 色・方眼を変える口が `EditOp` にもコマンドにも無く、コマンドは13個で打ち止め・`crates/` は触れないため、**画面側の好みとして持ち localStorage に覚える**方式にした(`lib/displayPrefs.ts`)。読み込んだ作品の `doc.display` へ差し込むので描画側はこれまで通り。**作品ファイル(.ori3)には保存されない**のが既知の制約
- **CPE-006**: 「選択」ツールで点をドラッグして動かせる。**押している間はプレビュー(つながる線を破線で引き直す)、離したときに `MoveVertex` で確定**。吸着は他の点とグリッド交点のみ(自分と、一緒に動く線には吸い付かない)。Escでやめれば元のまま。畳めなくなる場合は従来通り橙色の丸で知らせるだけで止めない
- **SEQ-005**: 折り操作の途中挿入は仕様上断られるため、**手順を選んで「前へ/後ろへ動かす」**(`RemoveStep` → `InsertStep`)を用意した。端では消さずに理由を出す。元に戻すは2回ぶんになるのが既知の制約
- **UI-004**: 2Dと3Dの境目をドラッグ(キーの左右も可)で2〜8割の範囲で変えられ、次回起動時に戻る。常設4区画は不変
- 画面のテスト225件→252件。SIM-009(仕上げの角度)は未着手

## 2026-08-06 - 大きな展開図(線1万本以上)での性能を実測した

- `crates/ori3-cp/tests/perf_large.rs`(`#[ignore]`付き。重いので常用の検査では走らない)で辺1,012 / 5,100 / 9,940 / 20,200のミウラ折り風格子を作り、主要8処理をrelease/debugで実測。表は同ファイルの冒頭コメントに記録
- **辺9,940(≒線1万本)でも編集・表示は実用速度**: 面抽出12ms / 線1本追加102ms / 平坦折り判定4.5ms / 手順再生52ms / SVG書き出し22ms / めり込み判定25ms(すべてrelease)。メモリは辺20,200でも最大約55MB
- **律速は`validate`(全辺ペア総当たり=O(E²)、辺9,940で623ms・20,200で2.6秒)と`insert_segment`(1回ごとに全辺×全頂点の線形探索=O(E·V)、辺20,200で1本405ms)**。線を引いて辺20,200まで作る過程は合計13.4秒
- `solve`は駆動の仕方で大きく変わる(1本を冷間で駆動すると辺9,940で1.2秒・20,200で4.7秒。手順再生の3手順分は52ms/127ms)
- 改善するなら格子や空間分割で近傍探索を絞る(`validate`・`insert_segment`とも桁が変わる)。NFR-002の目標(辺1,000)自体は満たしているので今回は実測のみで実装はしない

## 2026-08-06 - Task 4-6完了 - 伝承のカエルが完成形まで折れるようになった

- **花弁折りの層の置き方を修正**(`techniques.rs`): 持ち上げた紙を重なり全体の外側へまとめて回していたのを、**袋ごとに「その袋のいちばん外側の層の隣」へ置く**(`LayerTurn::Beside`)に変更。実際の紙の花弁折りは袋を1つずつ折る動きで、持ち上げた紙はその袋の中に留まるため。袋の切れ目は**中心線に乗った折り目**(袋の口を閉じている背)で、そこを渡らずに層をたどったまとまりが1つの袋(`petal_pockets`)
- `LayerTurn::Beside` は同じ基準面へ続けて置くと先に置いた紙のさらに外側へ積まれるようにした(`flat_motion.rs`)。1つの袋の中で羽→中央のくさびの順に重なる
- これでカエルの基本形の**足4本が層順序の上でひとまとまり**になり(以前は4本が(1,19,26)(7,15,22)…と飛び飛びだった)、足1本をつまんで中割り折りできるようになった
- **伝承のカエルが完成形まで到達**: 基本形9手 + 足4本の中割り折り4手 + 体の段折り1手 = **14手・140面**。足の先は中心線から±45°(基本形の開き角±22.5°の外)、前足は先端から0.1414、後ろ足は0.2828。警告ゼロ・再生一致・決定性・山谷と層順序の一致を検証(テスト10件)
- **折り鶴への影響なし**(層順序・面数とも無変更)。花弁折りのフラップが1袋のときは基準面が最外層になり、従来の「重なりの外側へ」と同じ結果になるため
- 既知の問題: 足は付け根の中割り折り1回だけで、膝の曲げ・つま先は入れていない(同じ技法をもう1回重ねれば表せるが、完成形の特徴としては足4本の突起で十分と判断した)

## 2026-08-06 - Task 4-4 / 4-5 - 折り手順を1コマずつ図にしてA4に組版できるようにした

- **折り図の1コマ**(EXP-003、`crates/ori3-export/src/diagram.rs`): `render_step(doc, i)` が手順iの図をSVGで返す。絵は `flat_state_at(doc, faces, i)` が返す**その手順を折る直前の平坦状態**で、面を下の層から順に描くので上の層の輪郭が手前に出る。折り線は展開図の座標で持つ `DriverLine` を `resolve_driver_edges` で今の折り目に引き当て、その折り目に面している面の置き場所を通して畳んだ絵の上へ写す(山=赤の一点鎖線 / 谷=青の破線)。矢印は折り線と直角に、紙の重心のある側へ向ける。技法10種それぞれに小さな目印(固定のSVGパス)を添え、手順番号・技法の呼び名(折り紙の言葉)・注記を入れる
- **A4組版**(EXP-003 / EXP-004、`pdf.rs`): `diagram_svg_pages` が**表紙(題+できあがりの形)+1ページ2列×3段**のページSVGを返し、`diagram_pdf` が同じ組版を `svg2pdf` + `pdf-writer` でA4複数ページのPDF1本にまとめる(文字は形に直して埋めるので書体に依存しない)。7手順なら表紙+2ページ。手順0件は「折り手順がまだありません」のErr
- **コマンドは13個のまま**: `ExportKind` に `DiagramPdf` / `DiagramSvg` を追加。折り図の画像は選んだ場所を基準に `鶴-01.svg`, `鶴-02.svg` … と連番で並べる
- **画面**: 書き出しダイアログに「折り図(PDF)」「折り図(画像・ページごと)」を追加。手順が無いときは選択肢もボタンも消さず、選べない理由を日本語で出す
- テストはRust +12件(3手順→3コマ・山谷の描き分け・技法別の目印・7手順→表紙+2ページのPDF・手順0件のErr・コマンド層)、画面222件→225件。全5検査合格

## 2026-08-06 - Task 4-6続き - 花弁折りのちょうつがいを直し、カエルの基本形に到達

- 花弁折りのちょうつがいの位置を「縁の長さから逆算」から「斜めの折り目がフラップの外へ出る点を結ぶ」に変更。実際の紙と同じ決め方になり、止まり点がフラップの境目に乗るので折り目が3本だけの畳めない点ができなくなった(折り鶴は同じ位置になり無変更)
- 伝承のカエルが**カエルの基本形**まで到達。紙の4隅が紙の中心と同じ1点に集まり、辺の中点は先端から(√2-1)/2、根元は√2/4。8本の先が1つの先端へ集まる無駄のない基本形(40面・警告ゼロ・再生一致・決定性を確認)
- 花弁折りは1回。4つの袋は中心線に乗った折り目でつながっており、1つだけ選ぶと必ず裂けるため、重なった4袋を1回の動きでまとめて折る(結果は手順書の4回と同じ)
- 既知の問題: 足になる4本の先が層順序の上で飛び飛びになり、足1本をつまんで中割り折りできない。花弁折りが持ち上げた紙を「いちばん上へまとめて」回しており、袋ごとに「もとの層の隣」へ置き直す指定が無いのが原因(完成形は未到達)

書式: `## 日付 - タスクID - 一行要約` + 箇条書き(やったこと / 既知の問題)

---

## 2026-08-06 - Task 4-6 - 伝承のカエルの受け入れテストを追加(基本形の下ごしらえまで)

- **`crates/ori3-layers/tests/acceptance_frog.rs`(新規、4件)**: 予備基本形 → **4つの袋を開いてつぶす**までを、アプリの折り操作の列だけ(重ね折り2回・つぶし折り2+4回=8手)で組み立てて固定。畳んだ形が90°の正方形から**45°のたこ形**(開き角±22.5°・いちばん遠い点は紙の隅で√2/2)になること、紙の1/4がそれぞれ「三角・もとの紙・三角」の3層続きになること(12面・どの向きにも8層)、記録した手順からの再生一致、決定性を検証
- **`squash`の不足を1つ修正**(`techniques.rs`): 奥の半分がフラップの外の紙とつながっている(向こう端が固定されている)場合、これまでは層まるごと回していたため必ずそこで紙が裂けていた。実際の紙と同じく**両側とも二等分線で折り返し**、折り返した紙を**開いた袋の中**(`LayerTurn::Inside`)へ入れるようにした。これで予備基本形の袋が警告なしで開ける
- 表示上の重なり順は t=0.99 の高さ読みが使えない(既にある折り目を開く動きを含むため。折り鶴の花弁折り・中割り折りと同じ事情)ので、折り目の山谷と層順序の一致で代替
- 全5検査合格。既存テストへの影響なし(41件すべて合格)
- 既知の問題: **カエルの基本形(花弁折り4回)には到達できていない**。下ごしらえの状態では、紙の隅を先端にした花弁折りはちょうつがいが紙の中心の手前(先端から0.2929、中心までは0.7071)に来るため持ち上げた先端が形の内側に埋もれ、足になる突起ができない。紙の中心を先端にすると先端は外へ出るが、中心で4つの袋がつながっているため4〜6本の折り目で紙が裂ける(袋の中の紙も一緒に動かす指定ができない)。花弁折りの入力の与え方か`petal`側の対応が要る

## 2026-08-06 - Task 4-3 - 展開図を画像ファイルとして保存できるようにした

- **展開図のSVG書き出し**(EXP-001、`crates/ori3-export/src/cp_svg.rs`): 紙の実寸(mm)で書き出す(`width="150mm" viewBox="0 0 150 100"`)。線の種類ごとに見た目を変える(輪郭=黒の実線 / 山=赤の一点鎖線 / 谷=青の破線 / 補助=灰の点線)。内部座標はy軸上向き・SVGは下向きなので `y_mm = 紙の高さ - y×長辺` で反転する。補助線は出す・出さないを選べる
- **展開図のPNG書き出し**(EXP-002、`cp_png.rs`): `resvg` 0.48(text機能なし)でSVGを描き起こす。長いほうの辺の点数を指定でき、既定は2048点、短辺は縦横比のまま。上限16384点で、0や大きすぎる指定は日本語のErr
- **`document_export`コマンドを追加**(12個→**13個で打ち止め**)。ロック下ではDocumentの複製だけを取り、図の組み立てとファイル書き出しはロックの外。折り図の書き出しは今後 `ExportKind` を増やして対応する(コマンドは増やさない)
- **画面**: 上部ツールバーに「書き出し」を1つ足し、独立ダイアログ(`components/dialogs/ExportDialog.tsx`)を開く。種類・補助線の有無・PNGの大きさを選び、保存先はOS標準の保存ダイアログ。成功は「保存しました」、失敗は日本語の理由を出す。常設4区画・ツールレールは不変
- テストはRust +14件(線種別スタイル・実寸viewBox・y反転・補助線の切替・PNGの解像度と署名・コマンド層)、画面213件→219件。全5検査合格(ただし並行作業中の `crates/ori3-layers/tests/acceptance_frog.rs` は別タスクの作業途中でコンパイルできない状態のため、`--exclude ori3-layers` で確認した)
- 既知の問題: SVGの線幅・色は固定(画面のCpEditorと同じ配色に合わせたのみで、書き出し時に選べない)。GUI実機での確認はos error 4551のため未実施

## 2026-08-06 - Task 4-2 - ひだ寄せとねじり折りを追加

- **ひだ寄せ**(`swivel`、SIM-006 / M4): 基準線`line`の自由端を`reference_point`へ寄せる。支点は寄せる先から遠いほうの端。動きは1回の`flat_motion`で「基準線とその二等分線Mに挟まれたくさびをMで折り返す」+「基準線の向こうの紙は支点まわりに角αだけ回る(鏡映2回)」。折り線は2本で支点で出会い、折り返したくさびは向こうの紙に重なる(層併合)
- **ねじり折り**(`twist`): `line`=中央多角形の1辺(両端)、`reference_point`=回転量を示す点、中心は選んだ層の重心。辺が中心に張る角から辺の数nを決めて多角形を作り、2n+1個の`MotionPart`(中央=回転 / 辺ごとのひだ / 頂点ごとの腕)で表す。腕の折り線は「ひだの両側と折り目でつながる」条件から決めるので、各頂点に折り線4本が集まり平らに畳める(正方形の紙で9面・折り線12本、三角形で7面・9本、いずれも裂けも山谷の食い違いも出ない)
- 画面の「技法」サブメニューが6種→**8種**になり、`SeqOp::Technique`が`Swivel`/`Twist`を受け付ける。テストはRust 213件→218件、画面213件。全5検査合格
- 既知の問題: ねじり折りの中央多角形は「1辺を中心のまわりに回してできる形」に限られる(`TechniqueInput`が線1本+点1つしか運べないため。`flat_motion`自体は半平面をいくつでも並べられるので、辺ごとに長さの違う多角形も表せる)。またねじり折りは腕と中央が折り目2つ分離れているため、折り終わる直前(t=0.99)の高さから重なりを読む検証が使えない(中割り折りと同じ事情)

## 2026-08-06 - Task 4-1 - 沈め折りを追加

- **沈め折り**(`open_sink`、SIM-006 / M4): フラップの先端(角)を袋の内側へ押し込む技法を、汎用の折り操作`flat_motion`を1回呼ぶマクロとして追加した。`line`が沈める折り線、`reference_point`が押し込む先端側を示す点
- 表し方は「先端側を**領域**にとり、紙は`Stay`(1mmも動かさず)、`reverse_layers`で領域の中の重なり順だけを逆にする」。領域の境界線が各層のCPへ引き戻されて折り線になり(層の分割)、山谷は新しい重なり順から`flat_motion`が決め直すので先端側だけ自動で反転する
- 層の数の偶奇・先端の形・層を一部だけ選ぶことに制限は無い。Errは定義できない入力(退化した折り線・折り線上の基準点・無い層)だけ
- 画面の「技法」サブメニューが5種→**6種**になり、`SeqOp::Technique`が`OpenSink`を受け付ける。テストはRust 209件→213件(予備基本形と鶴の基本形の頂点を沈める・一部の層だけ・再生一致・原子性)、画面213件。全5検査合格
- 既知の問題: 沈め折りは紙がまったく動かない遷移なので、折り終わる直前(t=0.99)の高さから重なりを読む検証は使えない(層が浮かないため)。折り目の向きと層順序の一致で代わりに確かめている

## 2026-08-06 - Task 3-4 - 骨格を指定して展開図を提案してもらう画面を追加

- **提案ウィザード**(PRO-001/003/004/005): 上部ツールバーの「提案」から開く独立ダイアログ(`apps/desktop/src/components/dialogs/ProposalWizard.tsx`)。「出っぱりを決める → 候補を選ぶ → 使う」の3画面。常設4区画とツールレールは不変
- **骨格エディタ**は根1つ+出っぱり1〜12本の放射状に限定し、本数の増減と長さ・太さのスライダー、SVGの2Dプレビュー(線の長さ=長さ、線の太さ=膨らみ)を出す。編集の純関数は`src/lib/skeleton.ts`。画面に「角」「木構造」「円充填」などの用語は出さず、頭・尾・足…の呼び名を使う(設計原則3b)
- **`proposal_generate`コマンドを追加**(11個→**12個**、上限13個以内)。8スタートで`pack`し、得られた配置ごとに`generate`して最大4候補を返す。作品の状態を一切読まないので**storeのロックを取らない**(充填の数百msの間も他の操作が止まらない)。候補は展開図サムネイル(CpEditorと同じ線種色)と「折りたたみにくい点の数」で見せ、選んだものは`edit_apply ReplaceCreasePattern`で流し込むので以後は普通に編集・元に戻すができる
- テストはRust 206件→209件(候補が1件以上返る・同じシードで同じ結果・骨格や紙が不正ならErr)、画面199件→213件(骨格の増減とスライダー、候補選択→流し込み、失敗時の日本語表示)。全5検査合格
- 既知の問題: 生成は毎回シードを変えるので「別の置き方も見る」を押すと前の候補は入れ替わる(残して比べることはできない)。骨格は放射状のみで、胴の途中から枝分かれする形(例: 背びれ)はまだ指定できない。GUI実機での確認はos error 4551のため未実施

## 2026-08-06 - Task 3-3 - 自動配置の結果から展開図を組み立てる機能を追加

- **展開図の生成**(PRO-002/PRO-003 / 要件§8-3・§8-4): 充填で決まった円中心と紙の四隅を頂点にドロネー三角形分割で軸多角形を作り、各三角形をウサギ耳分子で埋める(`crates/ori3-propose/src/generate.rs`、分割は`triangulate.rs`)。四隅を必ず含めるので三角形が紙全体をちょうど覆う。四角形以上の軸多角形は扇状分割してから同じ処理
- **ウサギ耳分子は「二等分線3本 + 1辺への垂線1本」**にした。内心のまわりが山3・谷1になり、前川定理と川崎定理を同時に満たす。3辺すべてへ垂線を下ろす形も試したが、軸線の途中に3叉の点が大量にでき平坦折り違反が3〜5倍になったため不採用。垂線の行き先は紙の縁に乗る辺を優先する。山谷は「軸線=谷、稜線=山」の既定則
- **違反は警告として提示**: 局所平坦折り判定の違反頂点数を数え、0でなくてもエラーにしない。円が紙をはみ出す角、一直線に並んで折り線が作れない場合も日本語の警告で伝えて生成は続行する
- 精度: 「頭1・尾1・足4」で頂点22・面28・平坦折り違反3(内部頂点10のうち)。角4本を四隅に置く配置では違反0。角1〜12本すべてで`ori3-cp::validate`の警告なし。Rustテスト196件→206件、全5検査合格
- 既知の問題: 軸多角形の三角形分割は円の接触関係(タイトな辺)を見ていないため、分子の内心と円の接点がずれ、軸線の途中に3叉の点が残る(平坦折り違反の主因)。完全に平坦に折れる展開図を出すには万能分子(universal molecule)相当の実装が必要で、要件の精度目標の範囲外

## 2026-08-06 - Task 2-8 - 30秒ごとの自動保存と、異常終了後の復元機能を追加

- **自動保存**(SYS-003): 背景スレッドが30秒ごとに、未保存の変更があるときだけ作業中の内容を別ファイルへ書き出す(`apps/desktop/src-tauri/src/autosave.rs`)。書き出し先は保存先があれば`<保存先>.autosave`、まだ保存していない作品はアプリデータ配下の`無題.ori3.autosave`。どこへ書いたかは同じ場所の目印ファイルに残し、次の起動時にそれを見る
- **`store.save`は流用していない**。`save`は保存先パスと未保存フラグを書き換えるため、自動保存に使うと本来の保存先が乗っ取られる。ロック下では`autosave_snapshot`でDocumentの複製を取るだけにし(`path`/`dirty`は無変更)、JSON化とファイル書き出しはロックを解放してから行う
- **復元の提案**: 正常終了時と明示保存の成功時に自動保存ファイルを消すので、起動時に残っていれば異常終了と分かる。`recovery_check`/`recovery_restore`の2コマンドを追加(9個→**11個**、上限13個以内)。画面には「前回の終了が正常に行われませんでした…復元しますか?」のダイアログを出し、「復元する」「破棄する」から選ぶ(常設4区画の上に重ねて出す。ダイアログはOS標準のファイル選択と合わせて2種)
- テストはRust 192件→196件(保存先と未保存フラグを変えないこと・変更が無ければ書かないこと・復元で内容と保存先が戻ること・破棄で消えること)、画面190件→199件(起動時の確認→ダイアログ表示→復元/破棄の分岐)。全5検査合格
- 既知の問題: **実機での確認ができていない**。この環境のアプリケーション制御ポリシー(os error 4551)が`desktop.exe`の起動を拒み続けるため(テスト用の実行ファイルは削除・再実行で通るが、GUIの実行ファイルは十数回試しても通らない)、復旧ダイアログの実画面は未確認。強制終了→再起動の確認は次に実機を動かせるときに行う

## 2026-08-06 - Task 3-1/3-2 - 骨格の指定と、紙の上への円・川充填を追加

- **骨格モデル**(PRO-001): 角(頭・尾・足)を根付き木で表す`Skeleton`を追加(`crates/ori3-propose/src/skeleton.rs`)。各辺に長さ、各葉に太さ係数を持つ。木として正しいか(根1つ・親の存在・循環なし・長さと太さが正・角が1〜12本)を調べ、理由を日本語1文で返す
- **円・川充填**(PRO-002 / 要件§8-2): 葉の円中心と縮尺を変数に、「2本の角の距離 ≥ 縮尺×(円半径+経路上の川幅+円半径)」と「円中心が紙の内側」を制約に縮尺を最大化する(`packing.rs`)。紙の角・辺への貪欲配置から始め、押し離しの掃引 + 縮尺の二分探索 + 揺さぶり直しで解く。乱数シード別に8スタート走らせ、上位4候補を返す(PRO-005)
- 返す縮尺は「その配置が実際に満たす値」なので**制約違反は常に0**(円の重なり・紙のはみ出しがない)。同じシードなら結果も同じ
- 性能: 12葉・8スタートで**約147ms**(debugビルド)。単位正方形に等しい角を並べる問題で、既知の最適配置に対して達成率84〜100%(6本以下は99%以上)
- 既知の問題: 角が11〜12本で全部同じ長さのときは格子状の配置で止まりやすく、最適の約85%にとどまる。要件どおり厳密な最適性は保証していない。充填結果から折り線を作る工程(要件§8-3/8-4)はまだ

## 2026-08-06 - Task 2-9 - 折り鶴が折れることを確認する自動テストを追加

- **M2受け入れテスト**として、アプリが提供する折り操作の列だけで折り鶴を完成させる自動テストを追加(`crates/ori3-layers/tests/acceptance_crane.rs`)。展開図の手作業編集は一切していない
- 手順は標準の折り方どおり11手: 半分に折る×2 → 開いてつぶす×2(予備基本形)→ 花弁折り×2(鶴の基本形)→ 中割り折り×3(首・尾・頭)→ 羽を下げる×2
- 各工程で層数・層順序・畳んだ形を検証し、完成形は首・尾・頭・羽の付け根から見た長さと角度で確かめる。記録した手順から再生した形が構築時と一致すること、2回折っても同じ結果になること(決定性)も確認
- **実装の穴を1つ埋めた**: 花弁折りが持ち上げた紙を必ず手前へ回していたため、背面の花弁折り(鶴の基本形に必要)が表せなかった。つぶし折りと同じ`open_to_back`指定を花弁折りにも通した
- 既知の問題: 花弁折り・中割り折りの後は「折り終わる直前の高さ」による重なり順の検証が使えない(開く動きの途中で紙が大回りするため)。その工程は折り目の山谷と層順序の一致で検証している。羽を下げる折り線は肩の線そのものには置けない(そこは羽を体につないでいる折り目で、折り直せない)ので、肩の少し上に置いている

## 2026-08-06 - Task 2-7 - 作図の補助線・折りたたみ可否の注意表示・紙のめり込み警告を追加

- **作図補助**(CPE-005): ツールレールに「作図」を追加(常設9個、上限10個以内)。サブメニューで二等分・垂線・等分(2〜8)・角度線(22.5/45/30/15°刻み)を選び、展開図で点や線をクリックすると補助線が引かれる。画面の左上に「次に何をクリックするか」を常に1行で出す(Escで中止)。等分は各等分点を端点にした短い目印を出すので、次に線を引くときその点へ正確に吸着できる
- **平らに畳めるかの注意表示**(CPE-009): 内部の点ごとに前川定理(山−谷=±2)と川崎定理(1つおきの角の和=180°)を検査し、合わない点を展開図で橙色の丸で示す。**操作は止めない**。点にカーソルを乗せると「山と谷の本数が合いません」「向かい合う角の和が合いません」と理由が出る。補助線は折りに関与しないので数えない
- **紙のめり込み警告**(SIM-007): 立体表示の面同士が突き抜けていないかを調べ(AABBで絞ってから三角形の貫通判定)、見つかれば「紙が重なって食い込んでいます」を3D画面の警告バッジに出す。折り目でつながった面・同一平面の重なり・平らに畳んだ状態(z≒0)は正常として数えない。面400枚でdebug約9ms
- Tauriコマンドは9個のまま。画面で使う作図の計算はTS側(`lib/construct.ts`)に同じ式を置き、結果を既存の`edit_apply`(補助線の追加)で送っている
- テストはRust 174件→192件、画面169件→190件。全5検査合格(clippyは並行作業中の`ori3-layers`のテストを除いて確認)
- 既知の問題: 局所条件は必要条件なので、通っても層の重なりで畳めない形はある。めり込み判定は凸でない面を扇状に割るため、まれに実際より広く見積もる(警告を出しすぎる側)

## 2026-08-06 - Task 2-6c - 紙をつかんで動かす直感的な折り操作に変更

- 折る操作を「折り線を引く→パネルで向き・対象層・動かす側を選ぶ→折るボタン」から、**立体表示で紙をつかんでドラッグするだけ**に変えた(UI-007)。つかんだ点と離した点の垂直二等分線が折り線になり、つかんだ紙が離した位置へ倒れてくる。近くに既存の折り目・紙の縁があればそちらへ吸着する
- 動かす紙は自動で選ぶ。実際に描かれている三角形を当てて「つかんだ層」を決め、そこから**折り線の動く側でつながっている面**まで広げる(動かさない側でつながる面はそこが蝶番になるので付いてこない)。Shiftで重なった紙を全部、Altでいちばん上の1枚だけに変えられる
- ドラッグ中は**折った結果の形を半透明の青で重ねて下見**できる(UI-008)。折り線と動く範囲も光らせる。バックエンドへは送らず、動く層の判定はfold_throughと同じ規則で計算する
- 立体表示の上端に**今できることを1行で常に表示**(UI-009)。折れないときもボタンは消さず「再生中は折れません」など理由を日本語で出す。修飾キーの意味も常に出す
- 位置をきっちり決めたいときのために、Ctrl+ドラッグで折り線を引いてパネルで指定する操作も残した
- 画面テストの土台にjsdom+@testing-library/reactを入れ(既存テストはnode環境のまま、必要なファイルだけjsdom指定)、ヒント表示・ドラッグで折る経路・下見の表示にテストを付けた。画面テスト142件→169件、全5検査合格。実機でも説明なしのドラッグ2回で紙が2回折れることを確認
- 既知の問題: つかむ位置は面の当たり判定で正確だが、離す位置は畳み平面への投影なので、層のずらし表示のぶん(最大で長辺の3%)だけ折り線がずれることがある

## 2026-08-06 - Task 2-6b後半(一部) - つぶし折り・花弁折りで折れなかった形に対応

- つぶし折りに「向こう側へ開く」指定を足した(`TechniqueInput`/`SeqOp::Technique` の `open_to_back`。省略時は従来どおり手前へ開く)。実際の紙では奥へも開けるので、片方に決め打ちしない
- 花弁折りのちょうつがい線を、左右の縁の長さの平均ではなく左右それぞれの長さから決めるようにした。左右が同じなら従来どおり中心線に直交する線、違えば斜めの線になる。縁の長さが違うフラップ(2:1の紙の角)でも裂けずに折れることをテストで確認した
- 平らに畳めない形の警告は「縁が中心線の横まで倒れて二等分線とちょうつがいの交点が無限遠へ飛ぶ場合」だけに絞った(根拠はコード内コメント)
- テストはRust 172件→174件。全5検査合格
- 既知の問題: つぶし折りの奥の半分は依然として層まるごと動く。背が開くには奥側は「裏返らない動き=回転」でなければならず、折り線で囲った領域を回転させると必ずその折り線で紙が離れるため、楔領域にしても解決しない。つながっている層を一緒に動かす仕組みが要る(次のタスク)
- 既知の問題: 「向こうへ開く」はまだ画面から選べない(選択状態を持つ `store/appStore.ts` が並行作業中のため触れていない)

## 2026-08-06 - Task 2-6b後半(一部) - 「花弁折り」を選ぶだけで折れるように追加

- 技法サブメニューを5種にし、「花弁」(花弁折り)を足した。重なった層をクリックして選び、先端と行き先を通る中心線をドラッグ、基準点で持ち上げる先端を指すと折れる
- 専用の仕組みは持たず、汎用の折り操作を1回呼ぶマクロとして実装した。折り線は先端から出る斜め2本(縁と中心線の角の二等分線。これで両側の縁が中心線に重なる)と、中心線に直交するちょうつがい1本。羽は斜め線→ちょうつがいの鏡映2回、中央のくさびはちょうつがいの鏡映で動く
- 羽の外側の縁でつながっている隣の層も一緒に中心線へ寄せる。その折り目は花弁折りで**開く**(角0°で手順に記録される)ので、これをしないと紙が裂ける
- 予備基本形の前面を花弁折りすると、紙が裂けず先端が閉じた角を越えて持ち上がり、鶴の基本形の細い先ができることをテストで確認した(M2受け入れ条件)
- 汎用の折り操作の「面が領域に掛かるか」の判定を、頂点だけを見る方式から面を領域で切り取った面積で見る方式に直した(花弁折りの羽のように頂点が全て領域の境界に乗る形を取りこぼしていた)
- テストはRust 169件→172件、画面142件(内容を1件更新)。全5検査合格
- 既知の問題: 左右の縁の長さが違うフラップではちょうつがいの位置を平均で決めるため、警告付きで紙が裂ける。沈め折り・ひだ寄せ・ねじり折りは引き続き未実装

## 2026-08-06 - Task 2-6b後半(一部) - 「開いてつぶす折り」を選ぶだけで折れるように追加

- 技法サブメニューを4種にし、「つぶす」(開いてつぶす折り)を足した。重なった層をクリックして選び、開く折り目(背)に重なる中心線をドラッグ、基準点でつぶす方向を指すと折れる
- 専用の仕組みは持たず、汎用の折り操作を1回呼ぶマクロとして実装した。背の一方の端を支点に、手前寄りの半分は新しい折り線(背と行き先の角の二等分線)で折り返し、奥の半分は層まるごと回す(鏡映2回=回転)。これで背の角が0°になり=開き、紙が左右に広がる
- つぶす方向が背の延長上にある退化ケース(2回半分に折った正方形を予備基本形の重なりへ組み替える動き)では、紙は1mmも動かず層順序と1組の折り目の山谷だけが変わる。実際の紙で「畳んだ正方形を開いて反対側から畳み直す」動きにあたる
- 層の数の偶奇やフラップの形は仮定していない。断るのは幾何的に決められない指定(中心線が点になっている・指定した層が無い)だけで、中心線に背が無いなどは警告して続行する
- テストはRust 165件→169件、画面141件→142件。全5検査合格
- 既知の問題: 奥の半分は層まるごと動くので、奥側も本体につながっているフラップ(4層の輪など)を大きく回すと紙が裂ける警告が出る。花弁折り・沈め折りは引き続き未実装

## 2026-08-06 - Task 2-6c(一部) - 畳んだ紙の層をずらして表示し重なりが見えるように変更

- 平らに畳んだ状態では全ての層が同じ高さに重なるため紙が1枚に見え、どの層を選んでいるのか画面から読み取れなかった。層ごとに微小な高さを付けて描き、重なりが目で数えられるようにした(表示だけの調整で作品データは変えない)
- ずらす間隔は紙の長辺の1%。層が多いときは重なり全体の厚みが長辺の3%を超えないよう間隔を詰めるので、何十枚重なっても不自然に分厚くならない
- 折り途中や立体的な形は形が歪むのでずらさない(全ての面が同じ平面に乗っているときだけ適用する)
- 高さの計算は画面から切り離した部品にしてテスト。画面テスト127件→141件、全5検査合格。実機でも2回折った紙の層が縁で数えられることを確認
- 既知の問題: 層を持ち上げた分、立体表示で紙をクリックした位置と畳み平面上の位置が最大で長辺の3%ずれる(点・輪郭への吸着でほぼ吸収される)

## 2026-08-05 - Task 2-6b前半 - どんな折り方でも表せる汎用の折り操作を追加

- 「実際の紙で折れるのにアプリで表現できない」状態を無くすため、畳んだ状態から畳んだ状態への**どんな紙の動きでも表せる1つの部品**を作った。動かす紙を「層+畳んだ面での領域+施す動き(鏡映の重ね方や回転)+重なりのどこへ入れるか」で指定する。領域の境目がそのまま新しい折り線になり、展開図へ自動で書き加わる
- これまで「折り線を1本足す」ことしかできなかったため作れなかった動きが作れるようになった: 折り目を開く(角度を0度に戻す)/ 紙がまったく動かず重なり順と山谷だけが変わる遷移 / 層ごとに反対向きへ回す(中割り折り・かぶせ折りの中身)/ 鏡映2回=回転を含む動き(つぶし折り・ねじり折りの中身)/ 開く動きと折る動きを1回にまとめる(花弁折りの中身)/ 領域の中だけ重なりを裏返す(沈め折りの中身)
- 山谷は最終的な重なり順から決め直すようにした。これで回転を含む動きでも展開図の山谷が正しくなり、折りたたみの決まり(山と谷の本数の差が2)を満たす
- 断るのは形が決まらない指定(線が点になっている・どちら側か決まらない・対象が無い)だけ。紙が裂ける選び方は警告して続行する。失敗したときは展開図を一切変更しない
- 既存の「畳んだ紙にまとめて折る操作」はこの部品への薄い委譲に置き換えた。既存のテストは1件も書き換えずに全て合格している
- テストはRust 156件→165件(洗い出した全ケース+重なり順の見た目検証+手順からの再生一致)。全5検査合格
- 既知の問題: 花弁折り・つぶし折りの「技法ボタン」と、画面から自由に層をつまんで動かす操作は後半の作業(ロードマップTask 2-6bの残り)

## 2026-08-05 - Task 2-6修正 - 中割り折り・かぶせ折りで先端の向きを紙のつながりから決めるよう修正

- 中割り折り・かぶせ折りは「重なった層をちょうど半分に割って手前/奥にする」と決め打ちしていた。層の数が奇数のときや、重なりの一部だけを選んだときに紙のつながりと食い違い、山谷と重なり順が矛盾した作品(展開図から折り直すと形が変わる)を黙って作っていた
- 先端をどちら向きに回すかを、紙のつながりから決めるようにした。折り線が横切る折り目でつながった2つの層は、先端どうしがつながったまま裏返るので必ず反対向きに回る。この関係で層を塗り分ける。奇数層のフラップ(段折りでできた3層など)も、重なりの一部だけを選んだ場合も、実際の紙の動きどおりに折れる
- 断るのは折り線がフラップを横切っていないなど「形が決まらない指定」だけにした。紙が裂ける選び方や、山谷と重なり順が食い違う折り上がりは、断らずに警告して続ける(実際に折れるものをアプリで表現できない状態を作らないため)
- かぶせ折りに「折り終わる直前の高さから本当の重なりを読む」検証を戻し、段折りに再生一致の検証を足した(この検証が使えないのは、フラップを開く動きを再生で表せない中割り折りだけ)
- 折り操作と技法で重複していた小さな道具(頂点座標表・辺と面の対応表・折り線の記録)を共有し、警告の選り分けも文言の一致頼みをやめた
- テストはRust 153件→156件(3層フラップの中割り折り・4層フラップ・一部だけ選んだときの警告)、画面127件。全5検査合格
- 既知の問題: 花弁折り・開いてつぶすは引き続き未実装(ロードマップのTask 2-6bとして引き継ぎ)

## 2026-08-05 - Task 2-6(一部) - 中割り折りなど基本の折り方を選ぶだけで折れる機能を追加

- 段折り・中割り折り・かぶせ折りを「選ぶだけ」で折れるようにした。ツールレールに「技法」を足し(8個目)、選ぶと下にサブメニューが出る。立体表示で紙をクリックするとその場所に重なっている層(フラップ)がまとまって選ばれ、そのままドラッグして折り線を引き、下のパネルで「適用」を押すと折れる
- 技法は専用の仕組みを持たず、既存の「畳んだ紙にまとめて折る操作」を2回組み合わせ、そのあと紙の重なり順を並べ替えて作っている。中割り折りは先端を層の間へ挟み込み、かぶせ折りは外側から包む(展開図の山谷はこの重なり順から決め直すので、中割り折りでは背の向きが入れ替わる)
- 生成した折り線は全て1つの手順にまとめて記録するので、展開図を直しても折り直せる。折れない形を指定したときは日本語で理由を返し、「手動の折り操作で代替してください」と案内する
- 内部の窓口(Tauriコマンド)は9個のまま(手順操作に技法の指定を足して実現)
- テストはRust 141件→153件、画面116件→127件。全5検査合格。実機でアプリが起動することを確認した
- 既知の問題: **花弁折りと開いてつぶす(つぶし折り)は未実装**。この2つは既存の折り目をいったん「開く」動きを含み、折り線を足すことしかできない今の部品では作れない。「畳んだ状態で折り目を開く」部品を足すまではサブメニューに出さず、手動の折り操作で作ることになる(ロードマップに残作業として記載)
- 既知の問題: 中割り折り・かぶせ折りは折り上がりの形と重なり順は正しいが、再生の途中では先端が紙の外側を大回りして見える(フラップを開く動きを表せないため)

## 2026-08-05 - Task 2-5修正 - 紙の重なり順が上下逆に記録される問題を修正

- 折るたびに記録される「紙の重なり順」が、画面に見える重なりと上下逆になることがあった。正方形を半分に折るいちばん普通の1手目でも起きていて、重なり順を使う表示・検査・書き出しと「いちばん上の1枚を折る」指定が、すべて逆の紙を指していた
- 原因は、記録側と表示側で紙の置き方の基準が違っていたこと。表示は常に「最初の面をその場に固定した姿勢」で描くのに対し、記録側は折った順に姿勢を積み上げていた。基準が裏返しの関係になると重なりの上下だけが食い違う(折るたびに入れ替わるので、手順の数によって合ったり合わなかったりしていた)。折り上がりの展開図に書く山谷は正しかったため、静止画では気づけなかった
- 折り操作の出力を、記録の前に表示と同じ基準へそろえるようにして解消した(裏返しの関係になるときは重なり順も裏返す)
- 引きかけの折り線が「元に戻す」や手順の選び直しをまたいで生き残り、別の形の上で引いた線が黙って適用される問題も直した。形が変わったら折り線は捨て、案内を出す
- 「動かす側」の選択肢が画面の左右と一致しないことがあったため、言葉で左右を言わず立体表示のハイライトで示す形に変えた。折り線の座標表示もmmにした
- テストはRust 135件→141件、画面110件→116件(折り終わる直前の高さから本当の重なりを読み取って突き合わせる検証を追加)。全5検査合格。実機で「1手目から重なりが正しいこと」を確認した
- 既知の問題: 表示の基準が最初の面なので、折るたびに立体の位置と向きが動いて見えること自体は残っている(重なり順は正しく追従する)

## 2026-08-05 - Task 2-5 - 3D画面に直接線を引いて折る操作を追加

- 立体表示の紙の上をドラッグして折り線を引き、まとめて折れるようにした(ツールレールに「折る」を追加)。線の端は紙の角や輪郭・折り目に吸着し、引いている間は折り線と「動く側」が黄色く光る
- 引き終わると下のパネルが折りの設定に切り替わる。向き(手前へ折る/向こうへ折る)、対象(全ての層/いちばん上の1枚)、どちら側を動かすかを選んで「折る」を押すと、展開図に折り線が自動で書き加わり、手順が1つ増え、立体が折れた形になる(「やめる」で破棄)
- 立体の状態は保存しない作りなので、折るときの「今の畳み方」は手順の再生結果から毎回導き直す。平らに畳めていない状態(折り途中の手順・再生中・角度スライダーで変形中)では折れないようにし、立体表示に「平らに畳んだ状態で使えます」と出す
- 展開図画面からも同じ操作ができるが、1度でも折った作品では展開図の位置と畳んだ紙の位置が食い違うため断り、3D画面へ案内する。手順の途中に折りを差し込むこともできない(最新の状態に戻してから折る)
- 内部の窓口(Tauriコマンド)は9個のまま(手順操作に折りの指定を足して実現)
- テストはRust 126件→135件、画面87件→110件。実機で座布団折り→観音折り(6手順)を立体表示の線引きだけで完成できることをスクリーンショットで確認した
- 既知の問題: 平らに畳んだ紙は全ての層が同じ高さに重なるため、立体表示がちらついて見える(層をずらして表示する機能は未実装)。折るたびに立体の位置がずれて見えることがある(位置は展開図の最初の面を基準に決まるため)

## 2026-08-05 - Task 2-4修正 - 手順の再生と表示の細かな不具合を修正

- 途中まで折って一時停止したあとに展開図を直すと、再生を押した瞬間に表示が一度巻き戻ってから折り直されていた。折り直した形に合わせて再生位置も「折り終わり」へ揃えるようにして解消した
- 折り線が見つからず飛ばされた手順の赤い印が、途中の手順を選ぶと消えてしまっていた。作品全体を通した結果と、途中までを見直したときの結果を別々に持つようにして、札の赤い印が消えないようにした
- 編集・元に戻す・やり直しでも再生を止めるようにした(止めないと折り直した形が次のコマですぐ上書きされ、一瞬跳ねて見える)
- いちばん端で「次へ」「前へ」を押し続けても、同じ形の作り直しを頼まないようにした
- 画面テストは83件→87件(赤い印の保持・一時停止中の編集・編集での再生停止・壊れた経過時間の扱い)。あわせて再生の間引きの検証を「ちょうど1件にまとまり、最後の形が送られる」まで厳しくした。全5検査合格(Rust 126件・画面87件)
- 既知の問題: 札の見た目そのもの(画面の描画)は自動テストで確かめられていない(画面テストの土台は未導入)

## 2026-08-05 - Task 2-4 - 折り手順の一覧表示と再生・コマ送りの画面を追加

- 立体表示の下に折り手順の並びを出した。各手順は「番号+折り方の日本語名」の小さな札で、クリックするとその手順まで折った形が立体表示に出る。「折る前」と「最新」の札もあり、手順が1つも無いときは一行の案内だけを出す。画面の常設の区切りは4つのままで、立体表示の区画を上下に分けて置いている
- 「◀前へ」「▶再生/⏸一時停止」「次へ▶」「⏮最初へ」を追加した。再生は今の手順から最後まで、1手順を0.32秒かけて少しずつ折る動きで見せる。画面の更新に合わせて計算を頼み、待ちは常に最新の1件だけにしているので、計算が重い作品でも行列が伸びない。最後の手順で自動的に止まる
- 折り線が見つからず飛ばされた手順は赤い札にし、マウスを乗せると理由が出る。手順を選ぶと下のパネルでその手順の折り方の種類を変えたり、覚え書きを書いたり、手順を削除したりできる
- 同じ注意書きが二重に出ていたのを直した(展開図の点検結果に自動再生の注意書きが合流しているため)。あわせて、最新の状態を見ているときは編集のたびに折り直しを頼み直さないようにした
- 画面テストは53件→83件(進み具合の計算13件・日本語名と注意書きの取り出し5件・手順の選択と再生12件)。全5検査合格(Rust 126件・画面83件)。実機で3手順の作品を開き、札の選択・再生の動き・飛ばした手順の赤表示・折り方の変更・手順の削除まで目視確認済み
- 既知の問題: 手順を選んでいる間は、下のパネルに線や点の操作が出ない(「最新」か「折る前」を選ぶと戻る)

## 2026-08-05 - Task 2-3修正 - 折り途中の手順を選んだときに違う形が表示される問題を修正

- 手順の途中(例: 3手順のうち1手順目まで)を表示すると、まだ折っていないはずの折り線まで曲がった形が、何の知らせもなく出ていた。まだ折っていない折り線に「角度0(平ら)」を明示していなかったため、計算が別の折り方へ落ち着いてしまうのが原因。全ての未実施の折り線を平らに固定するようにして解消した
- あわせて計算を1回にまとめた(全ての折り線の角度が決まるので、手順ごとに解き直す必要が無くなった)。10手順・400面の全再生は約23ミリ秒(目標3秒)
- 重なり順の代表の点が1つも今の面に当たらない手順は、面の並び順に戻さず直前の重なり順を保つようにした
- 展開図の編集後の自動再生を、内部データの鍵を握ったまま行わないよう、鍵を放してから行う形に変更した(角度スライダーの操作が編集のたびに待たされるのを防ぐ)。面の割り出しの二重実行も解消
- テストはRust 123件→126件(途中手順の外形検証・途中0%と1つ前の完了が一致・重なり順の解決失敗)。全5検査合格(Rust 126件・画面53件)
- 既知の問題: 一部の層だけを折る手順は、展開図からの折り直しでは正確に再現できない(平らな1枚の紙としては折り線の端が宙に浮くため)。再生すると一番近い形と日本語の警告が出る

## 2026-08-05 - Task 2-3 - 折り手順の記録と再生(展開図を直したら自動で折り直す)を追加

- 折り手順の再生を追加した。立体の状態は一切保存せず、平らな展開図と手順の折り線指定だけから毎回作り直す。手順の何番目まで・その途中どのくらい(0〜1)を指定でき、コマ送りやアニメーションにそのまま使える
- 折り線は展開図上の線分で持っているので、あとから線を足して分割されても再生できる。手順に無関係な補助線を足しても畳み上がりは変わらない。手順の折り線が消されていた場合はその手順だけ飛ばして日本語の警告を出し、残りの手順は続ける。一部だけ残っている場合は残った分で折って警告する。計算が収束しないときも止めずに一番近い形を返す
- 展開図を編集したり手順を変えたりすると、その場で最新の手順まで再生し直して結果(立体・飛ばした手順・警告)を画面へ返すようにした。手順が空のときは再生するものが無いので立体は返さない
- 同じ入力から2回再生すると結果が完全に一致することを確認済み。10手順・400面の全再生は開発機で約36ミリ秒(目標3秒に対し80倍以上の余裕)
- テストはRust 113件→123件(再生9件・自動再生1件)。全5検査合格(Rust 123件・画面53件)
- 既知の問題: 折っている途中(途中の割合を指定した状態)の重なり順は、その手順を折り終えるまで前の手順のものを使う

## 2026-08-05 - Task 2-2修正2 - 折り線が既存の逆向きの折り目と重なるときに知らせるよう修正

- 折り線の一部に反対向きの折り目(山のところへ谷を引くなど)が既にある場合、何も知らせずに折れてしまっていた。折り上がりの形は同じでも、折っている途中の形が求まらず手順の再生アニメーションが崩れるため、日本語の警告を出すようにした
- 原因は、警告を出す処理が「線が面を横切る場合」の側にしか無かったこと。折り線と同じ直線上にある既存の折り目は必ず面の境目になるため、そちらの分岐には決して届いていなかった
- あわせて、頂点の位置表を1区間ごとに作り直していたのを、線を書き加えた直後だけ作り直すように改めた
- 再現ケース(切り込み状の山がある紙を谷で折る)の回帰テストを1件追加。全5検査合格(Rust 113件・画面53件)
- 既知の問題: 反対向きの折り目と重なった区間は既存の線種のままで、自動では直さない(警告のみ)

## 2026-08-05 - Task 2-2修正 - 補助線の上で折れない・手順が再生できなくなる2つの欠陥を修正

- 補助線と重なる位置で折ると、重なった部分だけ線が入らず面が分かれないまま「成功」を返し、紙全体が裏返る状態になっていた。重なった補助線を折り線(山/谷)に変えるようにして解消した。念のため「線が横切ったのに面が分かれていない」場合は展開図を変えずに中止する安全弁も入れた
- 折り手順に残す角度の指定が線の番号だったため、あとから別の折りを加えて線が分割されると手順を再生できなくなっていた。指定を「展開図の上の線分+角度」で持つ方式に変え、再生時にその線分に乗る全ての断片を探して駆動するようにした(重なり順を代表の点で保存しているのと同じ考え方)
- あわせて、既存の折り線と重なる区間にも角度の指定を作るようにし(線種は既存のまま)、同じ警告が何度も出る問題と、頂点探索の無駄な繰り返しも直した
- テストは9件→10件(補助線との重なりの検証を配置・重なり順まで拡張し、線が分割された後も手順どおりに折れて畳んだ位置が一致することを確かめる新規1件を追加)。全5検査合格(Rust 112件・画面53件)
- 既知の問題: 折り線と重なった補助線は元に戻せない(折り線に変わる)。既存の折り線と完全に一致する再折りは引き続き未対応

## 2026-08-05 - Task 2-2 - 畳んだ紙に線を引いてまとめて折る操作を追加

- 畳んだ状態の上に折り線を引き、線の可動側に乗る層(または指定した層だけ)をまとめて折る操作を追加した。折り線は各層の置き方に合わせて展開図へ引き戻して書き込み、裏返っている層では山と谷を自動で反転する
- 折った後の各面の置き場所と重なり順(動いた面は順序を反転して山全体の一番上または一番下へ)を作り直し、手順の記録(折り線ごとの±180度の角度指定と重なり順の代表点)まで一度に生成する。途中で失敗した場合は展開図に一切手を付けない
- 折れない指定(対象の層が無い・線がどの層も横切らない)は理由を日本語で返して失敗し、危うい指定(既存の線と重なる・紙が裂ける形)は警告を付けて続行する
- テスト9件を追加(半分折り・2層への重ね折りで山谷が逆・段折り・上1枚だけ折る・失敗時の無変更・重なり順の往復一致と決定性・裂け警告・重なり警告)。全5検査合格(Rust 111件・画面53件)
- 既知の問題: 一部の層だけ折ったときの重なり順は「動いた面をまとめて一番上(または下)へ」の近似で、物理的な挟み込みと違うことがある。既存の折り線と完全に一致する再折り(新しい線を1本も引かない折り)は未対応でエラーになる

## 2026-08-05 - Task 2-1 - 平らに畳んだときの紙の重なり順を管理する機能を追加

- 平らに畳んだ状態を「面ごとの置き場所(移動・回転・裏返し)」と「下から上への重なり順」の組として持つ仕組みを追加した。初期状態は全ての面が動いておらず、重なり順は面の並び順
- 重なり順は面の番号では保存できない(面の番号は展開図を編集するたび振り直される)ため、面ごとに決めた「面の中の代表の点」の並びとして保存し、読み込み時に今の面へ結び付け直す方式にした
- 代表の点は面を三角形に切り分けたときの最初の三角形の中心を使う。スリット(切り込み)で凹んだ面でも必ず面の中に入り、同じ面からは必ず同じ点が出る。点が面の中にあるかの判定は凹んだ面と切り込みに対応し、境界線の上はごくわずかな誤差まで内側として扱う
- 展開図を編集して面が分かれても重なり順の意味が保たれることを確認した。どの面にも入らない点は日本語の警告を返してその層を飛ばし、結び付かなかった面は元の並び順のまま最後に足すので、全ての面が必ず1回ずつ重なり順に入る
- テスト12件を追加(半分に折った状態の鏡写し・重なり順の往復・面が分かれた後の復元・境界と切り込みの判定・枝分かれした切り込みなど)。全5検査合格(Rust 102件・画面53件)
- 既知の問題: 代表の点が後から引いた折り線の真上に乗ると、分割後のどちらの面として結び付くかが決まらない(面の番号が若い側になる)。全ての面が1回ずつ重なり順に入る決まりは保たれる

## 2026-08-05 - Task 2-0後半 - 3D表示を手順再生に耐える作りに改良

- 立体表示の作りを「形の骨組み」と「頂点の位置」に分けた。折り線や面が変わったときだけ骨組み(三角形への分け方・線のつなぎ方・折り線との対応・三角形がどの面かの対応表)を作り直し、折り具合が変わったときは頂点の位置を書き換えるだけにした。毎回の作り直しが無くなり、手順再生の連続更新に耐える
- 面の三角形への分け方を耳切り法に変更し、スリットでできる凹んだ面も正しく塗れるようにした(以前の扇形分割は凹み部分がはみ出していた)。表と裏は1つの形を2通りに塗る方式へまとめ、描画の指示回数を減らした
- 折り具合の計算依頼を「待ちは最新の1件だけ」に変更。計算中に何度指示が来ても、次に送るのは最新の1件だけになり、行列が伸びて表示が遅れることがなくなった。ファイルの編集や元に戻すは今までどおり全件を順番に処理する
- 使い勝手の細かい改善: 数値の入力欄は書き換えていなければ離れても角度指定にならず、Escで書きかけを取り消せる/ツールレールの「全体」で展開図と立体表示の両方が元の見え方に戻る/重なった折り線のクリックで手前が確実に選ばれる/画面の細かさの変化と描画資源の復帰に追従/描き直しは1コマ1回にまとめる
- 画面テストは差し引き17件増(資源の後始末3件・骨組みと形の更新9件・待ちは最新1件5件ほか)。テストの並び順に左右される問題も解消。全5検査合格(Rust 90件・画面53件)、アプリを起動して表裏の塗り分け・視点回転・「全体」での戻りを目視確認済み
- 既知の問題: なし

## 2026-08-05 - Task 2-0前半 - 折りの計算を大きな作品でも間に合う速さに改良

- つじつま合わせ計算を疎な構造に全面改修: 拘束の輪を「木の経路」から「最短の輪」に取り替え、微分を差分ではなく数式で厳密に計算し、連立方程式は帯行列の直接法で解くようにした。20×20ミウラ折り(面400・辺840)のスライダー操作相当で、1回あたり約27〜37秒→約3〜6ms(release実測、目標33msの5倍以上の余裕)。回帰テスト tests/perf_miura.rs で常時監視
- 数値面の改善: 収束判定を残差本数に依存しないRMS基準へ変更 / ±180度またぎで山谷の符号が反転して見える問題を前回値に近い側を選ぶ方式で解消(どちらも回帰テストあり)
- 軽微4件: 山谷参照のマップ化 / 木構築の二重実行除去 / pose_solveの面抽出をstoreのキャッシュ流用に / 角度指定を外した折り線が前回の形のまま残る仕様をdocに明文化
- テストはRust 90件+画面36件に増え(新規6件: 性能ベンチ・解析微分の検証・符号反転防止・自由ヒンジ仕様・facesキャッシュ等)、check.ps1 全5検査合格。既知の問題: なし(フロント側のTask 2-0後半は未着手)

## 2026-08-05 - M1完了 - 展開図エディタと剛体折りソルバーの節目を達成

- 受け入れ: やっこさん(座布団折り2回相当)の展開図で折り計算が収束し、畳んだ位置が理論値と一致することを自動テストで確認(外形は1/2角の正方形、4隅と辺中点は中心の1点に重なる。2回計算しても結果は完全に同一)
- check.ps1 全5検査合格(Rustテスト84件・画面テスト36件)
- 行数: 概算でRust 約2,000行(上限50,000)/ TS 約2,200行(上限20,000)※テスト除く、計測方法により多少前後。裏側の窓口(Tauriコマンド)8個(上限13個以内)
- 既知の問題: 大きな作品での計算速度(面400で33ms目標に未達)と3D表示の内部構造はM2先頭のTask 2-0で改修予定。アプリ実機でのGUI目視確認は未実施(ユーザー確認待ち)。受け入れ根拠の強化(山谷割り当ての検証)はM2の局所平坦判定(Task 2-7)実装後に追加する

## 2026-08-05 - Task 1-10 - やっこさんが折れることを確認する自動テストを追加

- やっこさんの展開図を、画面の線描画と同じ操作列(insert_segmentの呼び出し列、吸着なしの生座標)で構築し、全折り線±180度で平坦に畳まれることを検証する受け入れテスト5件を追加(座布団折り1回・2回・決定性・描く順序の入れ替え・畳めない展開図の検出)
- 設計上の発見: 2回目の座布団折りは「折った状態の線(内側の正方形)」をそのまま展開図に重ねるだけでは平坦に畳めない(川崎の定理違反。負のテストで確認)。1回目で裏返る隅の層まで線が届くため、展開図では1/4位置の縦横4直線になり、内側の区間は谷・外側の区間は山と反転する
- 画面操作上の注意: 2回目の折り線は1本の直線でも山/谷の区間ごとに分けて描く必要がある。必要な点は全て既定の8分割方眼に乗るため吸着で正確に描ける(GUIでの目視確認は統括側で実施)
- 既知の問題: なし

- 角度を指定した折り線を補助線などに変えると、指定が全て無くなったあとも立体が折れたまま残っていた(前回の計算結果を引き継いでいたため)。しかも指定が無い状態では「全て平らに戻す」も「解除」も押せず、平らに戻す手立てが画面から消えていた
- 指定が全て無くなったときは、全ての折り線に平ら(0度)を明示して計算し直すようにした(「全て平らに戻す」と同じ考え方に統一)
- この経路の再現テストを1件追加。全5検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-9修正 - 角度の入力と解除の使い勝手を改善

- 数値入力欄でマイナスの角度が打てなかった問題を修正(入力途中の「−」や空欄が打ち消されていた)。入力中は書き換えず、Enterか入力欄から離れたときに確定する方式にした
- 「この折り線の角度を解除」で、その折り線が直前の折れた形のまま残っていた問題を修正。解除時に一度だけ平ら(0度)を明示して送り、前回の計算結果を持ち越さないようにした(「全て平らに戻す」と同じ考え方)
- つじつま計算が合わなかったときの右上の知らせを、警告の有無に関係なく必ず出すようにした
- テスト6件追加(間引きの合流と間隔を空けた場合・1本解除・立体上の折り線の拾い上げ4件)。使われていない見た目の設定も削除
- 全5検査合格、アプリの起動確認済み。既知の問題: なし

## 2026-08-05 - Task 1-9 - 立体表示の画面と、折り線ごとの角度操作を追加

- 画面右側に立体表示を実装。展開図の面を紙の表(既定は赤)と裏(白)で塗り分け、境界線を黒で描く。ドラッグで回転、ホイールで拡大縮小、中ボタンで移動できる。新規作成・ファイルを開いた直後は紙全体が見える斜め上の位置に戻る
- 立体表示の折り線をクリックすると選択でき(選択中は黄色の太線)、展開図側の選択とも連動する。下のパネルにスライダーと数値入力が出て、−180〜+180度で折り具合を指定できる(プラス=山折り、マイナス=谷折り)
- スライダーを動かしている間は60ミリ秒ごとに間引いて計算を依頼し、離した位置の角度は必ず反映する。「この折り線の角度を解除」「全て平らに戻す」も追加。展開図を編集すると、残っている角度指定で立体形状を自動で計算し直す
- 計算のつじつまが合わなかった場合は右上に「⚠ 追従計算が収束していません」と表示し、操作は止めない
- 画面側テスト5件追加(間引き・全解除・編集後の追従・不収束の反映・折り線判定)。全5検査合格、アプリの起動確認済み
- 既知の問題: 角度指定を1本だけ解除しても、その折り線は直前の形のまま残る(前回の計算結果を引き継ぐ仕様のため)。平らに戻したいときは「全て平らに戻す」を使う。凹んだ形の面は塗りつぶしが乱れる場合がある(Task 2-0以降で対応)

## 2026-08-05 - Task 1-8修正 - 触っていない折り線が勝手に折れて表示される問題を修正

- つじつま合わせの計算で、輪の中にある折り線かどうかの判定が大まかすぎた(面のかたまり単位)。輪に乗っていないぶら下がりの折り線にも計算開始用の仮の角度が付き、拘束がないためその仮の角度がそのまま答えとして出てしまっていた
- 判定を折り線1本単位に直した(輪を作る線と、その輪を構成する経路上の線だけに仮の角度を付け、それ以外は必ず0度から)。再現ケース(輪+ぶら下がり線の混在)を回帰テストとして追加
- あわせて軽微な修正: 画面側コメントの窓口数を7→8に訂正、角度の折り返し計算を繰り返し引き算から剰余計算に変更(極端な値でも止まらない)
- 全5検査合格。既知の問題: なし

## 2026-08-05 - Task 1-8 - 複雑な展開図でも折り角度のつじつまを自動で合わせる計算を追加

- 展開図の内側で折り線が集まる点があると、全部の折り角度を自由には決められない(紙が裂けたり重なったりしないよう、つじつまが必要)。指定した角度を固定し、残りの角度を数値計算で自動的に求める仕組みを作った
- つじつまが取れない指定(両立しない角度2つなど)でも落ちずに、途中で見つかった一番よい形と警告を返す。前回の答えを次の計算の出発点に使い、スライダー連続操作でも安定して速く収束する
- 裏側の窓口に8個目の pose_solve を追加。混雑を避けるため、重い計算は共有データの占有(ロック)を外して行う決まりを実装。画面側にも呼び出し口と型を追加(画面への接続はTask 1-9)
- テスト6件(90度指定で閉じる/±180度で平ら/矛盾指定/前回解の再利用/ループなし/無効な辺の指定)+保管まわり1件。全5検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-7 - 折り線の角度から紙の立体的な形を計算する機能を追加

- 展開図の面どうしのつながりをたどり、指定した折り角度で各面の空間内の位置と向きを計算する機能を作った(基準の面を平らに固定し、隣の面へ順に姿勢を伝える方式)
- 折る向きの約束を決めてコードの説明に図解付きで固定した: プラスの角度=山折り(紙の表から見て奥へ倒す)、マイナス=谷折り(手前へ起こす)
- テスト5件: 180度で2面がぴったり重なる/90度で面どうしが直角/±90度で倒れる向きが逆/角度未指定は平らのまま/つながっていない面は警告付きでそのまま表示
- 既知の問題: なし

## 2026-08-05 - Task 1-5/1-6再修正 - 操作が連続したときに画面へ反映されない線が出る問題を修正

- 前回の「古い応答は捨てる」を見直し、成功した結果は順番どおり全て画面へ反映するようにした(捨てると後の操作が失敗したときに画面と内部データが食い違うため)。捨てるのは古い失敗の報告だけに限定
- ドラッグが途中で中断された場合(ポインタ捕捉の解除)に、選択の四角などの途中状態を片付けるようにした
- 「成功した結果は捨てられない」ことを固定するテスト4件を追加。全5検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-5/1-6修正 - 連続操作で画面と内部データがずれる問題を予防

- 裏側への要求を1件ずつ順番に送る仕組みを導入。素早い連続操作(元に戻す連打など)でも適用順が入れ替わらず、古い応答が新しい画面を上書きしない(最新でない応答は捨てる)。テスト6件付き
- ドラッグ中に画面の外でボタンを離すと、選択の四角が残って付いてくる問題を修正(ポインタ捕捉方式に変更)
- 線を引いている途中でツールを切り替えると、古い始点から別種類の線が引けてしまう問題を修正(切替時に途中状態を破棄)
- 新規作成やファイルを開いた直後は、紙全体が見える表示に戻すようにした
- 全5検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-6 - 展開図を描く画面(方眼・吸着・線の描画)を追加

- 展開図を描く画面を実装。白い紙・方眼(既定8分割)・輪郭(黒)・山折り(赤)・谷折り(青)・補助線(灰の点線)を色分けして表示し、高精細画面にも対応した
- カーソルの吸着を実装(近い順ではなく「点 > 方眼の交点 > 線の上」の優先順)。吸着計算は画面側の純関数にし、テスト8件で検証(優先順・縦横比の反映・範囲外・壊れたデータ耐性)
- マウス操作を実装: 2回クリックで線を引く(引いている途中は点線で予告、Escで中止)、クリックや四角いドラッグで選択、削除ツールとDeleteキーで線を消す、ホイールで拡大縮小、中ボタンで表示移動、「全体」ボタンで紙全体表示
- 検査の仕組みにテスト実行(vitest)を5番目として追加(check.ps1と実装ロードマップ§0.2)
- 全検査合格(Rustテスト・静的検査・画面ビルド・文法検査・画面テスト)。アプリの起動確認済み
- 既知の問題: なし

## 2026-08-05 - Task 1-5 - 画面の基本レイアウト(4区画)と画面側の土台を追加

- 画面を4区画(上部ツールバー / 左端ツールレール / 中央の展開図・立体表示 / 下部の状況パネル)に分けた土台を作った。立体表示と展開図の中身は後続タスクで実装する
- 画面側の状態を1か所で管理する仕組みを導入。作品データ・面の一覧・警告・選択中の線や点・使用中のツールをまとめて持ち、裏側(Rust)との窓口7個への呼び出しもここに集約した
- 新規・開く・保存・元に戻す・やり直しのボタンを接続。開く/保存はOS標準のファイル選択画面(.ori3形式)を使う
- 警告やエラーは右上の小さなバッジと下部パネルに表示(常設の区画は増やさない)。起動時は150×150mmの新しい紙が開く
- 全検査合格(Rustテスト・静的検査・画面ビルド・文法検査)
- 既知の問題: なし

## 2026-08-05 - Task 1-4修正 - 壊れたデータが原因でファイルが開けなくなる問題を予防

- 編集の確定処理の順番を見直し、面の検出や検査が途中で失敗した場合は保管庫の中身を一切変えないようにした(壊れたデータが確定→保存→二度と開けない、という連鎖を断つ)
- 存在しない点を参照する壊れた線が混ざっていても、面の検出・線の挿入が止まらず、警告として知らせるだけにした
- 全コマンドを別スレッド実行に変更(重い検査で画面が引っかからないようにする)
- 点の移動で指定した点が存在しない場合、黙って成功せず警告を返すようにした。エラーと警告の使い分け規則も説明書きに明文化
- テスト6件追加(壊れたデータ流し込み後も編集・保存・読み込みが続けられる、読めないファイルで分かる言葉のエラー、ほか)。全テスト・静的検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-4 - 作品データの保管と編集・元に戻す・やり直しを追加

- 作品データを1か所で預かる保管庫を実装。線の追加・削除・種類変更・点の移動・紙サイズ変更・手順の並び替えなどの編集を受け付け、画面側へは常に「作品全体+面の一覧+警告」を返す
- 元に戻す(最大100回まで、古いものから順に忘れる)とやり直しを実装。実際に中身が変わった編集だけを履歴に積む
- ファイルへの保存と読み込みを実装。新しい版のアプリで作られたファイルは分かる言葉で断る。輪郭線の削除・種類変更は行わず警告で知らせる
- 画面との窓口(コマンド)7個を登録。途中で計算が壊れてもアプリごと落ちないよう、全コマンドを保護の仕組みで包んだ
- テスト先行(14件): 追加→戻す→やり直しの一致・100回制限・保存→読み込みの一致などを確認。全テスト・静的検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-3追加修正 - 展開図検査の見逃しを3件修正

- 長さがほぼゼロに潰れた線と、存在しない点を参照する線を、それぞれ警告として検出するようにした(従来は黙って見逃していた)
- 繋がり具合の検査を面の検出と同じ基準に揃えた。潰れた線1本で繋がって見えていた浮きループを、正しく「外周と繋がっていない」と警告できるようになった
- テスト3件追加(点の移動で橋の線を潰したケース・潰れた線・参照切れの線)。全テスト・静的検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-3追加 - 展開図の壊れた状態を検出する検査機能を追加

- 展開図の検査機能を追加。外周と繋がらない浮いた線・閉ループ、点の移動などで生じた線同士の交差・重なりを見つけ、日本語の警告文として返す(直しはせず知らせるだけ。後でUIの警告表示に接続する)
- 面の検出の説明に既知の制限を明記: 外周と繋がらない閉ループは「穴」として扱われず、領域が重複した面が返る(上記の検査で検出できる)
- テスト4件追加(浮いた閉ループの警告・正常な米字で警告なし・移動後の交差警告・切り込み線の面境界の辿り方)。全テスト・静的検査合格
- 既知の問題: なし

## 2026-08-05 - Task 1-3 - 展開図の線の管理と面の検出を追加

- 展開図への線の挿入を実装。既存の線との交点で双方を自動分割し、近く(誤差以内)の点や線には吸着する。重なり判定を交点判定より優先し、既に引いてある線に重ねても二重登録や線種の上書きが起きない
- 線の削除(どこからも使われなくなった点の掃除つき)と点の移動(座標のみ更新)を実装
- 線で囲まれた領域(面)の検出を実装。紙の外側は除外、補助線は面の境界に使わない。同じ展開図からは常に同じ順序・番号で面が得られる
- テスト先行(15件): 対角線・交差・重複挿入・米字で面8つ・浮いた線・決定性などを確認。静的検査も警告ゼロ
- 既知の問題: なし

## 2026-08-05 - Task 1-2修正 - 品質レビュー指摘の修正

- 短い線分同士の交差が「平行」と誤判定される問題を修正(判定を線分の長さに応じた相対基準に変更)
- 同一直線上で重なる2線分の重なり区間を求める関数を追加(次タスクの線分挿入で使用)
- 回転角の表し方の揺れ(360度回って同じ向き)で同一の変換が不一致になる罠を解消し、意味的な比較手段を追加
- 紙サイズが0以下のときに明確なエラーで止まる防御、乱数テストの固定シード化もあわせて対応
- 既知の問題: なし

## 2026-08-05 - Task 1-2 - 幾何計算の基本部品を追加

- 線分同士の交点(端点接触も交差扱い、平行はなし)、点と線分の距離・線上判定、直線に対する点の折り返しを実装
- 回転・平行移動・鏡映をまとめて扱える変換(合成・逆変換つき)を実装。折りの展開計算の土台になる
- 交差の各パターンとランダムな点100個での合成・逆変換の一致をテストで確認(テスト先行)。静的検査も警告ゼロ
- 既知の問題: なし

## 2026-08-05 - Task 1-1 - 作品データの保存形式を定義

- 紙・展開図・折り手順・表示設定をひとまとめにした作品データの型と、編集操作・手順操作・3D表示用の型を定義
- 新規作品を作ると、紙の輪郭4辺入りの展開図が長辺=1.0の正規化座標で用意されるようにした
- JSON保存→読み込みで内容が完全に一致することをテストで確認(先にテストを書いて失敗を確認してから実装)
- 既知の問題: なし

## 2026-08-05 - M0完了 - 基盤のレビューと修正が完了

- 仕様適合レビュー(合格)と品質レビューを実施。検査スクリプトに「検査道具が起動できなかった場合に合格扱いになる抜け道」が見つかり、3経路すべて塞いだ(コミット 1b135c5, db3e826)
- あわせて画面側の文法検査の強化、型定義部品の置き場所の整理、説明書きの更新も実施
- M0完了時チェック: 全自動検査合格。既知の問題: Windowsのアプリ制御機能がビルド中の中間ファイルを時々ブロックする(再試行で回避可能、根本対応はユーザー判断待ち)

## 2026-08-05 - Task 0-3 - 全ての自動チェックを一度に実行できる仕組みを追加

- 4つの検査(計算部分のテスト・静的検査、画面部分のビルド・文法検査)を順に実行し、失敗があれば非0で終了するスクリプトを作成
- 画面部分の文法検査の仕組みを導入し、`npm run lint` で実行できるようにした
- 手動実行で全検査の合格を確認済み
- 既知の問題: アプリ制御機能のブロック(Task 0-2の項参照)が出た場合は、スクリプトを2〜3回再実行すると通る

## 2026-08-05 - Task 0-2 - アプリの画面が起動する最小の土台を作成

- デスクトップアプリの雛形を作成し、画面に「ORIGAMI3」とだけ表示する最小構成にした(ウィンドウ題名もORIGAMI3)
- 3D表示・状態管理用の外部部品を導入し、アプリ側の計算部分を全体のビルド管理に組み込んだ
- ビルド成功と、できあがったアプリ本体が起動することを確認済み(画面の目視確認は統括側で実施予定)
- 既知の問題: Windowsのアプリ制御機能が、ビルド途中で作られる実行ファイルを時々ブロックする。ブロックされた部品を作り直すと通る(検査スクリプトを2回実行すれば概ね解消)

## 2026-08-05 - Task 0-1 - 計算部品のフォルダ構成と空の部品一式を作成

- 計算コアを置く7つの部品(データ型・幾何計算・展開図・剛体折り・層管理・自動提案・書き出し)の空の入れ物を作成
- 共通で使う外部部品の版数を1か所で管理する仕組みを導入(最新安定版で固定)
- 自動テストと静的検査(警告ゼロ扱い)が通ることを確認
- 既知の問題: なし。画像書き出し用の外部部品は使う段階(M4)まで追加しない方針

## 2026-08-05 - 環境 - Rustビルド環境の修復

- cargoがWindowsのSmart App Control(アプリ制御機能)にブロックされて実行できない問題が発生
- 原因: 以前から入っていたRustツールチェーンの実行ファイルが、この機能の評判データを持っていなかった
- 対処: stableツールチェーンを削除して再ダウンロードしたところ解消(cargo 1.97.1で動作確認済み)
- 既知の問題: なし。今後ツールチェーン更新後に同じ症状が出たら同じ手順で直せる
