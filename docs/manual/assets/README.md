# 取扱説明書の画面写真

アプリ内ヘルプの `figure` ブロックへ `image: "ファイル名.png"` を指定すると、
同じ図解の直後に、このディレクトリのPNGが「画面例」として取扱説明書へ入る。

- サブディレクトリや `..` は使わず、PNGのファイル名だけを指定する。
- 画像がまだ無い場合は警告を出し、既存のSVG図解だけでPDF生成を続ける。
- 画像は縦横比を保ち、A4の本文枠へ収まる大きさで配置される。
