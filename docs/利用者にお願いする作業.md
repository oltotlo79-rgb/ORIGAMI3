# 利用者にお願いする作業

この文書で行うのは二つだけです。開発用の部品を1つ更新することと、公開後に4ファイルが同じ内容か確認することです。
部品の問題を調べる作業、更新のお知らせの記録、Node 22の版と40文字の識別子の確認は、GitHubの自動検査が行います。
各番号は一行ずつ実行します。赤い文字が出たら、その番号で止め、同じ行を繰り返しません。

## 1. 開発用の部品を1つだけ更新する（5〜10分）

開発のときだけ使う部品に問題が見つかったため、`nanoid`を3.3.18へ上げます。配布するアプリには入っておらず、利用者が使うアプリへの影響はありません。

この作業には通信が必要です。この作業機で動くかは未確認です。`npm audit fix`は、ほかの部品の版まで変える可能性があるため使いません。

1. 画面左側が `PS C:\Users\oltot\Documents\git-projects\ORIGAMI3\apps\desktop>` で終わっていることを確認します。違う場合は、次を貼り付け、Enterキーを押します。

   ```powershell
   Set-Location -LiteralPath "C:\Users\oltot\Documents\git-projects\ORIGAMI3\apps\desktop"
   ```

   **こうなっていればOKです**: 行の左側が `PS C:\Users\oltot\Documents\git-projects\ORIGAMI3\apps\desktop>` で終わります。

2. 次を貼り付け、Enterキーを押します。

   ```powershell
   $packageJsonHashBeforeNanoidUpdate = (Get-FileHash -LiteralPath 'package.json' -Algorithm SHA256).Hash; 'package.jsonの更新前の値を記録しました'
   ```

   **こうなっていればOKです**: `package.jsonの更新前の値を記録しました`と表示されます。

3. 次を貼り付け、Enterキーを押します。

   ```powershell
   $nanoidRecordDirectory = Join-Path $PWD '..\..\verification\improvement-roadmap\10-supply-chain'; '記録先を決めました'
   ```

   **こうなっていればOKです**: `記録先を決めました`と表示されます。

4. 次を貼り付け、Enterキーを押します。

   ```powershell
   New-Item -ItemType Directory -Force -Path $nanoidRecordDirectory | Out-Null; '記録先を用意しました'
   ```

   **こうなっていればOKです**: `記録先を用意しました`と表示されます。

5. 次を貼り付け、Enterキーを押します。

   ```powershell
   $nanoidLockBackup = Join-Path $nanoidRecordDirectory 'package-lock-before-nanoid-3.3.18.json'; 'バックアップの名前を決めました'
   ```

   **こうなっていればOKです**: `バックアップの名前を決めました`と表示されます。

6. 次を貼り付け、Enterキーを押します。

   ```powershell
   Copy-Item -LiteralPath 'package-lock.json' -Destination $nanoidLockBackup -Force; if (Test-Path -LiteralPath $nanoidLockBackup) { '更新前の記録を保存しました' } else { '更新前の記録を保存できませんでした。担当者へ知らせてください' }
   ```

   **こうなっていればOKです**: `package-lock-before-nanoid-3.3.18.json`が記録先にできます。

7. 次を貼り付け、Enterキーを押します。

   ```powershell
    npm.cmd update nanoid --package-lock-only --ignore-scripts --no-audit
   ```

   **こうなっていればOKです**: 最後が赤い文字ではなく、更新が終わった表示になります。`npm install nanoid@3.3.18`と`npm audit fix`は使いません。

8. 次を貼り付け、Enterキーを押します。

    ```powershell
    $nanoidLockLines = @(Get-Content -LiteralPath 'package-lock.json'); '更新後の記録を読み取りました'
    ```

    **こうなっていればOKです**: `更新後の記録を読み取りました`と表示されます。

9. 次を貼り付け、Enterキーを押します。

    ```powershell
    $nanoidStartIndex = [Array]::IndexOf([string[]]$nanoidLockLines, '    "node_modules/nanoid": {'); if ($nanoidStartIndex -ge 0) { 'nanoidの記録を見つけました' } else { 'nanoidの記録を見つけられませんでした。担当者へ知らせてください' }
    ```

    **こうなっていればOKです**: `nanoidの記録を見つけました`と表示されます。

10. 次を貼り付け、Enterキーを押します。

     ```powershell
     if ($nanoidStartIndex -ge 0 -and ($nanoidStartIndex + 4) -lt $nanoidLockLines.Count -and $nanoidLockLines[$nanoidStartIndex + 1] -eq '      "version": "3.3.18",' -and $nanoidLockLines[$nanoidStartIndex + 2] -eq '      "resolved": "https://registry.npmjs.org/nanoid/-/nanoid-3.3.18.tgz",' -and $nanoidLockLines[$nanoidStartIndex + 3] -eq '      "integrity": "sha512-DTg4MJbGMWkfi6VZFdNt2/caMbQy4Ou+Op/hJQvGEWcnVfoA1QA+xzRKAzw9jD6+GVOOeYr/mIcuDSdug6F6+w==",' -and $nanoidLockLines[$nanoidStartIndex + 4] -eq '      "dev": true,') { 'nanoid 3.3.18、照合値、開発用だけであることを確認しました' } else { 'nanoidの版、取得先、照合値、または開発用の印が一致しませんでした。担当者へ知らせてください' }
     ```

    **こうなっていればOKです**: `nanoid 3.3.18、照合値、開発用だけであることを確認しました`と表示されます。

11. 次を貼り付け、Enterキーを押します。

    ```powershell
    if ((Get-FileHash -LiteralPath 'package.json' -Algorithm SHA256).Hash -eq $packageJsonHashBeforeNanoidUpdate) { 'package.jsonは変わっていません' } else { 'package.jsonが変わりました。担当者へ知らせてください' }
    ```

    **こうなっていればOKです**: `package.jsonは変わっていません`と表示されます。

12. 次を貼り付け、Enterキーを押します。

    ```powershell
    $expectedNanoidLockLines = @('      "version": "3.3.17",','      "resolved": "https://registry.npmjs.org/nanoid/-/nanoid-3.3.17.tgz",','      "integrity": "sha512-xQLf0A3HOMlgHq0n247/LRuAOYmB7dXJ/DvAxGvsSBij45XtBSmQycu+F8ODbHwns/XyFZagyL1+J0Offw1E0g==",','      "version": "3.3.18",','      "resolved": "https://registry.npmjs.org/nanoid/-/nanoid-3.3.18.tgz",','      "integrity": "sha512-DTg4MJbGMWkfi6VZFdNt2/caMbQy4Ou+Op/hJQvGEWcnVfoA1QA+xzRKAzw9jD6+GVOOeYr/mIcuDSdug6F6+w==",'); '想定する6行を保存しました'
    ```

    **こうなっていればOKです**: `想定する6行を保存しました`と表示されます。

13. 次を貼り付け、Enterキーを押します。

    ```powershell
    $actualNanoidLockLines = @(Compare-Object -ReferenceObject (Get-Content -LiteralPath $nanoidLockBackup) -DifferenceObject (Get-Content -LiteralPath 'package-lock.json') | ForEach-Object { $_.InputObject }); "変更された行を$($actualNanoidLockLines.Count)件読み取りました"
    ```

    **こうなっていればOKです**: `変更された行を6件読み取りました`と表示されます。

14. 次を貼り付け、Enterキーを押します。

    ```powershell
    if ((Compare-Object -ReferenceObject $expectedNanoidLockLines -DifferenceObject $actualNanoidLockLines).Count -eq 0) { 'package-lock.jsonはnanoidの3項目だけ変わりました' } else { 'package-lock.jsonに想定外の変更があります。担当者へ知らせてください' }
    ```

    **こうなっていればOKです**: `package-lock.jsonはnanoidの3項目だけ変わりました`と表示されます。

15. 次を貼り付け、Enterキーを押します。

    ```powershell
    npm.cmd audit --package-lock-only --audit-level=high --json
    ```

    **こうなっていればOKです**: 結果がJSON形式で表示されます。次の手順で、重要度が高い問題が残っていないかを判定します。

16. 次を貼り付け、Enterキーを押します。

    ```powershell
    if ($LASTEXITCODE -eq 0) { '重要度が高い問題はありません' } else { '重要度が高い問題が残っているか、通信に失敗しました。表示を担当者へ知らせてください' }
    ```

    **こうなっていればOKです**: `重要度が高い問題はありません`と表示されるか、問題の重要度が下がったJSONが表示されます。

### うまくいかないとき

- 手順7が通信の失敗で止まった: 同じ行を繰り返さず、表示を担当者へ渡します。通信できる環境で手順7から再開します。
- 手順10、11、14のどれかが期待どおりでない: その場で別のコマンドを試さず、記録先の`package-lock-before-nanoid-3.3.18.json`と表示を担当者へ渡します。手順7から再開しません。
- 手順16で問題が残った: JSONの表示を担当者へ渡します。`npm audit fix`は実行しません。

## 2. 公開後に、ダウンロードした4ファイルを確認する（10分）

これは、公開ページから受け取った4ファイルが、公開時に記録したものと同じであることを確かめるために必要です。公開済みのファイルを置き換える操作はしません。

1. GitHubのプロジェクト画面で`Releases`を押します。見つからない場合は右側の`About`、`Releases`、`Tags`の順に見ます。今回の版を押し、`Assets`を開きます。
2. 次の6ファイルを1件ずつ押し、同じフォルダーへ保存します。ファイル名は変えません。

   ```text
   ORIGAMI3_0.5.0_setup.exe
   ORIGAMI3_0.5.0_x64.msi
   ORIGAMI3_0.5.0_portable.exe
   ORIGAMI3.pdf
   ORIGAMI3_0.5.0_SHA256SUMS.txt
   ORIGAMI3_0.5.0_artifact-record.json
   ```

3. Windowsキーを押しながら`E`を押します。上部の場所を示す欄へ次を入力し、Enterキーを押します。

   ```text
   C:\Users\oltot\Documents\git-projects\ORIGAMI3
   ```

4. 上部の`新規作成`、`New`、フォルダーの印のいずれかを押し、`downloaded-release-assets`と入力してEnterキーを押します。すでにある場合は開きます。
5. 手順2の最初の4ファイルを`downloaded-release-assets`へ移します。`SHA256SUMS.txt`と`artifact-record.json`は、プロジェクトの`verification\improvement-roadmap\10-supply-chain`へ移します。移動先が無い場合は、次をPowerShellへ貼り付け、Enterキーを押します。

   ```powershell
   New-Item -ItemType Directory -Force -Path "C:\Users\oltot\Documents\git-projects\ORIGAMI3\verification\improvement-roadmap\10-supply-chain"
   ```

6. PowerShellへ戻り、次を貼り付け、Enterキーを押します。

   ```powershell
   Set-Location -LiteralPath "C:\Users\oltot\Documents\git-projects\ORIGAMI3"
   ```

   **こうなっていればOKです**: 行の左側が `PS C:\Users\oltot\Documents\git-projects\ORIGAMI3>` で終わります。

7. 次を貼り付け、Enterキーを押します。

   ```powershell
   $recordRoot = Join-Path $PWD 'verification\improvement-roadmap\10-supply-chain'; '照合結果の保存先を決めました'
   ```

   **こうなっていればOKです**: `照合結果の保存先を決めました`と表示されます。

8. 次を貼り付け、Enterキーを押します。

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts/supply-chain-verify-release-hashes.ps1 -ManifestPath (Join-Path $recordRoot 'ORIGAMI3_0.5.0_SHA256SUMS.txt') -DownloadedDirectory (Join-Path $PWD 'downloaded-release-assets') -RecordPath (Join-Path $recordRoot 'ORIGAMI3_0.5.0_artifact-record.json') -ResultPath (Join-Path $recordRoot 'download-hash-verification.json')
   ```

   **こうなっていればOKです**: `download-hash-verification.json`が保存先にでき、4件すべてが一致しています。

### うまくいかないとき

- `Releases`または`Assets`が見つからない: 画面に見える見出しと住所を担当者へ渡します。別の版や別のファイルを選びません。
- 6ファイルのどれかが無い: そろっている名前を担当者へ知らせます。代わりのファイルを選びません。
- 手順8で一致しない、またはファイルが不足している: ダウンロードしたものを消さず、`download-hash-verification.json`と表示された文章を担当者へ渡します。手順2からやり直しません。

## この作業機について

この作業機のCPUは、一般的なWindowsパソコンとは違う種類（ARM64）です。そのため、一部のプログラムを組み立てられないことがあります。
エラーに`aarch64`または`ARM64`が出ていたら、CPUの種類が原因の可能性が高いです。表示を残して担当者へ渡し、自分で追加の開発用ソフトを入れません。

組み立てに失敗して `C:\Users\oltot\AppData\Local\Temp\ori3-target-supply-chain-user` が残った場合は、まずエラー表示を担当者へ渡します。今回残ったフォルダーは統括が片付けます。次回は担当者から片付けてよいと連絡を受け、`cargo`と`rustc`が動いていないことを確認してから、エクスプローラーでそのフォルダーを選び、`Delete`または「削除」を押します。
