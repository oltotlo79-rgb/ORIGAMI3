# 旧規約との対応と施行方法

この表は、旧 `CLAUDE.md`（`ebae7d8`）から見出しや対策を統合した場所と、現行の各条項を守らせる実体を示す。`統合` は内容を統合先へ移したもの、`削除` は規範でなかった記録を規約から外したものを表す。

## 変更した旧見出しの対応

| 旧見出し | 状態 | 統合先 | 理由 |
|---|---|---|---|
| `### 10.1 手元では通るのに自動検査（CI）が落ちる` | 統合 | §10.1「手元では通る…／出所の違う成果物」 | 旧§10.7.3の出所確認と同じ入力境界へ統合 |
| `## 10.5 「合格」と報告する前に、成果物を自分の目で見る（2026-08-12）` | 統合 | §10.5「成果物と形を自分の目で確かめる」 | 旧§10.7.10・§10.7.18の目視と形の契約を一元化 |
| `## 10.7 コミットと証拠の扱いで実際に起きた失敗(2026-08-12)` | 統合 | §10.7（全角括弧へ表記統一） | 配下の重複節を統合したため表記も是正 |
| `### 10.7.3 出所の分からない成果物を根拠に判断した` | 統合 | §10.1 | CIと目視で使う入力の追跡確認は同じ対策 |
| `### 10.7.4 測る前に原因を断定した` | 統合 | §10.7.4「…／混雑した測定を基準にしかけた」 | 旧§10.7.22の測定条件を同じ原因切分けへ統合 |
| `### 10.7.5 変えたcrateのテストだけで合格と判断した(2026-08-13)` | 統合 | §10.7.5（全角括弧へ表記統一）と§7 | 全体品質ゲートは§7だけを正本にした |
| `### 10.7.6 テストが追跡対象のファイルを書き換えていた(2026-08-13)` | 統合 | §10.7.6「通常テストが追跡対象や基準記録を書き換えていた」 | 旧§10.7.19の基準値再生成も同じ記録書込み問題 |
| `### 10.7.7 手元の複製検査に通ったのにCIが落ちた(2026-08-13)` | 統合 | §10.7.7「計算結果・実測値・小数を環境非依存の期待値として扱った」 | 旧§10.7.9とCPU差を一元化 |
| `### 10.7.8 関係する量を並べて比べずに、同じ場所を3回直した(2026-08-13)` | 統合 | §10.7.8「関係する量と同種箇所を全数で比べず…」 | 同じ役割・同じ欠陥形の全数走査を1条項にした |
| `### 10.7.9 検査の数値をCIの計算機で試さずに決め、2回続けてCIを落とした(2026-08-17)` | 統合 | §10.7.7 | 実測境界と小数厳密一致は計算機差の同じ対策 |
| `### 10.7.10 「未確認」の報告を受け入れて、壊れた表示を利用者に見せた(2026-08-17)` | 統合 | §10.5 | 報告を確認の代用にせず画像を見る対策を一元化 |
| `### 10.7.11 組み立て先を担当ごとに分けた結果、ディスクが尽きた(2026-08-20)` | 統合 | §10.7.11「組み立て先・複製の数え漏れと誤削除」 | 旧§10.7.12・§10.7.16と安全な清掃手順を一元化 |
| `### 10.7.12 組み立て先と複製を数え切れず、翌日に同じ容量不足を起こした(2026-08-21)` | 統合 | §10.7.11 | 配置の数え上げと複製除外を清掃手順へ統合 |
| `### 10.7.13 「サブエージェントで作業して」と3回言われたのに、Claudeが自分で作業を続けた(2026-08-22〜23)` | 統合 | §10.7.13（歴史）と§1（現行規範） | 許可コマンド表を§1だけにし、旧モデル名を規範から外した |
| `### 10.7.14 画面を同梱していない実行ファイルを起動し、利用者に接続エラーを見せた(2026-08-24)` | 統合 | §10.7.14（歴史）と§1（現行規範） | 同梱版だけを起動する現行境界へ統一 |
| `### 10.7.15 利用者への報告が会話の中だけで流れた(2026-08-25)` | 統合 | §10.7.15（全角括弧へ表記統一）と§1 | 報告記録の責任と3検査を§1へ一元化 |
| `### 10.7.16 待機中の組み立て先を、フォルダーの更新時刻だけで消した(2026-08-26)` | 統合 | §10.7.11 | 容量対策と削除保護を `scripts/clean-build-dirs.ps1` へ一本化 |
| `### 10.7.18 「裂け0・自己交差0」だけを見て、鶴になっていない作品を正しいと判断した（2026-08-28）` | 統合 | §10.5 | 既存の形条件を `crates/ori3-layers/tests/work_shape_contract.rs` の契約へ接続 |
| `### 10.7.19 壊れたモデルから記録（作品ファイル・基準値）を作り直しかけた（2026-08-28）` | 統合 | §10.7.6 | fixture再生成規律を基準値へ拡張 |
| `### 10.7.20 統括が、実在と現在値を確かめずに指示を書いた（2026-08-28）` | 統合 | §3 | 「実名で記す」の前提確認へ統合し半自動化対象にした |
| `### 10.7.21 単一のテスト対象を無条件に走らせ、規約が除外している長時間の検査を踏んだ（2026-08-28）` | 統合 | §10.6 | 単一対象でも共通skip正本を使う穴を塞いだ |
| `### 10.7.22 混雑した機械で測った値を、性能の判断に使いかけた（2026-08-28）` | 統合 | §10.7.4 | 仮説検証と測定条件を一体化 |
| `### 10.7.23 担当の返信が時間切れで切れ、生存を判断できなくなった（2026-08-28）` | 統合 | §4 | 規約でなく停滞監視の運用へ統合 |
| `### 10.7.24 作業ツリーごとに未コミット分が食い違い、担当の検査結果が本体と一致しなかった（2026-08-28）` | 統合 | §2 | worktree開始時のHEAD・差分・ベースライン確認へ統合 |
| `### 10.7.25 統括が担当の報告を鵜呑みにせず読んだ結果、方針が3回変わった（2026-08-28・記録のみ）` | 削除 | `docs/報告記録.md` の既存記録 | 失敗でなく、担当が編集前に止まり実測した正しい動作。3回の変更と修正前4/15は記録側に残る |
| `## 2. Codexの起動と使い方` | 統合 | §2「Claudeサブエージェントの起動と使い方」 | Codexから作業担当（Claudeのサブエージェント）への切替（2026-08-29）に伴う改称。区分（git書込禁止・ファイル限定・モデル使い分け）は同節に保持 |
| `## 3. Codexへ渡す指示の書き方` | 統合 | §3「作業担当へ渡す指示の書き方」 | 同上の切替に伴う改称。7項目の内容は変更していない |
## 見出し以外の統合

| 旧記述 | 統合先 | 理由 |
|---|---|---|
| §10.7.5の「変えたcrateだけで合格にしない」 | §7 | workspace・5検査・差分確認を品質ゲートへ一本化 |
| §10.7.7と§10.7.9の「実測を境目にしない／小数を厳密一致しない」 | §10.7.7 | CPU・丸め・余裕を同じ数値契約として管理 |
| §10.5と§10.7.10の「報告を確認にしない／画像を見る」 | §10.5 | 目視と形の自動契約を同じ受入節に集約 |
| §10.7.11・§10.7.12・§10.7.16の容量・複製・削除 | §10.7.11 | 計数から保護付き削除まで1手順にした |
| §10.1・§10.7.1・§7のcommit済みtreeと差分確認 | §10.1・§7 | 入力の出所とstaged差分の責任を分離して重複を除いた |
| §10.2・§10.7.13の稼働0件と委譲 | §1・§4 | 役割と停滞監視へ一元化し、安全な非重複作業に限定 |
| §10.3・§10.7.2のgit禁止と差分保護 | §1・§2・§10.7.2 | 権限境界と既存変更の保護を分けた |
| §10.7.8・§10.7.9の同種箇所の全数走査 | §10.7.8、`.github/known-defect-shapes.json` | 人の一回限り走査を件数ラチェットへ変えた |
| §10.7.14と§1のアプリ組み立て・起動 | §1 | `npm run tauri -- build --no-bundle` の同梱版だけへ統一 |
| §10.7.15と§1の報告記録 | §1 | 責任と検査実名の正本を1箇所にした |
| §10.7.17と§5の二重起動禁止 | §5 | process・9222・親PID確認を禁止条項へ集約 |
## 矛盾の是正

| 状態 | 旧 | 新 | 理由 |
|---|---|---|---|
| 是正済み | §2はSol Ultra、旧§10.7.13はSonnet/Opus | 現行は§2の作業担当（Claudeのサブエージェント）によるopus/sonnet使い分け（2026-08-29改訂）。Sol UltraはCodex稼働時、Sonnet/Opusは2026-08-22〜23の歴史 | 現行起動規則を一つにする |
| 是正済み | §1は同梱版だけ、旧§10.7.14は配信server応答Aも許可 | `npm run tauri -- build --no-bundle` の同梱版だけ | Aは利用者画面へ `ERR_CONNECTION_REFUSED` を再発させ得る |
| 是正済み | §4はCPUだけで進行判定しない、旧§10.7.23はCPU増加なら停滞でない | 成果物を主証拠、CPUと`CommandLine`は既知の長時間検査の補助証拠 | CPU単独では空回りを除外できない |
| 是正済み | 旧§10.7.11は担当終了後すぐ削除、旧§10.7.16は6時間・process・lock等を保護 | `scripts/clean-build-dirs.ps1` の判定だけを削除経路にする | 稼働状態やフォルダー時刻だけでは誤削除する |
| 是正済み | `ORI3_DEVIL_FIXTURE_OUT` で出力差替え、旧§10.7.19は固定出力 | 前者は歴史記録。新規再生成の設計条件は固定出力と書込前検査 | 現コードに前者も後者の新入口も無く、自動化済みと断定しない |
| 是正済み | §10.6は全件必須だが#13・#14を一時ignoreと記載 | 2026-08-28の最適化あり再測定で両方`changed_directions=0`となり、`#[ignore]`を解除 | 実装と一覧を一致させ、端点回帰をperformance jobで恒久実行するため |
| 是正済み | Claudeの許可作業6分類と旧§10.7.13の広い許可表 | §1の6分類内に実名コマンド表を統合 | 拡大解釈を防ぐ |
| 是正済み | §2の一時フォルダ禁止が `%TEMP%\ori3-wt-*` も禁じるように読める | 割当済みgit worktreeなら `%TEMP%` 配下も可。非worktreeへ移動しない | 場所でなくgit worktree境界が目的 |
| 是正済み | 非追跡 `scratchpad/watch-codex.ps1` と `scratchpad/live-capture-report.md` を規範実体として参照 | 監視は追跡対象の `scripts/watch-agents.ps1`、画像の裏率条件は§10.5本文 | 別worktreeに無い非追跡物へ依存しない |
| 是正済み | `crates/ori3-layers/tests/fixtures/traditional-crane-cp.ori3` | `crates/ori3-layers/tests/fixtures/traditional-crane/traditional-crane-cp.ori3` | 実在する階層を存在確認した |
| 是正済み | 残作業があれば検査前に無条件で次を投入 | 独立・非重複で安全に並行できる次作業だけを先行 | 共有差分と未検証成果への依存を守る |
| 是正済み | §5はbuildを強制終了しない、§10.7.4は上限3倍で停止 | 3倍停止は性能測定processだけ。成果物生成中のbuildは対象外 | 対象processを区別した |
## 条項を守らせる実体

`自動` は通常のhook・検査が違反を止めるもの、`半自動` は人が実行・確認する検査を持つもの、`人` は現時点で機械的な強制が無いものを表す。

| 条項 | 種別 | 守らせる実体 |
|---|---|---|
| §1 実作業の委譲 | 人 | §1の役割表。`.claude/hooks/enforce-coordinator-boundary.ps1` は**この作業機では実装できない**。`.claude/` 配下は系の保護で書き込みが拒否される（2026-08-29 実測）。機械化は§3の指示文検査（`scripts/check-agent-instruction.ps1`）と統括の`git diff`確認どまり（難:高・効:高） |
| §1 Claudeの許可コマンド | 人 | `enforce-coordinator-boundary.ps1` による許可コマンド限定も**この作業機では実装できない**。`.claude/` 配下は系の保護で書き込みが拒否される（2026-08-29 実測）。§10.7.13の直接cargo/npm拒否hookも同じ理由で撤去済み（難:高・効:高） |
| §1 同梱版だけの実機確認 | 人 | 機械化: 可（半自動）— `scripts/verify-bundled-desktop.ps1` でbuild receipt（command・exe実パス・SHA-256）を照合（難:高・効:高） |
| §1 報告記録 | 自動 | `scripts/check-report-log.ps1`、`scripts/hooks/pre-commit`、`scripts/check-release-ready.ps1`（第5検査）。§10.7.15の同名行と重複していたため2026-08-30に1行へ統合 |
| §2 モデル選択・worktree root | 半自動 | `scripts/check-agent-instruction.ps1` の項目8が指示文中の割当worktree絶対パスの記載を検査（実装済み・2026-08-29）。モデル選択理由の記録はClaude自身の行為（01-役割と委譲.md:31）で指示文の必須記載とは条文上読めないため対象外とした |
| §2 git書込・ファイル境界 | 半自動 | 指示文（prompt）への明記（gitへの書込禁止・触ってよいファイルの限定）と統括の `git diff` 確認 |
| §2 HEAD・差分・baseline | 半自動 | `git rev-parse HEAD`、`git status --porcelain`、既知欠陥形ラチェット |
| §3 指示書の7項目 | 半自動 | `scripts/check-agent-instruction.ps1` の項目1〜7が実パス・関数名・数値条件・禁止6件・段階分割・過去失敗・道具・保存先パスの記載を検査（実装済み・2026-08-29、`check-agent-instruction.test.ps1` 30例138表明）。rg証拠による実在確認は統括が引き続き担当 |
| §3 実名・現在値の存在確認 | 半自動 | 指示書へ `rg` コマンドと出力行を添付して照合 |
| §4 10分間隔の停滞監視 | 半自動 | 機械化: 可（今回適用）— `scripts/watch-agents.ps1` と同testが報告書/sourceを10分ごとに監視し40分無更新を通知、process/CommandLineは補助表示だけ（難:低・効:高） |
| §4 返信切断後の継続 | 半自動 | `scripts/check-agent-instruction.ps1` の項目9が「報告書ファイルへ書く」旨の記載を検査（実装済み・2026-08-29。依頼の9項目一覧に無かったが条文01-役割と委譲.md:57優先で追加）。実際の更新確認は引き続き`scripts/watch-agents.ps1`が担う |
| §5 ブラウザを開かない | 半自動 | `scripts/check-agent-instruction.ps1` の項目3がブラウザ・desktop.exe・配信サーバー禁止の記載を検査し、`scripts/watch-forbidden-processes.ps1` が開始時点／開始後出現を分けて表示（両方実装済み・2026-08-29。process名だけで犯人断定しない） |
| §5 `desktop.exe` 単一起動 | 半自動 | `Get-Process`、`Get-NetTCPConnection`、port所有親PID確認（9222番を含む）。§10.7.17の同内容行と重複していたため2026-08-30に1行へ統合 |
| §5 manifest・vendor承認 | 半自動 | `scripts/hooks/checks/cargo-manifest-approval.ps1`（commit-msg接続が必要） |
| §5 build中断禁止 | 人 | 機械化: 可（自動）— `.claude/hooks/block-unsafe-process-stop.ps1` と `scripts/stop-agent-safely.ps1` で対象treeのbuild子processが生成中なら停止を拒否（難:中・効:高） |
| §5 期待値・性能上限を緩めない | 人 | 機械化: 可（半自動）— `scripts/hooks/checks/no-contract-relaxation.ps1` と `.github/test-contract-baselines.json` で緩和候補を列挙して停止（難:高・効:高） |
| §5 `#[allow]` 禁止 | 半自動 | `scripts/hooks/checks/no-allow-attribute.ps1`（hook接続前） |
| §6 commit・push・署名 | 人 | 機械化: 可（半自動）— `scripts/hooks/commit-msg` で日本語・禁止prefix・Codex署名を、pre-push個別checkでremote/mainを検査（難:中・効:中） |
| §7 全品質ゲート | 半自動 | `scripts/check.ps1`、`scripts/check-ci.ps1`、pre-commit、pre-push、CI |
| §8 f64と限定的厳密演算 | 人 | 機械化: 可（半自動）— `scripts/hooks/checks/numeric-type-boundary.ps1` とallowlistで追加f32/厳密数型を走査し、承認済みQ(√2)以外を停止（難:高・効:高） |
| §8 止めずに警告 | 人 | 機械化: 可（半自動）— `.github/nonblocking-warning-contracts.json` と `scripts/check-nonblocking-warning-contracts.ps1` で既知受入testを登録・全実行（難:高・効:高） |
| §8 常設UI 4区画 | 人 | 機械化: 可（自動）— `apps/desktop/src/app-layout-contract.test.tsx` で4区画のlandmark/panel契約をVitest検査（難:中・効:高） |
| §8 Zustand 1本 | 人 | 機械化: 可（自動）— `apps/desktop/eslint-rules/zustand-single-store.js` でstore生成を1箇所、`useState` を表示用allowlistだけに限定（難:中・効:高） |
| §8 3D状態を保存しない | 人 | 機械化: 可（自動）— `apps/desktop/src/lib/persistence-contract.test.ts` で保存schemaに導出3D keyが無く、展開図+折り手順から再生一致することを検査（難:中・効:高） |
| §8 表現の完全性 | 人 | 機械化: 不可 — 未知入力を含む「実際の紙で折れる全操作」は全称命題で、有限fixture/operation matrixは既知範囲の回帰にしかならない（難:—・効:高） |
| §8 直感的UI | 人 | 機械化: 不可 — 利用者・文脈依存の知覚品質で、accessibility/lintやclick数は代用にならず実機利用者評価が必要（難:—・効:高） |
| §9 古い数値上限を復活させない | 人 | 機械化: 可（半自動）— `.github/deprecated-limit-patterns.json` と `scripts/check-deprecated-limits.ps1` で上限表現を走査し候補をreview（難:中・効:中） |
| §10.1 追跡fixtureだけを参照 | 半自動 | `scripts/hooks/checks/tracked-fixture-only.ps1`（hook接続前） |
| §10.2 安全な次作業の委譲 | 人 | 機械化: 可（半自動）— `.github/agent-assignments.schema.json` と `scripts/check-agent-assignments.ps1` でwrite範囲重複・未完依存を拒否（意味上の独立性は人、難:高・効:高） |
| §10.3 担当のgit禁止 | 半自動 | workspace sandboxと本体 `.git` 実パスの禁止 |
| §10.4 原資料との照合 | 人 | 機械化: 不可 — 折り図の矢印・前後関係と資料間矛盾は画像の意味判断で、画像一致やOCRでは正しい折り動作を保証できない（難:—・効:高） |
| §10.5 作品の形契約 | 半自動 | `crates/ori3-layers/tests/work_shape_contract.rs` を本体へ取り込み、`cargo test -p ori3-layers --test work_shape_contract` が通ったことを確認後だけ `自動` へ変更する |
| §10.5 `#[ignore]` の実測理由 | 半自動 | `scripts/hooks/checks/ignore-reason-has-number.ps1`（hook接続前） |
| §10.5 画像・PDFの目視 | 人 | 機械化: 不可 — 外形・裏表・重なり・資料の完成品質は視覚的・意味的判断を要し、生成成功や画素統計を目視の代用にできない（難:—・効:高） |
| §10.6 push前一覧と単一skip | 半自動 | `scripts/check-ci.ps1`、`scripts/hooks/pre-push`、CI |
| §10.6.1 CPU差とCI実結果 | 人 | 機械化: 可（半自動）— `scripts/check-github-ci-result.ps1` でrustc情報・commit SHA・GitHub conclusion・runner arch/logを同じreceiptへ記録（難:中・効:高） |
| §10.7.1 staged差分全文 | 半自動 | `scripts/review-staged-diff.ps1`が全文表示したstaged patchのSHA-256と確認receiptを結び、-Verifyで確認後の差分変更を拒否（実装済み・2026-08-30、`review-staged-diff.test.ps1` 7例17表明。hook接続前のため半自動） |
| §10.7.2 既存差分の保護 | 半自動 | `scripts/check-worktree-boundary.ps1`が-Initで開始時status/hashと許可path manifestを保存し、-Verifyで既存差分変更・範囲外pathを拒否（実装済み・2026-08-30、`check-worktree-boundary.test.ps1` 9例18表明。hook接続前のため半自動） |
| §10.7.4 仮説・混雑・3倍停止 | 人 | 機械化: 可（半自動）— `scripts/run-isolated-measurement.ps1` で仮説・変更量・baseline・上限を必須化し、他process数0と測定processだけの3倍停止をreceipt化（難:中・効:高） |
| §10.7.5 workspace回帰 | 自動 | `scripts/hooks/pre-commit`、`scripts/install-hooks.ps1` |
| §10.7.6 通常testの書込み禁止 | 自動 | `scripts/check.ps1` 前後のtracked差分比較 |
| §10.7.6 記録再生成の物理gate | 半自動 | 専用 `#[ignore]` testの設計条件。新入口は未統合 |
| §10.7.7 小数厳密一致・否定比較ratchet | 半自動 | `scripts/hooks/checks/known-defect-shapes.ps1` と `.github/known-defect-shapes.json`（hook接続前） |
| §10.7.7 CIログを待つ | 人 | 機械化: 可（半自動）— `scripts/wait-github-ci.ps1` が対象commitのrun完了を待ちgreenのURL/結論を保存、未完/赤なら「CI合格」を出さない（難:低・効:高） |
| §10.7.8 同種箇所の全数走査 | 半自動 | `scripts/hooks/checks/known-defect-shapes.ps1` の件数ラチェット（hook接続前） |
| §10.7.8 表示量の同単位比較 | 人 | 機械化: 可（半自動）— `.github/visual-measurements.schema.json` と `scripts/check-visual-measurements.ps1` で量・単位・scale・resolutionを同じ表へ必須化（難:中・効:中） |
| §10.7.11 build dirの安全な清掃 | 半自動 | `scripts/clean-build-dirs.ps1` と `scripts/clean-build-dirs.test.ps1` |
| §10.7.13 Claudeのcargo/npm直接実行禁止 | 人 | `.claude/settings.json`のPreToolUse hookは利用者判断で撤去済み（2026-08-29）。`.claude/hooks/block-direct-cargo-npm.ps1`自体は現存するが未接続で機械的強制は無い。代わりに①Claudeは直接cargo/npmを実行しない運用規律（§1）②担当は専用`CARGO_TARGET_DIR`を設定し`scripts/check-agent-instruction.ps1`項目11が半自動検査③`cargo test --workspace`等には§10.6の--skip4件を必須化し同項目10が半自動検査、の3点で補う |
| §10.7.14 同梱版の確認 | 半自動 | 組み立てコマンドと実行物の照合 |
| §11 version・文書鮮度 | 自動 | `scripts/check-release-ready.ps1` |
| §11 画像を含む説明書更新 | 半自動 | release-readyと目視 |
| §11.1 利用者向け解説の範囲 | 人 | 機械化: 可（半自動）— `.github/user-doc-prohibited-terms.json` と `scripts/check-user-doc-language.ps1` で内部語をhelp/manual追加差分から検出（説明価値は人、難:低・効:中） |
| §5 禁止文書(`competitive-review-2026-08-20.md`)を読まない・変更しない | 半自動 | `scripts/hooks/checks/no-prohibited-doc.ps1`（hook接続前）。指示文検査は`scripts/check-agent-instruction.ps1`項目3が兼ねる（実装済み・2026-08-30）。旧「利用者指示」欄から条文が`02-禁止事項.md`に出来たため§5へ改称 |
| §5 悪魔・1分ローズを使わない | 半自動 | `scripts/check-agent-instruction.ps1`項目3が指示文中の言及を検査（実装済み・2026-08-30。従来はdocs/rules配下に条文が無く、統括が指示文へ手で書くだけだった） |
**集計: 自動 4件 / 半自動 27件 / 人 23件（合計54件）。2026-08-29時点の55件から2026-08-30に次の4点で再集計: ①§1報告記録と§10.7.15報告記録(同一の3検査を指す重複行)、②§5 `desktop.exe`単一起動と§10.7.17単一process・9222(同一のGet-Process/port確認を指す重複行)をそれぞれ1行へ統合（自動-1・半自動-1・計-2行）、③`docs/competitive-review-2026-08-20.md`を読まない/悪魔・1分ローズを使わないの2件が`02-禁止事項.md`へ条文化され、旧「利用者指示」の無anchor行を§5へ改称のうえ悪魔・1分ローズの半自動行を新設（+1行）、④§10.7.1(`scripts/review-staged-diff.ps1`)と§10.7.2(`scripts/check-worktree-boundary.ps1`)を実装し人→半自動へ移動（各テスト付き、行数は既存2行の書き換えのみで増減なし）。決定と残条件表の「停滞監視の実体」行も§4の行・矛盾の是正表と全文重複していたため削除（tally対象外・行数のみ-1）。**
次作業の推奨は、旧①`.claude/hooks/enforce-coordinator-boundary.ps1`は§1行の通り実装不可、旧②`scripts/check-agent-instruction.ps1`・旧③`scripts/check-worktree-boundary.ps1`・旧④`scripts/review-staged-diff.ps1`はいずれも実装済み（2026-08-29・30）のため、人に残る23件のうち`.claude/`と`apps/desktop/`（scripts/の新規ファイルという範囲外）を除いた候補から選ぶ。
## 決定と残条件

| 論点 | 決定・条件 |
|---|---|
| 形契約testの施行種別 | このworktreeでは「半自動」を維持。本体への取込み後、`cargo test -p ori3-layers --test work_shape_contract` が通ったことを確認後だけ「自動」へ変更。 |
| Claudeのcargo/npm直接拒否 | `.claude/settings.json` とPreToolUse hookを適用し「自動」へ変更。明確な直接実行だけを拒否し、許可script・git・内部エラー（日本語警告）は許可する。 |
