# 作業引き継ぎ（中断時点の完全な状態）— 2026-08-28 夜

利用者の指示により全担当を切りのよいところで中断した。**この文書が再開の正本**である。
読む順: `CLAUDE.md`（41行の入口）→ `docs/rules/` 6分冊 → 本書 → `docs/報告記録.md` の 2026-08-28 の節 → 各担当の報告書末尾「中断時点の引き継ぎ」節。

---

## 1. git の状態

- 本体: `C:\Users\oltot\Documents\git-projects\ORIGAMI3`、branch `main`、HEAD `3cdd825`
- **未 push 0 件**（利用者が push 済み）
- **本体の未コミット 68 件**（Rust 分 約16,000行＋今日の追加分）
- worktree 15 本、Cドライブの空き 310 GB

### 本日コミットしたもの

| コミット | 内容 | 規模 |
|---|---|---:|
| `5487e66` | 作業規約を入口41行＋6分冊へ整理、機械化28件 | 28ファイル・+4,875/−650 |
| `c6fc95b` | 3D表示の遅延読み込み（実機確認済み） | 7ファイル・+963/−37 |
| ほか | 報告記録・引き継ぎ文書 | — |

### 1.1 退避参照（）

hook を通さない plumbing で作った完全スナップショット。push されない。
復元は `git worktree add <path> refs/wip/<name>` または `git checkout refs/wip/<name> -- <path>`。

| ref | 対象 | 規模 |
|---|---|---|
| `wip/main-rust-slice` `88fa16c` | 本体の未コミット分 | 502ファイル・+148,578/−1,819 |
| `wip/layer1` `e35e4ac` | 正方基本形（HEAD 47970e3） | 98ファイル・+55,938/−2,833 |
| `wip/collapse` `fe069c5` | 動画の方式A（HEAD 47970e3） | 60ファイル・+38,458/−1,408 |
| `wip/crane2` `72c7f0c` | 花弁折り＋首尾頭羽（HEAD b6fb6b8） | 12ファイル・+12,520/−1,037 |
| `wip/web` `2481ea0` | ブラウザ版（HEAD b6fb6b8） | 52ファイル・+10,375/−31 |
| `wip/rules` `0c69215` | 規約（HEAD ebae7d8。成果はコミット済み） | 29ファイル・+6,036/−650 |
| `wip/fixb` `88bd7df` | 端点検査の元（HEAD 885888f。本体へ移植済み） | 51ファイル・+23,199/−1,404 |
| `wip/viewer` `6534782` | 3D遅延読み込み（HEAD 3efebe2。コミット済み） | 8ファイル・+1,458/−37 |
| `wip/cifix` `21c12b0` | CI検査の修正（HEAD ff8812d。本体へ適用済み） | 2ファイル・+394/−35 |

除外: `docs/competitive-review-2026-08-20.md`、root の `traditional_crane_math_bundle/`・`traditional_crane_complete_cp.png`。
`scratchpad` は文書と差分（`*.md`/`*.patch`/`*.txt`）だけを取り込む。


---

## 2. 担当スレッド（Codex MCP）

**MCP の返信は 7,200 秒の無応答で打ち切られるが、担当は生きている**（今日10回以上発生）。成果は報告書ファイルに書かれるので、そちらを直接読むこと。

| 担当 | thread | 場所 / 組み立て先 | 報告書 |
|---|---|---|---|
| fix-a | `01a04537-e2d6-7351-9fb2-0a3fbd3b7d7a` | 本体 / `%TEMP%\ori3-target-fix-a` | `scratchpad/desktop-red-a-report.md` |
| 層順 | `01a0453a-40a7-7b42-8f46-fbd90cb06c65` | `%TEMP%\ori3-wt-layer1` / `ori3-target-layer1` | `…\scratchpad\layer-order-unit2-report.md` |
| 途中姿勢 | `01a0453c-9717-78c2-aa0d-c7f2b4410d60` | `%TEMP%\ori3-wt-collapse` / `ori3-target-collapse` | `…\scratchpad\collapse-motion-stage2-impl-report.md` |
| crane2 | `01a0467a-7a67-7ea2-a4a9-26ea164f94eb` | `%TEMP%\ori3-wt-crane2` / `ori3-target-crane2` | `…\scratchpad\crane-remaining-sites-report.md` |
| Web版 | `01a0467c-d126-7e10-9c11-25af71573047` | `%TEMP%\ori3-wt-web` / `ori3-target-web` | `…\scratchpad\web-bridge-report.md` |
| 規約 | `01a046fd-9c25-7462-ad95-7733543d5f0c` | `%TEMP%\ori3-wt-rules` | **完了・打ち切り**（成果はコミット済み） |

---

## 3. 利用者の決定（2026-08-28）

1. **検査を一時除外しない。** 折り鶴の生成器が直るまで、約16,000行のコミットを待つ（提案側の赤2件のため）。
2. **提案の記録は再測定値へ更新し、許容は4項目とも現行値を据え置く**（緩めない）。標本は当面3作品。
3. **規約を増やしすぎない。** 既存の改訂と機械化で対応する。体系化して管理しやすくする。
4. **類似ミスを確認する仕組みを持つ。**
5. `docs/competitive-review-2026-08-20.md` に触らない。悪魔・1分ローズを使わない。
6. リリースへ向けてどんどん進める。

---

## 4. 今日確定した事実（最重要）

### 4.1 アプリの折り鶴は「鶴になっていなかった」

正解は `crates/ori3-layers/tests/fixtures/traditional-crane/traditional-crane-cp.ori3`。
内部頂点44、前川 0/44、川崎 0/44、山61本すべて +180°、谷41本すべて −180°、**0°の線 0/102**。
利用者提供の `traditional_crane_math_bundle/`（10ファイル）と座標差0・頂点0/56・辺と山谷0/114・面0/59 で**完全一致**。

生成側との差（修正前）:

| 分類 | 実測 |
|---|---:|
| 参照にあって生成側に無い折り目 | 68 / 102本 |
| 生成側の余分な折り目 | 21本 |
| 山谷が逆 | 12本 |
| 頂点の一致 | 13（生成33、参照56） |
| 面の一致 | 0 / 31 |
| 独立した先端 | 3 / 4（両翼が距離 1.625064657e-15 で潰れる） |
| 完成時の一般制約違反 | 16 / 415、破棄31 |

### 4.2 直すべき箇所は5つ。うち4つは実装完了

| 工程 | 状態 | 成果物 |
|---|---|---|
| 正方基本形 | **実装中**（層順担当） | `stage11-*.patch` 13件 |
| 花弁折り | ✅ 工程終端 0/0/0 達成 | `stage8-petal.patch` `8ADD5928…DDB3B3` |
| 首・尾・頭・前羽・背羽 | ✅ 実装完了 | `stage7-neck-tail-head-wings.patch` `84AA894B…CA471B` |

花弁折きの達成値: 入力の境界 −22.500000000000021°/+22.499999999999996°、二等分線 11.249999999999989°/33.750000000000000°、欠落/余分/山谷逆 **0/0/0**、裂け 2.58946281965557501e-15、自己交差0、一般制約違反0。

**残る問題: 途中姿勢の自己交差392組。**
原因は `pose_motion.rs::poses_for_step` が `MotionPart` の driver 集合を1つの補間で動かし、0°open と ±180°close が同一 `t` で同時進行すること。
- 単純な逐次化は最終形を壊す（欠落20・逆12）
- 分割は無意味（4分割・8分割とも392のまま）
- 交差は 0°の折り目 `E33/E34/E39/E40` の4本のみ

**採用した方式（案A）**: `MotionPhase { drivers: Vec<DriverLine> }` と `FoldStep.motion_phases: Vec<MotionPhase>`。段階が空なら従来どおり（`serde(default)`）。構築箇所51件・27ファイル。**`pose_motion.rs:477` の1件は途中姿勢担当が追加済み**（`motion_phases: Vec::new(),`、SHA-256 `393E0FF4…3CE8A0`、47,272 bytes）。
基点: `replay.rs` 245,382 bytes `5BCE4B76…E3991B`、`ori3-model/src/lib.rs` 34,911 bytes `7159C090…633308F`。
見込み: 3〜5時間（自己交差0までの追加時間は不明）。

### 4.3 動画は「飛び」が解消。残るは紙の伸びと貫通

**伝承折り鶴は紙を曲げずには折れない**（恒久検査として固定済み）。
- 132×102、階数88、動ける方向14、**厳密な方向 0**、最大 support 34、**二次まで延長できる方向 0**
- 適用範囲: 正本の102本のみ／記録の山谷を維持／正確に平らな状態から連続に開始／各面を剛体
- **数値的証拠であって形式証明ではない**

gate の到達状況:

| # | 項目 | 上限 | 実測 | 判定 |
|---|---|---:|---:|:--:|
| 1 | 連続移動 | 0.02 | **0.0018829899** | ✅ |
| 2 | 裂け | 1e-6 | 7.63e-12 | ✅ |
| 3 | 貫通 | 0 | 3,395 / 1,012,979 | ❌ |
| 4 | 辺・対角 | 1.5% | 85.7273% | ❌ |
| 5 | 平面離れ | 3.3365624530% | 5.6385% | ❌ |
| 6 | 端点 | bit一致＋直前0.02 | 一致 | ✅ |
| 7 | 決定性 | 10/10 | 10/10 | ✅ |
| 8 | 初回 | 200ms | 468.3〜580.4ms | ❌ |
| 9 | 2回目以降 | 16ms | 0.2785ms | ✅ |

**今日直したもの**: 折り角の符号の反転392回（102本中101本）→ 修正済み。距離拘束の配線（訪問数 0 → 2,414,016）。

**分かっていること**:
- 中間姿勢**単体**の辺誤差は 1.4999999999985448%（上限内）
- **終点は健全**（t=1 の辺誤差 3.34843447818438422e-11）
- **85.7273% の実体は途中の sample 516**、face pair 最大は sample 515
- 競合の実測: 距離のみ 辺0%/平面43.7387%、平面のみ 辺86.1112%/平面1.4901%、交互 辺1.5%/平面3.0%
- **姿勢を増やしても効かない**（56→73→91 で 83.50→81.98→82.02%）
- **旧補正を戻す方向はすべて悪化**

### 4.4 端点検査 #13・#14 を解除

| 検査 | 実測 |
|---|---|
| #14 | `robust_stacks=1298 / changed_directions=0` |
| #13 | 110折り目・220方向、`robust_stacks=4888 / changed_directions=0` |

`#[ignore]` 解除後の通常実行も 1 passed / 0 failed。**`Reseed::distance_from_previous` の EdgeId 順加算を維持**（丸ごとコピーを避けた成果）。patch `fixb-port-delta.patch` 68,132 bytes、逆適用 exit 0。

### 4.5 提案側の赤2件は折り鶴待ち

- `changing_the_specified_position_changes_the_chosen_fold_order`（位置を変えると手順が空。29候補中0件）
- `a_fold_order_cut_short_is_detected_by_the_unfinished_shape`（前進しつつ未完成の系列が36中0件）

原因: **折り鶴の有効な最初の手が 1/27 しかない**。無効26件の内訳は**反射配置網の不整合25件**＋候補16の一般制約違反1件。

### 4.6 ブラウザ版は計算の往復まで到達

- 計算コア **8 crate すべて**がブラウザ向けに組み立て成功
- `ori3-web.wasm` 1,250,651 → `wasm-bindgen` 後 770,720 → **圧縮後 181,078 bytes**
- **実画面の操作から実 WASM の `document_new` 成功まで Node 内で通した**。製品向けの組み立てにも同一ハッシュの WASM
- `Ori3AppCore` の受け口18件を型で全件保証
- **限界**: Node での確認は実 OS の Web Worker・CSP・同一生成元の取得の証明ではない
- **残る条件**: ①本体コミット後に生成物4件を作り直す ②**実ブラウザでの受け入れ（統括が行う）**

### 4.7 規約を体系化し、機械化を28件に

| 種別 | 着手前 | 現在 |
|---|---:|---:|
| 自動 | 5 | 6 |
| 半自動 | 20 | 21 |
| **人（仕組みが無い）** | **30** | **28** |

- 総量 **650行**（分割前 670）、`CLAUDE.md` は41行の入口＋6分冊
- 統括が同日追記した §10.7.18〜25 の8節は**すべて既存章へ統合または削除**
- 矛盾12件を是正
- **類似ミスの台帳**（`.github/known-defect-shapes.json`）＋ラチェット。登録46件。**本物の欠陥2件を発見して修正済み**
- フック検査5件、`scripts/watch-agents.ps1`、**統括が `cargo`/`npm` を直接実行できない設定**
- 残る28件のうち26件は機械化可能。推奨上位5件は「統括の境界」「Codex起動の包み」「指示書の検査」「既存差分の保護」「staged差分の受領証」

---

## 5. 統括の判断の誤り（3回。すべて担当が実装前に止めた）

| # | 誤り | 訂正 |
|---|---|---|
| 1 | 花弁折りを複数の `MotionPart` に分ける | `MotionPart` は**空間領域**で時間の順序を表せない → 折り手順に段階を持たせる案Aへ |
| 2 | 中間姿勢を細分化すれば辺の伸びが直る | 姿勢を56→91に増やしても 83.50→82.02% でほぼ不変 → 姿勢自体の誤差が原因 |
| 3 | 終点の縮約で辺が壊れている | t=1 の辺誤差は 3.35e-11 で健全 → **途中の sample 516 が実体** |

**いずれも損失ゼロ。** 規約に入れた「担当が前提の違いを示して止めたら、指示より担当の実測を優先する」が機能している。

---

## 6. 次に行うこと（優先順）

1. **層順担当の正方基本形を完了させる**（唯一の律速）
2. `stage8-petal.patch` と `stage7-neck-tail-head-wings.patch` を適用し、段階的に検証
3. **折り手順の段階の仕組み**（案A）を実装 → 途中姿勢の自己交差392組を0へ
4. `scripts/check.ps1` を通し、**約16,000行をコミット**（未到達の単元が最大9つ減る）
5. Web版の生成物4件を作り直し、**統括が実ブラウザで受け入れ確認**
6. 動画の gate 3・4・5・8 を詰める（sample 515/516 が焦点）
7. 規約の「人」28件のうち推奨上位5件を機械化

---

## 7. 運用の状態

- **アプリは終了済み**（`desktop.exe` 0個）。実機確認は `npm run tauri -- build --no-bundle` の同梱版のみ使う
- 組み立て先: `%TEMP%\ori3-target-*`。空き 310 GB。片付けは `scripts/clean-build-dirs.ps1`
- 停滞監視: `scripts/watch-agents.ps1`（コミット済み）。10分間隔・40分無変化で通知
- **フォントの入手を利用者へ依頼中**（`%TEMP%\ori3-font-audit-stage5` へ Noto Sans JP 公式ファイル）
- 依存の更新提案5件は**当面 merge しない**（変更履歴が取得できず、説明書PDFへの影響を確認できないため）
- ワークフローの `uses:` 19件のうち**固定は4件のみ**。リリース前の項目として記録

---

## 8. 再開の手順

1. `git status`、`git worktree list`、`git for-each-ref refs/wip` で §1 と一致を確認
2. 各担当の報告書末尾「中断時点の引き継ぎ」節を読む
3. §6 の順に再開。担当は `codex-reply` で該当 thread へ「引き継ぎ節を読んで再開」と送る。**スレッドが失効していたら、報告書の引き継ぎ節を起点に新規セッションを起こす**
4. 退避の作り直しは `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/snapshot-worktrees.ps1`（引数なしで9本すべて、`-Name <名前>` で1本）

---

## 9. 中断の完了確認（2026-08-28 21:29）

| 担当 | 引き継ぎ節 | 報告書の状態 |
|---|---|---|
| fix-a | ✅ |  |
| 層順 | ✅ | 209,905 bytes、SHA-256 。段階別 patch **16件**すべて逆適用確認済み。旧WIP 1件は「一括適用禁止」と明記。組み立て成功、lib試験 77/77、製品内の一時コード0件 |
| 途中姿勢 | ✅ | 252,135 bytes、SHA-256 。 47,272 bytes （依頼した1行のみ） |
| crane2 | ✅ | 155,760 bytes、SHA-256 。Stage 7/8 patch とも逆適用 exit 0。**Stage 10 は設計のみで書きかけ無し** |
| Web版 | ✅ | 173,393 bytes。実ブラウザ受け入れ手順 （23,077 bytes）も作成済み |

- ** は 0 個**（起動していない）
- **退避 9本すべて更新済み**（上表の ref）
- 稼働していた cargo 2件は、中断指示どおり**強制終了していない**（規約 §5）
- 本体の未コミット 68件は、折り鶴の生成器が直るまでコミットしない（利用者の決定）

**この文書と  と各担当の報告書の引き継ぎ節があれば、作業ツリーが消えても完全に再開できる。**
