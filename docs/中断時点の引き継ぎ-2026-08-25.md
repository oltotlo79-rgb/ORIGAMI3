# 中断時点の引き継ぎ — 2026-08-25 23:15

**この文書だけを読めば、ロスなく再開できることを目的とする。**

**利用者の指示（2026-08-25）**: 「Codexのリミットが近いので、作業の切りのついたところで
どこまで実施したかと残り作業を詳細に記録し、確実にロスなく再開できる状態にして中断して。
間違っても作業を中途半端な状態で終わらないこと」

---

## 0. 再開するとき、最初にやること

```powershell
cd C:\Users\oltot\Documents\git-projects\ORIGAMI3
git log --oneline -5
git status --porcelain
```

**そのうえで、この文書の §3「担当ごとの引き継ぎ」を読む。**
**各担当の詳しい記録は `scratchpad/` の報告書にある。実パスは §3 の表に記す。**

**注意: `scratchpad/` は `.gitignore` 対象で、コミットされていない。**
**この計算機の中にしか無い。他の計算機では読めない。**

---

## 1. いまの到達点

### 1.1 改善ロードマップ（106単位）

**2026-08-25 21:01 に証拠へ基づいて数え直した値**（`docs/improvement-roadmap-2026-08-24.md` §17.3）

| 施策 | 単位数 | 完了 | 実行中 | 止まり | 未着手 |
|---|---:|---:|---:|---:|---:|
| 1 提案の負荷非依存 | 5 | 3 | 0 | 2 | 0 |
| 2 手順の入れ替え | 3 | 1 | 2 | 0 | 0 |
| 3 30作品の標本 | 8 | 1 | 0 | 6 | 1 |
| 4 巨大ファイルの分割 | 36 | 4 | 0 | 0 | 32 |
| 5 ダイアログの操作性 | 6 | 5 | 1 | 0 | 0 |
| 6 起動の軽量化 | 6 | 4 | 0 | 0 | 2 |
| 7 文書の機械検証 | 13 | 3 | 0 | 1 | 9 |
| 8 FOLD 1.2 | 15 | 1 | 6 | 1 | 7 |
| 9 実利用者の検証（準備） | 3 | 0 | 3 | 0 | 0 |
| 10 供給網 | 6 | 0 | 1 | 5 | 0 |
| 11 一斉折り | 5 | 0 | 5 | 0 | 0 |
| **合計** | **106** | **22** | **18** | **15** | **51** |

**この表は 2026-08-25 21:01 時点であり、その後の完了分を反映していない。**
**再開時に §3 の担当ごとの記録を読んで、表を更新すること。**

**表の更新規則**（§17.3 に明記）: **担当の完了報告ごとに、その施策の完了単位と状態を同じ更新で直す。**
**この規則は 2026-08-24 から 2026-08-25 21:01 まで一度も守られなかった。**
**機械で検査する `scripts/check-roadmap-recount.ps1` の案が §17.3 にある（未実装）。**

### 1.2 利用者から報告された不具合

| # | 報告 | 状態 |
|---|---|---|
| 1 | 2つの線を合わせても折れない | **修正済み・実機確認済み・push済み** |
| 2 | 「動かす側」の意味が分からない | **修正済み・実機確認済み・push済み** |
| 3 | 見えないはずの線が見える | **修正済み・実機確認済み・push済み** |
| 4 | 裏が見える／取り消しで戻らない／経路で表示が変わる | **中核は修正済み。確定時の跳びが残る** |
| 5 | （作業中に発見）やっこさんが57度で折れない | **修正済み・push済み** |
| 6 | （作業中に発見）「折れません」と嘘の案内 | **修正済み・push済み** |

### 1.3 利用者が提示した競合との差3件（**106単位に入っていない**）

| # | 内容 | 状態 |
|---|---|---|
| 1 | 万能分子が未実装（展開図が平らに折れる保証がない） | **予定なし。実測と見積もりを依頼中** |
| 2 | 充填が最適の約85% | **予定なし。同上** |
| 3 | 骨格が放射状のみ（胴の途中から枝分かれできない） | **予定なし。同上** |

**利用者へ「修正予定はあるか」と問われ、「予定に入っていない」と回答した。**
**統括の抜けである。競合との差として洗い出しながら、ロードマップの単位に落としていなかった。**
**実測と見積もりを `scratchpad/propose-gap-survey.md` へ出させている（中断時点で未完）。**

---

## 2. 利用者の決定（**再開時に必ず守る**）

| 日付 | 決定 |
|---|---|
| 2026-08-25 | **106単位すべてを完了させてからリリースする**（施策4の内部の作り直しも今版に含める） |
| 2026-08-25 | **プッシュは不具合の修正が終わり次第。全部終わってからまとめてではない** |
| 2026-08-25 | **リリースを伴わない保全目的のプッシュでは、監査は最低限でよい**（`--no-verify` を使う） |
| 2026-08-24 | 一斉折りは**案B**（つまみ1本・一時表示・手順に記録しない・保存しない・取り消し対象にしない） |
| 2026-08-24 | FOLD 1.2 は**限定profile**を承認（F/U の完全往復は不採用） |
| 2026-08-24 | **Linux対応は見送り**（全機能を確認できないため） |
| 2026-08-25 | 確定時の跳びは、**動かす折り目は指定角度に厳密**と**同じ書類からは必ず同じ形**を優先し、**跳ばないことは諦める** |
| 2026-08-25 | コード署名は**未署名で配布**（SignPath不承認。実績が積み上がってから再申請） |

**統括（Claude）の役割**: 指示・確認・検査・git操作・アプリの起動と終了・利用者への報告と記録**のみ**。
**実作業はすべてCodexへ委譲する。** **`cargo` と `npm` を統括が直接叩かない**（`CLAUDE.md` §10.7.13）。

**触ってはいけないファイル**: `docs/competitive-review-2026-08-20.md`
（利用者の指示。開かない・参照しない・コミットしない。`rg` は `--glob '!docs/competitive-review-2026-08-20.md'` で除外）。

**使ってはいけない作品**: 悪魔（devil）と1分ローズ（rose）。**折り鶴・やっこさん・カエル・鳥の基本形を使う。**

---

## 3. 担当ごとの引き継ぎ

**各担当の報告書の末尾に「## 中断時点の引き継ぎ（2026-08-25）」の見出しがある。**

| 担当 | 報告書（実パス） | 中断時の状態 |
|---|---|---|
| 30作品の標本（施策3） | `scratchpad/corpus-3a-report.md` | **収束済み（完了まで進めた）** |
| 文書の機械検証（施策7 D2〜D10） | `scratchpad/doc-link-7d-report.md` | **収束済み** |
| 一斉折り（施策11 画面側） | `scratchpad/foldall-11-report.md` | **収束済み。再組み立てと再撮影が未実施** |
| FOLD 1.2（施策8） | `scratchpad/fold-8a-report.md` | **収束指示済み。結果は報告書を見る** |
| CSSの分割（施策4-D） | `scratchpad/split-4a-report.md` | **完了。ファイル解放済み** |
| 裏面・取り消し・経路依存 | `scratchpad/backface-undo-report.md` | **完了** |
| 「折り返す紙」の操作性 | `scratchpad/fold-tool-usability-report.md` | **完了** |
| 供給網（施策10） | `scratchpad/supply-chain-report.md` | **完了。通信が要る手順は §5.2** |
| 書き方の点検 | `scratchpad/clippy-motion-report.md` | **`motion.rs` は完了。`commands.rs` は FOLD 担当へ移管** |
| 重なり順の検査の整理（施策4-E） | `scratchpad/surface-order-4e-report.md` | **中断時点で開始直後。状態は報告書を見る** |
| 3D場面の分割（施策4-C C1〜C4） | `scratchpad/scene-4c-report.md` | **中断時点で開始直後。状態は報告書を見る** |
| 提案の差3件の実測 | `scratchpad/propose-gap-survey.md` | **中断時点で未完** |
| ロードマップの数え直し | `scratchpad/roadmap-recount-report.md` | **完了** |

---

## 4. 再開したら、最初に片付けるべきこと（**優先順**）

### 4.1 【最優先】書き方の点検が通らない

```
apps/desktop/src-tauri/src/commands.rs
  pose_solve                 8引数（#[tauri::command(async)]）
  pose_solve_core_with_mode  8引数（pub(crate)）
```

**`cargo clippy --workspace --all-targets -- -D warnings` が、この2件で止まる。**
**通らないとコミットもpushもできない**（`scripts/hooks/pre-commit` と `pre-push`）。

**確定した事実**: **`pose_solve` は画面からの7入力と Tauri の `State` で必ず8引数になる。**
**署名・IPCを維持し、`#[allow]` も使わない条件では、この警告を原因から消せない。**

**統括が許可した直し方**: **`pose_solve` を要求構造体へ変え、`apps/desktop/src/ipc/client.ts` も同時に直す。**
**呼び出しは `rg` で6件（定義1・呼び出し5）。**

```
apps/desktop/src-tauri/src/commands.rs                                  内部2件
apps/desktop/src-tauri/src/commands/tests/canonical_pose_contract.rs
apps/desktop/src-tauri/src/surface_order_sa_warm_path_canonical.rs
apps/desktop/src-tauri/src/commands/tests/angle_finish_continuity.pending.rs
```

**`#[allow]` を足さない。全体の閾値を変えない**（`CLAUDE.md` §5）。

### 4.2 一斉折りの再組み立てと再撮影

**「戻る」ボタンの位置は直り、専用検査12/12合格・`npm run build` 合格。**
**しかし、旧い実行ファイルが動いていたため、新しい実行ファイルを作れていない。**

**再開したら、次を担当に指示する。**

```powershell
$env:CARGO_TARGET_DIR = "C:\Users\oltot\AppData\Local\Temp\ori3-target-foldall"
cd apps/desktop
npm run build
npm run tauri -- build --no-bundle
```

**撮り直すもの**: 100%で「いつもの表示に戻る」がスクロールなしで見えること／
0%へ戻したとき開始前と画素単位で一致すること。**画像に実行ファイルのSHA-256を写し込む。**

**未解決の判断**: **`fitCamera` は一斉表示中も使えるが、その後 0% へ戻しても視点が変わったままになる。**
**「0%へ戻すと完全復帰する」という主張と食い違う。** **扱いを決める必要がある。**

### 4.3 30作品の標本 — 打ち切りは混雑が原因だった

**負荷のない状態で E-04 を3回測り直した結果**

```
21,402ms / 24,465ms / 24,153ms   （上限 30,000ms）
3回とも候補を返した。打ち切りに当たっていない。
```

**つまり「30,921ms で超過」は、8件の担当が同時に計算機を使っていたことが原因。**
**`CLAUDE.md` §10.6 と `ci.yml` と `check-ci.ps1` の3ファイルを変える必要は、おそらく無い。**

**ただし、設計上の問題は残っている。**

> **`corpus_all_thirty_cases_match_recorded_current` は、**
> **「30作品のうち9件が打ち切りで失敗すること」を期待値にしている。**
> **打ち切りは壁時計で測る。CIの計算機は手元より約3.6倍遅い実測がある。**
> **「9件が失敗する」は計算機が変われば8件にも12件にもなる。原理的にCIで再現しない。**
> **`CLAUDE.md` §10.7.7・§10.7.9 が禁じている形そのもの。**

**指示した直し方**: **「30作品がどう折れるか」と「何秒かかるか」を分ける。**
**折れ方は打ち切りのない予算で走らせ、速さは別に測る。**
**打ち切りのない全30件の測定は、外部負荷で中止したため「未測定」と記録されている。**

**正本の目標到達は 6/30 のまま。`recorded_current`・事前target・fixture は変更していない。**

### 4.4 進捗文書が実態より進んでいる（29件）

**182項目の照合で、49件の食い違いを検出した。**

| 分類 | 件数 | 意味 |
|---|---:|---|
| A: 本当に完了 | 5 | **ロードマップの記載が古い**（検査を実行して確認済み） |
| **B: 完了していない** | **29** | **`docs/progress.md` の「完了」が誤り**（受入未実施または検査の範囲不足） |
| C: 別物を指していた | 15 | 対応付けの誤り |
| D: 判断できない | 0 | — |

**分布**: M2=19、M3=11、M4=18、M5=1。

**主因**: **進捗のTask番号だけで照合し、実装・手動受入・コミット履歴を同じものとして扱った。**
**M3の3件は、監査する側の見出し解析の不具合**（「作業18」の比較実験なのに直前のTask 3-4を引き継いだ）。

**統括の判断: いま直さない。** 理由は2つ。

1. **29件を一度に書き換えると、何を根拠に変えたかが追えなくなる。**
2. **確かめずに文書だけを合わせるのは、`CLAUDE.md` §5 が禁じる「不具合を直さず合格に見せかける」のと同じ。**

**再開時は、B の29件を「受入未実施」と「検査の範囲不足」に分け、**
**それぞれ何をすれば完了と言えるかを決めるところから始める。**

**逆方向（ロードマップは完了だが進捗が未完了）は、現行の監査が調べていない。存在しないとは結論できない。**

### 4.5 角度を厳密に同一視している箇所18件（**未調査**）

**やっこさんが折れなくなった原因は、`+180度` と `−180度` を同じ角度として最短側へ補間したことだった。**
**`CLAUDE.md` §10.7.5 に記録済みの誤りの再導入である。**

**修正後、同じ形の危険を全数監査させた結果**

| | 箇所 |
|---|---:|
| 角度を厳密に同一視 | **18** |
| 周辺を含む総数 | **35** |
| **動きや候補の順位に影響し得る要注意** | **18** |
| 終点・幾何・診断として妥当 | 17 |

**不具合とは断定していない。変更もしていない。** **再開後の調査対象。**
**詳細は `scratchpad/backface-undo-report.md` にある。**

### 4.6 確定時の跳び（課題A）— **設計案に誤りがあった**

**統括は「確定前の形を順位付けにだけ使えば経路依存は戻らない」と考えていたが、誤りだった。**

> **順位が変われば選ばれる候補が変わり、最終形が変わる。経路依存が戻る。**

**この線での実装は中止した。** **別の方法を検討する必要がある。**

**現状**: **原因になった最短側への補間だけを戻した。**
**書類由来の固定候補、決定的な順位付け、Canonical で warm を排除する契約は残っている。**

---

## 5. 利用者にお願いすること

### 5.1 プッシュ

**統括の環境では `git push --no-verify` が安全装置で拒否される。**
**通常のプッシュは検査が10分で終わらず時間切れになる。**

**利用者に次を実行していただく。**

```
! git push --no-verify origin main
```

### 5.2 外部通信が要る検査

**この環境は外部通信ができない。** **次は利用者の環境で実行していただく必要がある。**

**手順は `scratchpad/supply-chain-user-steps.md` に、そのままコピーできる形でまとめてある。**

- 依存部品の脆弱性の確認（Rust・npm）
- 固定参照の確認
- 配布物4件のハッシュ作成とダウンロード後の照合

---

## 6. 中断時点の環境

**`desktop.exe`**: 統括が終了させる。

**組み立て先**（`CLAUDE.md` §10.7.11・§10.7.12。**再開前に空き容量を確認し、不要なものを消す**）

```
C:\Users\oltot\AppData\Local\Temp\ori3-target-foldfix
C:\Users\oltot\AppData\Local\Temp\ori3-target-backface
C:\Users\oltot\AppData\Local\Temp\ori3-target-css
C:\Users\oltot\AppData\Local\Temp\ori3-target-foldall
C:\Users\oltot\AppData\Local\Temp\ori3-target-fold
C:\Users\oltot\AppData\Local\Temp\ori3-target-corpus
C:\Users\oltot\AppData\Local\Temp\ori3-target-clippy
C:\Users\oltot\AppData\Local\Temp\ori3-target-scene
C:\Users\oltot\AppData\Local\Temp\ori3-target-surfaceorder
C:\Users\oltot\AppData\Local\Temp\ori3-target-doclink
C:\Users\oltot\AppData\Local\Temp\ori3-target-propose-survey
```

**数えるときは `verification/target-*` と `verification/ci-*` も数える**（§10.7.12）。
**1つあたり 5〜20GB。**

**git worktree**（消す前に `git worktree list` で確認。消したら `git worktree prune`）

```
C:\Users\oltot\AppData\Local\Temp\ori3-yakko-head    （やっこさんの調査で作った複製）
```
